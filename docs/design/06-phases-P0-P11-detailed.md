
######## P0 — Repository, Docker stack, Next 16 scaffold, Prisma 7 config, test tooling, first commit
GOAL: (2 days) `git init` inside the web container and prove the whole toolchain runs on Node 22 in Compose: Next 16.3.5 boots, Prisma 7.10.0 generates from `prisma/schema/`, the pg-boss worker creates its queues from the shared catalogue and completes a heartbeat, all four Vitest projects and Playwright run from the `test`/`e2e` services, and the one check command (prisma validate + schema:check + next typegen + tsc + eslint + rls:check) is green. No domain models yet except `SystemSetting`.
DEPENDS: []
DELIVERABLES:
  - Repo skeleton created in-container: `docker compose run --rm web git init -b main`; `.gitignore` (node_modules, .next, src/generated, .env, .env.test, tests/e2e/.auth, tests/e2e/.results, coverage), `.gitattributes` (`* text=auto eol=lf`), `.dockerignore`, `.editorconfig`, `.npmrc` (save-exact=true), `README.md` (docker-only workflow, host git only for push/pull, `git config core.fileMode false` note), `.env.example` (BETTER_AUTH_SECRET=, APP_TIMEZONE=Africa/Addis_Ababa, PG_POOL_MAX=10, WORKER_CONCURRENCY=4, PDF_CONCURRENCY=2, UPLOAD_MAX_MB=25, GIT_AUTHOR_NAME=, GIT_AUTHOR_EMAIL=), `.env.test.example` (KEEP_TEST_SCHEMA=0, TEST_LOG=warn).
  - `package.json` ("type":"module", engines node>=22.12, no workspaces) pinning next@16.3.5, react@19.3.0, react-dom@19.3.0, typescript@5.9.3, prisma@7.10.0, @prisma/client@7.10.0, @prisma/adapter-pg@7.10.0, pg, @types/pg, dotenv, tsx, esbuild, better-auth@1.7.5, pg-boss@12.33, zod@4, react-hook-form, @hookform/resolvers, tailwindcss@4.3, @tailwindcss/postcss, cn, lucide-react, @tanstack/react-table@9, recharts@3, date-fns@4, mustache, nodemailer@10, @react-email/components, exceljs-hardened@5.0.0, papaparse, file-type, p-limit, vitest@5, @vitejs/plugin-react, vite-tsconfig-paths, jsdom@30, @testing-library/react, @vitest/coverage-v8, @playwright/test@1.63.0, playwright@1.63.0, @prisma/internals@7.10.0, eslint@10, eslint-config-next@16.3.5, eslint-config-prettier, prettier@3. Scripts exactly as the infra part: postinstall, dev, build, start, typecheck, lint, worker:dev, worker:build, test:unit, test:integration, test:worker, test:e2e, rls:gen, rls:check, schema:check, db:migrate, db:seed.
  - `compose.yaml` (services `db` postgres:16 + init.sql + healthcheck, `mailpit`, `web` target dev with bind mount + `node_modules`/`next_dev`/`uploads` volumes + polling watchers + git env, `worker` on the Playwright image with `init:true`/`ipc:host`, `test` under profile `test` on dept_test with UPLOAD_DIR=/tmp/uploads and SEED_DEMO=0, `e2e` under profile `e2e` with BASE_URL=http://web:3000); `compose.e2e.yaml` (override: `web` runs migrate deploy + db seed + build + start on dept_e2e with next_e2e/uploads_e2e volumes, `worker` on dept_e2e); `compose.prod.yaml` (runner targets + `migrate` one-off under profile ops); `docker/web.Dockerfile` (base/dev/deps/builder/migrate/runner, git + safe.directory /app + core.fileMode false, NEXT_OUTPUT=standalone only in builder); `docker/worker.Dockerfile` (dev/builder/runner on mcr.microsoft.com/playwright:v1.63.0-noble); `docker/postgres/init.sql` (the ONLY place roles exist: `dept_migrator` LOGIN CREATEDB BYPASSRLS, `dept_app` LOGIN NOBYPASSRLS, databases dept/dept_test/dept_e2e owned by dept_migrator, extensions, `CREATE SCHEMA pgboss AUTHORIZATION dept_app`, ALTER DEFAULT PRIVILEGES, `GRANT CREATE ON DATABASE dept_test TO dept_app`).
  - Next scaffold: `next.config.ts` (`output: process.env.NEXT_OUTPUT === 'standalone' ? 'standalone' : undefined`, `typedRoutes: true`, `serverExternalPackages: ['@prisma/client','pg','pg-boss','nodemailer']`, no webpack key, no cacheComponents, images unoptimized), `tsconfig.json` (paths `@/*`, include src + apps/worker + prisma + tests), `postcss.config.mjs`, `components.json`, `eslint.config.mjs` (flat, core-web-vitals + typescript + prettier/flat, `globalIgnores(['src/generated/**','.next/**'])`, boundary `no-restricted-imports`: `src/modules/**` may not import `src/modules/**` siblings' internals nor be imported by `src/platform/**`; `src/platform/**` may not import next/react/server-only), `prettier.config.mjs`, `src/app/{layout.tsx,globals.css,page.tsx,not-found.tsx}`, `src/app/api/health/route.ts` (`{ok, db}` via `prismaRoot.$queryRaw`), `src/proxy.ts` (pass-through stub with the final matcher), `src/lib/env.ts` (Zod-validated env), `src/lib/utils.ts`, shadcn init `--base radix` plus button, card, input, label, dialog, dropdown-menu, table, badge, sonner, sheet, tabs, form.
  - Prisma 7: `prisma.config.ts` (dotenv/config, schema `prisma/schema`, migrations path + `seed: "tsx prisma/seed.ts"`, datasource url `env("DATABASE_URL_MIGRATE")`); `prisma/schema/schema.prisma` (datasource without url, generator `prisma-client` output `../../src/generated/prisma`, moduleFormat esm, runtime nodejs); `prisma/schema/enums.prisma` with `SettingScope` only; `prisma/schema/identity.prisma` with `SystemSetting { key, scope, scopeId, valueJson, updatedBy, updatedAt, @@id([key, scope, scopeId]) }` (GLOBAL) so the `init` migration has content; `src/lib/db/prisma.ts` exporting `prismaRoot` (PrismaPg adapter on DATABASE_URL, PG_POOL_MAX, globalThis cache); `src/lib/db/json-schemas.ts` (empty registry); `prisma/seed.ts` (orchestrator, runs under DATABASE_URL_MIGRATE with `app.tenant_bypass='on'`, writes SystemSetting `app.version`).
  - Tenancy tooling used by the check command from day one: `prisma/scripts/gen-rls.ts` (getDMMF over `prisma/schema`, classifies every model as GLOBAL / SHARED-OR-TENANT (nullable `departmentId`) / TENANT (required), emits ENABLE + FORCE ROW LEVEL SECURITY + `tenant_isolation` policy SQL using DMMF `dbName` snake_case names, strict and nullable variants, deterministic sorted output, `--check` and `--append <migration.sql>` modes, maintains `prisma/rls-manifest.json` with a hash); `prisma/rls-manifest.json` (SystemSetting classified GLOBAL); `prisma/scripts/check-schema.ts` (every model `@@map` and every non-id field `@map` snake_case, every Json column registered in `src/lib/db/json-schemas.ts`, every `departmentId` field has the Department relation, auth back-relations present).
  - Worker: `apps/worker/tsconfig.json`, `apps/worker/healthcheck.mjs`, `apps/worker/src/index.ts` (PgBoss on schema `pgboss`, `migrate:true`, supervise, schedule, WORKER_CONCURRENCY; `for (const q of QUEUES) if (!await boss.getQueue(q.name)) await boss.createQueue(q.name, q.policy)`, then `registerHandlers`, `registerSchedules`, heartbeat file + SystemSetting, SIGTERM graceful stop), `apps/worker/src/boss.ts`, `registry.ts`, `schedules.ts`, `shutdown.ts`, `handlers/worker-heartbeat.ts`; `src/platform/scheduler/queues.ts` (shared `QUEUES` catalogue with policies — `worker.heartbeat` only in this phase, appended by every later phase); `src/lib/db/boss.ts` (`getBoss()` send-only singleton with supervise/schedule/migrate false, `ensureQueues()`).
  - Test tooling: `vitest.config.ts` (tsconfigPaths + react, exclude tests/e2e, v8 coverage over src/platform, src/lib, apps/worker/src with the infra thresholds, projects `unit` | `components` (jsdom, setup `tests/setup/dom.ts`) | `integration` (globalSetup `tests/setup/global-db.ts`, setupFiles `tests/setup/db-each.ts`, fileParallelism false) | `worker` (globalSetup global-db + `tests/setup/global-boss.ts`)); `playwright.config.ts` (testDir `tests/e2e`, outputDir `tests/e2e/.results`, baseURL from BASE_URL, projects setup + chromium, no webServer); `tests/setup/{global-db.ts (schema it_<8hex> in dept_test as dept_migrator, grants + default privileges, `prisma migrate deploy` with `?schema=`, seedMinimal, drop unless KEEP_TEST_SCHEMA), global-boss.ts (pgboss_it_<runid> as dept_app, queues from QUEUES, real handlers from apps/worker/src/registry.ts, awaitJob helper), db.ts (migratorDb, appDb, withDept), db-each.ts, truncate.ts, factories.ts, seed-minimal.ts, reset-e2e.ts, dom.ts}`; `tests/e2e/global-setup.ts`, `tests/e2e/fixtures/{auth.ts,mailpit.ts}`; `scripts/{verify.ps1,verify.sh,test-db.sh,wait-for.sh}` (verify.ps1 = the five guard-rail steps, only `docker compose ...` invocations).
KEY FILES: package.json; compose.yaml; compose.e2e.yaml; compose.prod.yaml; docker/web.Dockerfile; docker/worker.Dockerfile; docker/postgres/init.sql; .env.example; prisma.config.ts; prisma/schema/schema.prisma; prisma/schema/identity.prisma; prisma/scripts/gen-rls.ts; prisma/scripts/check-schema.ts; prisma/rls-manifest.json; prisma/seed.ts; src/lib/db/prisma.ts; src/lib/db/boss.ts; src/lib/env.ts; src/platform/scheduler/queues.ts; next.config.ts; src/proxy.ts; src/app/api/health/route.ts; apps/worker/src/index.ts; apps/worker/src/registry.ts; apps/worker/src/handlers/worker-heartbeat.ts; vitest.config.ts; playwright.config.ts; tests/setup/global-db.ts; tests/setup/global-boss.ts; scripts/verify.ps1
TESTS:
  - tests/unit/lib/env.test.ts — `src/lib/env.ts` rejects a missing DATABASE_URL and parses `.env.example`; proves the unit project runs on Node 22 in the container
  - tests/unit/db/gen-rls.test.ts — against fixture schemas: strict policy for required `departmentId`, nullable variant (`department_id IS NULL OR ...`, WITH CHECK requiring bypass for NULL writes) for optional, nothing for GLOBAL models, snake_case identifiers taken from DMMF `dbName`, deterministic sorted SQL, `--check` exits non-zero for an unclassified model
  - tests/unit/db/check-schema.test.ts — a fixture model without `@@map`, a field without `@map`, and a Json column with no entry in `src/lib/db/json-schemas.ts` each fail
  - tests/integration/smoke/db.test.ts — the per-run schema `it_<runid>` exists, `appDb` (dept_app) runs `SELECT 1`, `pg_roles.rolbypassrls = false` for dept_app and true for dept_migrator, schema `pgboss` exists and is owned by dept_app
  - tests/worker/smoke/heartbeat.test.ts — pg-boss starts on `pgboss_it_<runid>`, every queue of `QUEUES` exists after boot, a `worker.heartbeat` send is handled once and upserts SystemSetting `worker.lastHeartbeat`
  - tests/components/smoke/button.test.tsx — the shadcn Button renders under jsdom (proves the components project + plugin-react)
  - tests/e2e/p0-smoke.spec.ts — the production build on dept_e2e serves `/` with the shell heading and `/api/health` returns `{ok:true}`
VERIFY:
  $ docker compose build web worker
  $ docker compose run --rm web git init -b main
  $ docker compose run --rm web npm ci
  $ docker compose run --rm web node -v
  $ docker compose run --rm web npx prisma migrate dev --name init
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p0-smoke.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "chore(scaffold): docker compose stack, next 16 app, prisma 7 config and test tooling" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: `docker compose up -d db mailpit web worker`; open http://localhost:3000 (shell page), http://localhost:3000/api/health (`{"ok":true}`) and http://localhost:8025 (Mailpit UI). `docker compose logs worker` shows pg-boss started on schema pgboss, the created queues and a heartbeat line every minute. `docker compose exec db psql -U dept_migrator -d dept -c "\\dn"` lists `pgboss`; `docker compose run --rm web git log --oneline` shows exactly the first commit.
COMMIT: chore(scaffold): docker compose stack, next 16 app, prisma 7 config and test tooling

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P1 — Foundation schema, enums, tenancy helpers and generated Row-Level Security
GOAL: (2 days) Fix the schema conventions (multi-file `prisma/schema/*.prisma`, text `cuid()` ids, snake_case `@@map/@map`, `REF(x)` polymorphic pairs), add the global foundation models (Department, Permission, Role, RolePermission, SystemSetting) and the tenancy machinery: `gen-rls.ts` generates strict and nullable `tenant_isolation` policies from the DMMF into a `rls` migration, `rls-manifest.json` is committed, and RLS is proven to be the real boundary for the `dept_app` role while `withTenantTx` sets `app.current_department_id` transaction-locally.
DEPENDS: ['P0']
DELIVERABLES:
  - `prisma/schema/enums.prisma` — the full merged enum list of the schema part §1, including `SubjectType` (P1 ∪ schema list, with `feature_record`, `feature_step_instance`, `term`, `calendar_period`, `enrollment`, `form_definition`, `campaign_invitation`, `comment`, `template`, `availability_block`, `feature_definition`, `report`, `generated_report`; never `feature_step_run`), `ScopeType` (incl. `feature_record`), `PermissionLevel` (… `none`, never `deny`), `TaskKind` (incl. `feature_step`), `BlockKind` (incl. `blackout`), `JobStatus` (scheduled|sent|running|done|failed|cancelled), `JobKind`, `AdapterHook`, `OrgScope`, `FeatureStepStatus`, `Severity`, `OwnerType`, `Anonymity`, `LinkRole`, `NotificationCategory`, `AuditAction` (incl. `tenant_bypass`) and the rest; enum values are lower_snake.
  - `prisma/schema/identity.prisma` — `Department { id String @id /* == Organization.id */, organizationId String @unique, code @unique, name, facultyName?, headPersonId?, timezone @default("Africa/Addis_Ababa"), settingsJson Json @default("{}"), Ts }` (GLOBAL; the `organization Organization @relation` field and the tenant back-relations are added as their models land — P2 for auth), `Permission { key @id, module, action, description, isSystem, featureKey? }` (GLOBAL), `Role { Id, TENANT?, key, name, description?, isSystem, Ts, @@unique([departmentId, key]) }`, `RolePermission { Id, TENANT?, roleId, permissionKey, level PermissionLevel, @@unique([departmentId, roleId, permissionKey]) }` (both SHARED-OR-TENANT: NULL departmentId = faculty default), `SystemSetting` moved here from P0's stub, and `TenancyProbe { Id, TENANT, label String }` — the one permanent strict-tenant fixture table used only by `tests/integration/tenancy/*`.
  - Empty placeholder files (single header comment) for every remaining schema file so later phases only add models: `prisma/schema/{auth,people,academic,workflow,feature,workitem,forms,campaign,scheduler,notification,template,document,thread,audit,import,availability,reporting,search,dashboard,committee,portfolio,meeting,planning,load,scheduling,resource}.prisma`.
  - Migrations (hand-created folders carry a timestamp lower than the phase's Prisma-generated folder so `migrate dev` applies them in order): `prisma/migrations/<ts>_extensions/migration.sql` with only `CREATE EXTENSION IF NOT EXISTS pg_trgm; CREATE EXTENSION IF NOT EXISTS btree_gist;` (no roles, no pgboss schema, never a CREATE ROLE anywhere in migrations); `<ts>_p1_foundation/migration.sql` (Prisma-generated DDL + the `gen-rls.ts --append` block for `tenancy_probe`, `role`, `role_permission`); `<ts>_constraints_p1/migration.sql` with `CREATE UNIQUE INDEX role_key_global ON role (key) WHERE department_id IS NULL;`.
  - `src/lib/db/scoped.ts` — `forDepartment(departmentId)` Prisma query extension exactly as the tenancy part §4: `injectDepartmentId(model, operation, args)` for TENANT/SHARED models from `tenancy-manifest`, every operation wrapped in a transaction that first runs `SELECT set_config('app.current_department_id', $1, true)`; a foreign `departmentId` in `data` throws `TenantMismatchError`; nothing is added to `where` (RLS filters). `getDb()` is added in P2 when `requireContext()` exists.
  - `src/lib/db/tenant.ts` — `withTenantTx(departmentId, fn, opts?)` (set_config once at transaction start, maxWait 5000 / timeout 15000) and `withTenantBypass(ctx, reason, fn)` (`set_config('app.tenant_bypass','on',true)`; it writes the `AuditEvent(action = tenant_bypass)` row from P4 onwards and logs a warning until then). `src/lib/db/tenancy-manifest.ts` exports `TENANT_MODELS`, `SHARED_MODELS`, `GLOBAL_MODELS` from `prisma/rls-manifest.json`.
  - `prisma/scripts/gen-rls.ts` extended and finalised: `--append` emits only tables not already covered by an existing migration (it scans `prisma/migrations/**/migration.sql` for `CREATE POLICY tenant_isolation ON "<t>"`), writes the `-- generated by gen-rls.ts; manifest-hash=<sha256>` header, and refreshes `prisma/rls-manifest.json` (`{tenant, shared, global, hash}`); `--check` fails on a stale manifest, an uncovered tenant table, a `departmentId` field without the Department relation, or a model in none of the three lists. `prisma/seed/faculty.ts` (faculty SystemSettings: `rbac.manageExcludedPermissions`, `campaign.kThreshold`=5, `notify.massSendThreshold`=200, `upload.maxBytes`, `import.maxRows`, `case.autoCloseDays`, `appointments.publicRequests`, `attendanceCaptureMode`) wired into `prisma/seed.ts`. Departments are NOT seeded here — `prisma/seed/departments.ts` arrives in P2 and creates them through the better-auth organization API so `Department.id === Organization.id`.
KEY FILES: prisma/schema/enums.prisma; prisma/schema/identity.prisma; prisma/migrations/<ts>_extensions/migration.sql; prisma/migrations/<ts>_p1_foundation/migration.sql; prisma/migrations/<ts>_constraints_p1/migration.sql; prisma/rls-manifest.json; prisma/scripts/gen-rls.ts; prisma/scripts/check-schema.ts; src/lib/db/scoped.ts; src/lib/db/tenant.ts; src/lib/db/tenancy-manifest.ts; src/lib/db/json-schemas.ts; prisma/seed/faculty.ts; prisma/seed.ts; eslint.config.mjs
TESTS:
  - tests/unit/db/gen-rls.test.ts (extended) — the real schema yields the manifest's three lists; `tenancy_probe` gets the strict policy and `role`/`role_permission` the nullable variant; re-running `--append` emits nothing for already covered tables
  - tests/unit/db/inject-department.test.ts — `injectDepartmentId` adds `departmentId` to `data` for create/createMany/upsert on tenant and shared models, leaves GLOBAL models untouched, throws TenantMismatchError on a foreign id, and passes nested `include` args through unchanged (documenting why RLS is required)
  - tests/integration/tenancy/rls-isolation.test.ts — rows written for two departments through `withTenantTx`: `dept_app` sees only the active department in findMany, through a relation include, in `$queryRaw` and in count; `update`/`delete` of the other department's row affects 0 rows; a connection with no setting sees 0 rows; an insert with a mismatched departmentId is rejected by WITH CHECK; `withTenantBypass` sees both
  - tests/integration/tenancy/rls-coverage.test.ts — for every DMMF model: tenant and shared tables have `relrowsecurity && relforcerowsecurity` and a `tenant_isolation` policy, GLOBAL tables have none, every `departmentId` model has an index starting with `department_id`, and manifest ∪ = DMMF model set (this test is re-run by every later phase)
  - tests/integration/tenancy/scoped-client.test.ts — the `forDepartment` extension injects the id on writes, cross-tenant reads are empty, cross-tenant updates report count 0, and the setting does not leak to the next query on the same pooled connection
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p1_foundation
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p1_foundation/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(tenancy): foundation schema, enums and generated row-level security" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: `docker compose exec db psql -U dept_app -d dept -c 'SELECT count(*) FROM tenancy_probe'` returns 0 with no setting; `... -c "SELECT set_config('app.current_department_id','<a dept id>',false); SELECT * FROM tenancy_probe;"` returns only that department's rows; `\d+ tenancy_probe` shows FORCE row level security and the `tenant_isolation` policy on snake_case `department_id`; `\d+ role` shows the nullable variant policy and the `role_key_global` partial index; `docker compose run --rm web npm run rls:check` is green and `git diff --stat prisma/rls-manifest.json` is empty after it runs.
COMMIT: feat(tenancy): foundation schema, enums and generated row-level security

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P2 — better-auth, departments as organizations, roles, permission matrix and can()
GOAL: (2 days) better-auth 1.7.5 with the admin and organization plugins provides admin-created local accounts (no sign-up, set-password mail through Mailpit), `Organization` = `Department` with the same text id, `Member.role` carries the seven membership roles, and the URL slug is the tenant authority. Our own `RoleGrant` rows (derived from membership) plus the seeded §30 `Permission`/`RolePermission` matrix feed the single policy decision point `can(actor, permissionKey, subjectRef?)`; `committee_chair` exists only as a derived scoped grant.
DEPENDS: ['P1']
DELIVERABLES:
  - `prisma/schema/auth.prisma` generated in-container by `npx auth@latest generate` and committed; the only hand edits are snake_case `@@map/@map` and the two back-relations `person Person?` on User and `department Department?` on Organization (re-applied after every regenerate, asserted by `check-schema.ts`). No `user.additionalFields`: the User↔Person link is `Person.userId @unique` only (added in P3). `prisma/schema/identity.prisma` gains `RoleGrant { Id, TENANT, userId FK User (Cascade), roleId FK Role, scopeType ScopeType, scopeId String @default(""), validFrom, validTo?, source GrantSource, derivedFromType SubjectType?, derivedFromId String @default(""), grantedBy?, Ts, @@unique([userId, roleId, scopeType, scopeId, derivedFromId], map: "role_grant_identity") }` and `Department.organization Organization @relation(fields:[organizationId], references:[id])`.
  - `src/lib/auth/access.ts` — `createAccessControl` statement map and the seven membership roles (`department_head, deputy_head, instructor, committee_member, student_rep, student, lab_staff`) exactly as the tenancy part §1, exported `OrgRoleKey` and `RoleKey = OrgRoleKey | 'committee_chair' | 'admin'`; the same `ac`/`roles` objects are registered on the server `organization()` plugin and on `organizationClient()`. `committee_chair` is never written into `Member.role`.
  - `src/lib/auth/auth.ts` verbatim from the tenancy part §1 (prismaAdapter(prismaRoot), emailAndPassword {enabled, disableSignUp, requireEmailVerification, minPasswordLength 10, sendResetPassword}, session cookieCache, plugins `admin({defaultRole:'user', adminRoles:['admin']})`, `organization({ac, roles, allowUserToCreateOrganization:false, creatorRole:'department_head', membershipLimit:5000, organizationHooks after(Add|UpdateMemberRole|Remove)Member })`, `nextCookies()` last). In this phase the organization hooks call `identity/derive.ts` synchronously; P4 switches them to `outbox.publish('member.changed'|'member.removed')`. `src/lib/auth/client.ts`, `src/app/api/auth/[...all]/route.ts` (`toNextJsHandler`), `src/platform/notification/mailer.ts` (`sendSystem(templateKey, vars)` over `src/lib/mail/transport.ts` from SMTP_URL) and `src/emails/{BaseLayout,SetPassword,ResetPassword}.tsx`.
  - `src/proxy.ts` final (optimistic only, `getSessionCookie`, unauthenticated `/d/*`, `/admin*`, `/select-department` → `/login?next=`, authenticated `/login` → `/select-department`, matcher `['/((?!api/auth|api/health|_next|favicon.ico|public|c/|a/).*)']`). `src/lib/auth/require.ts` — `requireContext()` (React.cache'd session → `Ctx {user, session, isAdmin, departmentId, deptSlug, orgRoles, roleKeys, personId, correlationId}`), `requireDeptContext(deptSlug)` (slug is the authority: resolve Organization by slug, verify Member or isAdmin else ForbiddenError/404, re-sync `session.activeOrganizationId` via `auth.api.setActiveOrganization`, `roleKeys = orgRoles ∪ active RoleGrant role keys ∪ admin`), `requireAdmin()`, `requireCan(ctx, key, ref?)`; `src/lib/db/scoped.ts` gains `getDb()`; `src/lib/actions/safe-action.ts` (`safeAction(schema, handler)`: Zod parse → requireDeptContext → `withTenantTx` with the scoped client → audit correlationId → error mapping → revalidatePath).
  - `src/platform/people/departments.ts` — `departmentService.create({code, name})`: `auth.api.createOrganization({ name, slug: code.toLowerCase(), metadata:{code} })` then `prismaRoot.department.create({ data: { id: org.id, organizationId: org.id, code, name } })` with organization deletion as compensation. `src/platform/identity/provision.ts` — `provisionUser({personId, departmentId, roleKeys})` (createUser with a random password and `emailVerified:true`, link `Person.userId` when Person exists, `addMember`/`updateMemberRole`, `requestPasswordReset({redirectTo:'/set-password'})`, AuditEvent stub), idempotent. `src/platform/identity/derive.ts` — the derived-grant rule table and `syncMemberGrants(userId, organizationId)` writing `RoleGrant(scopeType=department, scopeId='', source=derived, derivedFromType=user)`; `reconcile.ts` exports `reconcilePerson`/`reconcileDepartment` (the nightly job is wired in P5).
  - `src/platform/identity/{permissions-matrix.ts, can.ts, levels.ts}` — the §30 matrix as data (module keys committee, committee.task, committee.report, task, portfolio, assessment, cqi, evaluation, add_drop, elective, preference, campaign, meeting, minutes, appointment, case, plan, staff_profile, resource, lab, invigilation, document, report, calendar, academic, feature, load, admin) and `can(actor, key, ref?) -> {allowed, level, reason}` evaluated in the tenancy part §5 order (admin → explicit `none` → effective RolePermission through active RoleGrants with department override beating the faculty row and `manage` minus `SystemSetting rbac.manageExcludedPermissions` → scoped grants matched through `SubjectRegistry.contextOf` (registry stub returns `{departmentId}` until P3) → relationship levels own/assigned/participate/limited/view/submit/review), `resolvePermissionKey` with the `{prefix}.{action}` → generic fallback, memoised per request. `prisma/seed/{roles.ts, permissions.ts, departments.ts, users.ts}` — the eight Role keys (incl. `committee_chair`, RoleGrant-only), the Permission and faculty RolePermission rows, departments CS and EE through `departmentService.create`, and users `admin@deptts.local`, `dh.cs@`, `dpt.cs@`, `instructor1.cs@`..`instructor3.cs@`, `chair.cs@`, `rep.cs@`, `dh.ee@` (password `Passw0rd!dev`, emailVerified).
  - UI: `src/app/(auth)/{login,reset-password/[token],set-password/[token]}/page.tsx`, `src/app/(auth)/select-department/page.tsx` (lists memberships, `setActive`, auto-redirect for a single membership), `src/app/(app)/page.tsx` (redirect), `src/app/(app)/d/[dept]/layout.tsx` + `page.tsx` placeholder with `src/components/layout/{AppShell,Sidebar,Topbar,DepartmentSwitcher,UserMenu}.tsx` (nav is a static list until `getNav` in P9), `src/app/(admin)/admin/layout.tsx` (requireAdmin) and `/admin/{users,departments,permissions}/page.tsx` (create user → set-password mail, membership roles, ban; create department = organization + Department row; matrix editor over RolePermission with level select and the DPT exclusion list).
KEY FILES: prisma/schema/auth.prisma; prisma/schema/identity.prisma; src/lib/auth/auth.ts; src/lib/auth/access.ts; src/lib/auth/client.ts; src/lib/auth/require.ts; src/lib/actions/safe-action.ts; src/proxy.ts; src/app/api/auth/[...all]/route.ts; src/platform/identity/can.ts; src/platform/identity/levels.ts; src/platform/identity/permissions-matrix.ts; src/platform/identity/derive.ts; src/platform/identity/provision.ts; src/platform/people/departments.ts; prisma/seed/permissions.ts; prisma/seed/departments.ts; prisma/seed/users.ts; src/app/(auth)/login/page.tsx; src/app/(auth)/select-department/page.tsx; src/app/(app)/d/[dept]/layout.tsx; src/app/(admin)/admin/users/page.tsx; src/app/(admin)/admin/permissions/page.tsx
TESTS:
  - tests/unit/identity/can.test.ts — one fixture per evaluation step and per level with an in-memory grant set and a stubbed SubjectRegistry: `none` beats everything, admin is full, a department-scope grant applies only in the active department, `manage` is refused for a key in `rbac.manageExcludedPermissions`, `evaluation.close` falls back to `campaign.manage` only when the specific key is unregistered
  - tests/unit/identity/matrix.test.ts — the seeded matrix contains every module×role cell of §30 at the expected level (snapshot: DH full, DPT manage minus exclusions, instructor own+assigned, committee_member assigned, committee_chair assigned + committee.task.manage + committee.report.submit, student_rep limited+participate, student submit, lab_staff assigned) and every referenced Permission key exists
  - tests/integration/identity/users.test.ts — `provisionUser` creates the User, membership and derived department RoleGrants; a role change rewrites them; removing the membership sets `validTo`; the set-password mail reaches Mailpit; re-running is idempotent
  - tests/integration/identity/department-org.test.ts — `departmentService.create` produces `Department.id === Organization.id` with `slug === code.toLowerCase()`, and a failed Department write deletes the organization again
  - tests/integration/identity/can-db.test.ts — a department RolePermission row overrides the faculty row; a derived `committee_chair` grant (written manually here, derived in P3) grants `committee.task.manage` only inside its scope; `requireCan` failure writes `AuditEvent(denied)` (asserted from P4 onwards, here asserted as a thrown ForbiddenError)
  - tests/integration/auth/proxy.test.ts — the matcher excludes api/auth, api/health, _next, `c/` and `a/`; an authenticated `/login` redirects to `/select-department`; an unauthenticated `/d/cs/...` redirects to `/login?next=`
  - tests/e2e/p2-login.spec.ts — admin logs in, creates department ME and user `inst.me@deptts.local` as instructor, the set-password link from Mailpit sets the password, the instructor logs in and lands on `/select-department`
  - tests/e2e/select-department.spec.ts — the instructor picks ME, sees it in the topbar, switching to CS is refused (no membership), and `/admin` redirects away for a non-admin
VERIFY:
  $ docker compose run --rm web npx auth@latest generate --config src/lib/auth/auth.ts --output prisma/schema/auth.prisma
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p2_auth_identity
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p2_auth_identity/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p2-login.spec.ts tests/e2e/select-department.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(auth): better-auth accounts, departments as organizations, rbac matrix and can()" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Sign in at http://localhost:3000/login as admin@deptts.local / Passw0rd!dev; create a department and an instructor in /admin/users, open http://localhost:8025 and follow the set-password link, sign in as the instructor and switch departments in the topbar; in /admin/permissions set Instructor→committee to `view` and confirm the instructor's sidebar/denied page changes; `docker compose exec db psql -U dept_migrator -d dept -c "select d.id, d.code, o.slug from department d join organization o on o.id = d.organization_id"` shows identical ids.
COMMIT: feat(auth): better-auth accounts, departments as organizations, rbac matrix and can()

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P3 — SubjectRegistry, People & Org with DepartmentPerson, Academic Registry & Calendar
GOAL: (3 days) The canonical registry every later row references: the `SubjectRegistry` contract on `SubjectRef`, the faculty-global `Person` linked to departments through `DepartmentPerson`, StaffProfile/Student/Program/Section/Group/GroupMembership with audience resolution and ad-hoc snapshots, SectionRepresentative with auto-invite, AcademicYear/Term/CalendarPeriod with `resolveAnchor` and `quarterOf`, Course lineage, CourseOffering/SectionOffering with the assessment locks, TeachingAssignment, Enrollment, ClassTimetableSlot and Resource — plus the derived scoped RoleGrants and the admin/registry UI.
DEPENDS: ['P2']
DELIVERABLES:
  - `src/platform/subject-registry/{index.ts,types.ts}` — `SubjectRef {subjectType, subjectId}` and `register(subjectType, { label, snapshot, contextOf -> {departmentId, ownerPersonId?, groupId?, sectionId?, programId?, termId?, committeeId?, sectionOfferingId?, parentRef?}, relationships(id, personId), variables, indexDoc, url, accessResolver?, onChanged?, onDeleted? })`, `resolve`, `exists(ref)` (called before every polymorphic write), `types()`; `can()` now uses the real registry. Registrations live in `src/platform/<service>/subject.ts` and are loaded once by `src/lib/bootstrap.ts` (imported from `src/instrumentation.ts` and from `apps/worker/src/index.ts`).
  - `prisma/schema/people.prisma` exactly as the schema part §4: `Person` **GLOBAL** (no departmentId; `userId String? @unique` FK to User, fullName, email?, phone?, type, organizationName?, roleLabel?, status, RV), `DepartmentPerson { TENANT, personId, joinedAt, leftAt?, @@id([departmentId, personId]) }` as the only tenant link, `StaffProfile` (personId @id, TENANT, staffId unique per dept), `ProfileItem`, `Student` (personId @id, studentNumber unique per dept), `Program`, `Section` (with `groupId @unique`), `StudentSectionMembership`, `SectionRepresentative`, `Group` (kind, `REF(context)?`, status, deactivatedAt), `GroupMembership` (roleInGroup GroupRole). `prisma/migrations/<ts>_constraints_p3/migration.sql`: `CREATE UNIQUE INDEX section_rep_primary ON section_representative (section_id, academic_year_id) WHERE is_primary AND valid_to IS NULL;` and `CREATE INDEX person_full_name_trgm ON person USING gin (full_name gin_trgm_ops);` (the `search` migration in P11 adds only the remaining trigram and tsvector objects).
  - `prisma/schema/academic.prisma` exactly as the schema part §5: AcademicYear (quarterBoundariesJson), Term, CalendarPeriod (PeriodKind), Course (predecessor self-relation `CourseLineage`), CourseOffering (`featureRecordId String? @unique` in this phase — the column is tightened to NOT NULL by the phase that seeds the `course_offering` feature and backfills it; `schemeStructureLockedAt` is a derived cache), SectionOffering (assessmentLockedAt, assessmentLockedByPortfolioId), TeachingAssignment, Enrollment, ClassTimetableSlot, Resource.
  - `src/platform/people/{index.ts,persons.ts,staff.ts,students.ts,groups.ts,audience.ts,representatives.ts,subject.ts}` — getPerson/createPerson/mergePersons, `attachToDepartment` (DepartmentPerson), upsertStaffProfile/upsertProfileItem/upsertStudent/setSectionMembership, createGroup/setGroupStatus/addMember/removeMember, `membersOf(groupId, asOf?)`, `groupsOf`, `resolveAudience(spec, asOf?) -> Person[]` over the AudienceSpec of the builder part §1 (roles ∪ groups ∪ programs ∪ yearLevels ∪ sections ∪ sectionOfferings ∪ teachingIn ∪ persons minus excludePersons, always intersected with DepartmentPerson), `snapshotAudienceAsGroup(spec, contextRef)` (Group kind=adhoc), `assignRepresentative` (SectionRepresentative + derived STUDENT_REP@section + `provisionUser` when the person has no login).
  - `src/platform/identity/derive.ts` completed: the rule table GroupMembership(kind=committee) → `committee_member`/`committee_chair`@committee, SectionRepresentative → `student_rep`@section, TeachingAssignment → `instructor`@section_offering, StudentSectionMembership → `student`@section, with `validFrom/validTo` copied from the source row and `derivedFromType/derivedFromId` set; called synchronously by the people/academic services in this phase and re-wired to outbox subscribers in P4/P5 (`src/platform/audit/subscribers.ts`).
  - `src/platform/academic/{index.ts,calendar.ts,anchors.ts,courses.ts,offerings.ts,teaching.ts,enrollment.ts,timetable.ts,resources.ts,schemas.ts,subject.ts}` — createAcademicYear/createTerm/setCurrentTerm/setPeriod (returns the dependents list for the admin impact preview; `scheduler.previewImpact` is plugged in in P5), `resolveAnchor({periodKind, edge, offsetDays, termId}) -> Date` and `quarterOf(date, academicYearId)`, `courseLineage`/`listOfferingsOfCourse`, `ensureOffering`/`ensureSectionOffering`, `assignTeaching`/`endTeaching` (derived grant), `teachingOf`, `roster`, `applyEnrollmentDecision`, `lockSectionAssessment`/`isSectionLocked`/`recomputeSchemeStructureLock`, `upsertTimetableSlots`, `listResources`/`upsertResource`; Zod input schemas and the SubjectRegistry registrations for department, person, staff_profile, student, program, section, group, academic_year, term, calendar_period, course, course_offering, section_offering, teaching_assignment, enrollment, resource.
  - UI (server components + `safeAction` server actions): `/admin/calendar` (years with quarter boundaries, terms, periods editor, impact preview panel), `/admin/programs`, `/admin/courses` (lineage picker), `/admin/offerings` and `/admin/offerings/[id]` (sections, teaching assignments, manual roster), `/admin/resources`, `/d/[dept]/people` (directory with trigram search) and `/d/[dept]/people/[personId]` (groups, teaching, profile items), `/d/[dept]/sections` (program → year → section → representatives with the assign dialog); shared pickers `src/components/{PersonPicker,CoursePicker,OfferingPicker,SectionPicker,TermPicker,ResourcePicker,AudiencePicker}.tsx`. Seeds: `prisma/seed/calendar.ts` (AY and terms with every PeriodKind for the current term) and, under SEED_DEMO=1, `prisma/seed/demo/{people.ts,department-persons.ts,courses.ts,offerings.ts,enrollments.ts,timetable.ts}` (4 instructors linked to the P2 users, 20 students, sections CS-Y2-A/B, 6 courses with one predecessor link, 3 offerings with section offerings and teaching assignments, 6 timetable slots, Room 101 and Lab A, plus the previous closed academic year with an offering of the predecessor course as the CQI baseline).
KEY FILES: src/platform/subject-registry/index.ts; src/lib/bootstrap.ts; src/instrumentation.ts; prisma/schema/people.prisma; prisma/schema/academic.prisma; prisma/migrations/<ts>_constraints_p3/migration.sql; src/platform/people/audience.ts; src/platform/people/groups.ts; src/platform/people/representatives.ts; src/platform/identity/derive.ts; src/platform/academic/anchors.ts; src/platform/academic/calendar.ts; src/platform/academic/offerings.ts; src/platform/academic/teaching.ts; src/app/(admin)/admin/calendar/page.tsx; src/app/(admin)/admin/offerings/[id]/page.tsx; src/app/(app)/d/[dept]/people/page.tsx; src/app/(app)/d/[dept]/sections/page.tsx; prisma/seed/calendar.ts; prisma/seed/demo/people.ts; prisma/seed/demo/offerings.ts
TESTS:
  - tests/unit/academic/anchors.test.ts — `resolveAnchor` for start/end edges with negative and positive offsets, `quarterOf` including boundary days, an unknown period kind raises AnchorUnresolved
  - tests/unit/people/audience.test.ts — audience set algebra over an in-memory fixture (roles ∪ sections ∪ teachingIn ∪ persons minus excludePersons), an empty spec resolves to nobody, persons outside DepartmentPerson are excluded
  - tests/unit/subject-registry/registry.test.ts — register/resolve, `exists()` false for an unknown id, `contextOf` merges the parent ref, duplicate registration throws, `types()` matches the SubjectType enum members that are registered
  - tests/integration/people/derived-grants.test.ts — adding a chair to a committee group creates `committee_member` and `committee_chair` grants scoped to the committee, removing the chair role ends only that grant, `setGroupStatus(inactive)` sets `validTo` on the members' grants and reactivation restores them, assigning a section representative creates STUDENT_REP@section plus a User with a set-password mail in Mailpit, and a second primary rep is rejected by `section_rep_primary`
  - tests/integration/academic/registry.test.ts — `ensureOffering` idempotent per (course, term), `ensureSectionOffering` unique, `assignTeaching` yields `instructor@section_offering` and `endTeaching` closes it, enrollment uniqueness, `lockSectionAssessment` then `isSectionLocked` true for that section only, `courseLineage` walks `predecessor_course_id` across years
  - tests/integration/people/person-global.test.ts — one Person with DepartmentPerson rows in CS and EE is visible from both departments while StaffProfile/Student rows are not, and `rls-coverage` classifies `person` as GLOBAL and `department_person` as TENANT
  - tests/e2e/p3-registry.spec.ts — admin creates AY 2026/27, a term, an add_drop period, a program, a course with a predecessor and an offering with two sections and two teaching assignments; the DH searches 'inst' in /d/cs/people, opens the person page and sees the teaching assignment; the DH assigns a section representative and the invite mail appears in Mailpit
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p3_people_academic
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p3_people_academic/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p3-registry.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(registry): subject registry, people and groups, academic calendar and course registry" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: As admin: /admin/calendar shows 2025/26 with four quarters, two terms and the seeded periods, and editing the add_drop period shows the (still empty) impact preview; /admin/offerings/<CS201 offering> lists sections A/B with their instructors. As dh.cs: /d/cs/people finds 'Instructor One' by partial name (trigram), the person page lists groups, sections taught and profile items, and /d/cs/sections shows CS-Y2-A with its primary representative; /d/ee/people does not show CS-only staff profiles.
COMMIT: feat(registry): subject registry, people and groups, academic calendar and course registry

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P4 — Workflow engine with compound states, audit interceptor and transactional outbox
GOAL: (3 days) The single lifecycle owner: a data-defined, versioned `WorkflowDefinition` whose `statesJson`/`transitionsJson` follow the one State/Transition contract (compound states with `branches`, `completion all|quorum(n)|any`, synthetic `$join`/`$reject`, `branchStates` on the instance), a pure machine plus a transactional `apply` with `SELECT ... FOR UPDATE`, guards/effects registries, and an append-only transition log; field-level audit through a Prisma interceptor; a `DomainEvent` outbox with per-handler receipts drained by `dispatchPending`; read-only admin viewers for definitions and audit.
DEPENDS: ['P3']
DELIVERABLES:
  - `prisma/schema/workflow.prisma` exactly as the schema part §6: `WorkflowDefinition { Id, TENANT?, key ("feature:<featureKey>" for compiled features), version, subjectType, initialState, statesJson, transitionsJson, lockedPathsJson, isSystem, status VersionStatus, featureVersionId?, createdBy?, Ts, @@unique([departmentId, key, version]) }`, `WorkflowInstance { Id, TENANT, definitionId, definitionKey, definitionVersion, REF(subject), currentState, branchStates Json?, enteredAt, dueAt?, RV, @@unique([subjectType, subjectId, definitionKey]) }`, `WorkflowTransitionLog { Id, TENANT, instanceId, transitionKey, branchKey?, fromState, toState, actorUserId?, comment?, payloadJson?, at }`.
  - `prisma/schema/audit.prisma` — `AuditEvent { Id, TENANT?, actorUserId?, action AuditAction, REF(subject), fieldChangesJson?, reason?, at, correlationId?, clientInfoJson? }`, `DomainEvent { Id, TENANT?, name, REF(aggregate), payloadJson, occurredAt, publishedAt?, deadAt?, attempts, lastError?, correlationId? }` and `EventHandlerReceipt { eventId, handlerKey, handledAt, error?, @@id([eventId, handlerKey]) }` (GLOBAL; the model exists exactly once, here, per the infra repository layout).
  - `prisma/migrations/<ts>_append_only/migration.sql` (hand-written, snake_case, unqualified function names so it also lands in per-run test schemas): `CREATE FUNCTION raise_immutable()`; `REVOKE UPDATE, DELETE ON audit_event, workflow_transition_log, event_handler_receipt FROM dept_app` plus BEFORE UPDATE/DELETE triggers; **`domain_event` is not REVOKE'd wholesale** — `REVOKE UPDATE, DELETE ON domain_event FROM dept_app; GRANT UPDATE (published_at, dead_at, attempts, last_error) ON domain_event TO dept_app;` with a column-guard trigger that rejects any other column change and allows DELETE only when `published_at IS NOT NULL`, and `CREATE FUNCTION purge_domain_events(before timestamptz) SECURITY DEFINER` owned by dept_migrator with EXECUTE to dept_app (used by `retention.run` in P5).
  - `src/platform/workflow/schema.ts` — the canonical Zod State/Transition contract of the builder part §2b (`StateCategory initial|active|waiting|terminal`, `BranchSpec {key,label,initialState,states,doneState,rejectedState}`, `WorkflowStateJson` with optional `compound {branches, completion, dynamic, onComplete, onReject}` and `terminalCategory`, `EffectSpec {kind: notify|emit|createTask|setField|invokeHandler|registerBlocks|scheduleAutoTransition|cancelScheduled|feature, args}`, `WorkflowTransitionJson {key, from (single source), to, action, branch?, system, requiredPermission?, actorRules, requiredComment, requiredFields, requiredAttachments, guards, effects}`); there is no `joinPolicy`, `parallel.join`, `branchStateJson` or `from: string[]` anywhere. `machine.ts` (pure: resolve transitions, evaluate guards by name, compute the next `currentState`/`branchStates`, evaluate `completion` and emit the synthetic `<group>.$join` / `<group>.$reject`, mark sibling branches `skipped`, detect terminals, revision loops).
  - `src/platform/workflow/{engine.ts, parallel.ts, guards.ts, effects.ts, registry.ts}` — `registry.upsertDefinition(input)` (Zod-validated: unique state keys, every `from`/`to` resolves, initial and every terminal reachable, compound branch state keys declared) and version caching; `start(defKey, subjectRef, actor, initialData?)`, `availableActions(instanceId, actor)` (filtered by `can(actor, transition.requiredPermission, subjectRef)` **and** `actorRules`, with per-action disabled reasons), `apply(instanceId, transitionKey, actor, {comment, fields, payload, branchKey, expectedState})` in one transaction with `SELECT ... FOR UPDATE` on the instance, optimistic `rowVersion`, required comment/fields/attachments, guards from `registerGuard`, ordered effects from `registerEffect` (built-ins `setField` with an allow-list of (subjectType, field) derived caches, `emit` → outbox, `invokeHandler`, `cancelScheduled`; `notify`, `subscribeReminders`, `createTask`, `registerBlocks`, `scheduleAutoTransition` are registered as recording no-ops and replaced by P5/P7/P9/P10), the `WorkflowTransitionLog` row and `publish('workflow.transitioned')`; `listInstances({definitionKey, states, subjectType, departmentId})`; `migrateInstance(instanceId, toVersion, stateMap, stepMap)` (audited, used by P9's feature migration).
  - `src/platform/audit/{interceptor.ts, outbox.ts, subscribers.ts, receipts.ts, history.ts}` — the Prisma extension recording `AuditEvent` with field-level before/after inside the caller's transaction (actor and correlationId from the request context; excluded models: AuditEvent, DomainEvent, EventHandlerReceipt, session, verification, SearchIndexEntry, DashboardProjection; anonymous writes record `actorUserId = null`); `record()` for explicit actions (login, download, export, denied, tenant_bypass — `withTenantBypass` now writes its audit row); `publish(tx, name, aggregateRef, payload)`; `subscribe(eventName, handlerKey, handler)` with the subscriber table (`derive` grant sync moved here: `member.changed`, `member.removed`, `group.membership.changed`, `group.status.changed`, `teaching.assigned/ended`, `representative.changed`, `student.section_membership.changed`, `person.user_linked`); `dispatchPending(limit)` (`FOR UPDATE SKIP LOCKED`, insert-first `EventHandlerReceipt`, `publishedAt` when all receipts exist, `attempts`/`lastError`, `deadAt` after 10) used by the worker in P5 and directly by integration tests; `history(subjectRef, actor)` merging AuditEvent and WorkflowTransitionLog. The better-auth organization hooks from P2 now publish instead of calling `derive` directly.
  - Test fixtures and the single-source guard: `tests/fixtures/workflows/{linear.json, compound_review.json}` (the compound fixture is request → compound review with two branches, quorum 1, `onReject` → revision) registered through `registry.upsertDefinition` by tests only; `prisma/seed/workflows/index.ts` exporting `seedWorkflows(db)` — called by `prisma/seed.ts` before `seedFeatures()` and deliberately empty in milestone 1, because every real lifecycle is compiled from a FeatureDefinition (`key = feature:<key>`); the only exception is P7's provisional `task` definition, which is listed in `PROVISIONAL_DEFINITION_KEYS` and retired by P9. Read-only UI: `/admin/workflows` (list) and `/admin/workflows/[key]` (state graph via `src/components/workflow/StateGraph.tsx` with a layered SVG layout, compound states drawn as lanes, version list with a JSON diff, and the note that editing happens in the feature builder), `src/components/feature/ActionBar.tsx` (buttons from `availableActions`, confirm dialog, comment sheet) and `src/components/AuditPanel.tsx` (from `history()`), `/admin/audit` viewer with actor/subjectType/action/date filters.
KEY FILES: prisma/schema/workflow.prisma; prisma/schema/audit.prisma; prisma/migrations/<ts>_append_only/migration.sql; src/platform/workflow/schema.ts; src/platform/workflow/machine.ts; src/platform/workflow/engine.ts; src/platform/workflow/parallel.ts; src/platform/workflow/guards.ts; src/platform/workflow/effects.ts; src/platform/workflow/registry.ts; src/platform/audit/interceptor.ts; src/platform/audit/outbox.ts; src/platform/audit/subscribers.ts; src/platform/audit/history.ts; prisma/seed/workflows/index.ts; tests/fixtures/workflows/compound_review.json; src/components/workflow/StateGraph.tsx; src/components/feature/ActionBar.tsx; src/components/AuditPanel.tsx; src/app/(admin)/admin/audit/page.tsx; src/app/(admin)/admin/workflows/[key]/page.tsx
TESTS:
  - tests/unit/workflow/machine.test.ts — transition resolution and unknown actions, guards by name, required comment/fields raising typed errors, effects returned in declared order, terminal detection, revision loops, and compound states: `all` waits for every branch, `quorum(1)` emits `$join` on the first done branch and marks siblings skipped, `any`, a branch reaching `rejectedState` emits `$reject` to `onReject`, a branch action on a non-active branch is refused, dynamic compound states instantiate one `$person` branch per resolved person
  - tests/unit/workflow/schema.test.ts — the contract rejects a `from` array, a `joinPolicy` key, a quorum `n` greater than the branch count, an undeclared branch state key and an unreachable terminal; both fixtures parse
  - tests/integration/workflow/apply.test.ts — `apply` writes WorkflowInstance + WorkflowTransitionLog + AuditEvent(transition) + DomainEvent(workflow.transitioned) in one transaction; a failing guard or a failing effect leaves zero rows; a stale `rowVersion` raises ConflictError; two concurrent branch completions serialise through `SELECT ... FOR UPDATE` and produce exactly one `$join`; an instance started on v1 stays on v1 after v2 is published; `setField` writes only allow-listed derived caches
  - tests/integration/workflow/definitions.test.ts — after `prisma db seed` every WorkflowDefinition row either has `featureVersionId` set (compiled from a feature) or its key is in `PROVISIONAL_DEFINITION_KEYS`; `seedWorkflows` is idempotent
  - tests/integration/audit/interceptor.test.ts — updating `Person.fullName` records before/after with actor and correlationId, a delete records the before image, an anonymous write records `actor_user_id IS NULL`, and `UPDATE audit_event` as dept_app is denied
  - tests/integration/events/outbox.test.ts — `publish` inside a rolled-back transaction leaves no event; `dispatchPending` runs two subscribers once each, a throwing subscriber increments `attempts` and is retried while the succeeded subscriber's receipt prevents re-execution, and an event reaches `deadAt` after 10 attempts
  - tests/integration/tenancy/append-only.test.ts — as dept_app: UPDATE/DELETE on audit_event, workflow_transition_log and event_handler_receipt raise; `UPDATE domain_event SET payload_json` raises while `SET published_at` succeeds; DELETE of an unpublished domain_event raises and of a published one succeeds
  - tests/e2e/p4-audit.spec.ts — the DH renames a person in /d/cs/people, the admin opens /admin/audit, filters by subject type person and sees the before/after values with the actor; /admin/workflows shows the empty state explaining that definitions are compiled from features
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p4_workflow_audit
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p4_workflow_audit/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p4-audit.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(workflow): workflow engine with compound states, audit interceptor and transactional outbox" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: /admin/audit lists the Person edit made on the people page with before/after values, the actor and the correlation id, and a `tenant_bypass` row for an admin faculty page; /admin/workflows shows the (still empty) definition list and renders the compound fixture graph when a test schema is loaded; `docker compose exec db psql -U dept_app -d dept -c 'DELETE FROM audit_event'` and `-c "UPDATE domain_event SET payload_json = '{}'"` are both denied while `-c "UPDATE domain_event SET attempts = attempts + 1"` succeeds.
COMMIT: feat(workflow): workflow engine with compound states, audit interceptor and transactional outbox

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P5 — Worker, scheduler ledger, anchored reminders, notifications, templates and email
GOAL: (3 days) The worker becomes the only executor of deferred work: queues created from the shared catalogue, the outbox dispatcher (policy `short`, self-chaining plus a one-minute cron re-seed), a `ScheduledJob` ledger written in the caller's transaction through pg-boss's per-call `db: fromPrisma(tx)`, calendar-anchored `ReminderSubscription`s materialised synchronously inside the 48 h horizon and hourly beyond it, overdue sweep, auto transitions, nightly grant reconcile and weekly retention. Notifications are the one per-user inbox with acknowledge/decline and per-channel deliveries; templates are versioned logic-less mustache; email goes react-email + nodemailer to Mailpit.
DEPENDS: ['P4']
DELIVERABLES:
  - `prisma/schema/scheduler.prisma` (`ReminderSchedule`, `ReminderSubscription` with `deadlineSpecJson {at}|{anchor}|{everyDays, whileInStates}`, `ScheduledJob { Id, TENANT?, kind JobKind, queue, REF(subject)?, runAt, payloadJson, idempotencyKey @unique, status JobStatus, attempts, lastError?, pgBossJobId?, singletonKey?, Ts }`), `prisma/schema/notification.prisma` (`Notification` with **required unique `dedupeKey`**, `NotificationDelivery`, `ChannelPreference` keyed by userId — `Announcement` is added by its own module phase), `prisma/schema/template.prisma` (`Template`, `TemplateVersion` with `channelVariantsJson` and `declaredVariablesJson`).
  - `src/platform/scheduler/queues.ts` extended to the full catalogue of the infra part with policies: `outbox.dispatch` (short), `reminder.materialize`, `reminder.fire`, `notification.deliver`, `email.send` (retryLimit 5, retryBackoff, deadLetter `email.dead`), `email.dead`, `workflow.auto_transition`, `overdue.sweep`, `grant.reconcile`, `calendar.autotransition`, `retention.run`, `worker.heartbeat`; later phases append their own names. `src/platform/scheduler/enqueue.ts` — `fromPrisma(tx)` and `enqueue(tx, name, data, {singletonKey, startAfter, retryLimit, idempotencyKey, kind, subjectType, subjectId, departmentId})` writing the `ScheduledJob` ledger row on the same transaction connection; `ledger.ts` (`cancel(prefix)` = ledger `cancelled` + `boss.cancel`, `markRunning/Sent/Done/Failed`, `dryRun(range)`).
  - `src/platform/scheduler/{reminders.ts, anchors.ts, upcoming.ts}` — `subscribeReminders(subjectRef, deadlineSpec, scheduleKey, audienceSpec, variables, whileInStates?)` resolving calendar anchors through `academic.resolveAnchor`, storing the subscription and **materialising synchronously in the caller's transaction** every offset inside the 48 h horizon (key `{subjectType}:{subjectId}:{scheduleKey}:{offsetDays}`, past offsets skipped, plus the escalation job), `rescheduleSubject`, `cancelBySubject`, `subscribeInterval`, `upcoming(personOrScope, range)`; the `calendar.period.changed` subscriber re-resolves every matching anchor (cancel by prefix + re-materialise) and returns the count that P3's `setPeriod` impact preview shows.
  - `src/platform/scheduler/{notify.ts, inbox.ts, channels/in-app.ts, channels/email.ts}` — `notify({recipients|audienceSpec|recipientRule, templateKey, variables, category, subjectRef, ackRequired, declinable, dedupeKey, channels?})` inserting `Notification` with `ON CONFLICT (dedupe_key) DO NOTHING`, one `NotificationDelivery` per enabled channel (in_app only when the Person has a userId, email when `Person.email` exists and ChannelPreference allows) and `enqueue(tx,'notification.deliver')`, all in the caller's transaction; `acknowledge`, `decline(reason)` (publishes `notification.declined`), `ackStatus(subjectRef)`, `inbox(personId, filters)`, `markRead`; mass-send guard above `SystemSetting notify.massSendThreshold`. `src/platform/template/{service.ts, render.ts, mustache-safe.ts, variables.ts}` — mustache 4.2 with per-channel escaping, partials and lambdas disabled, `validate(body, declaredVariables)` rejecting undeclared names at save, `variablesFor(subjectRef)` merging `SubjectRegistry.variables` with person/department/calendar variables, versions with `newVersion`/`activate`/`previewWithSample`.
  - Worker: `apps/worker/src/handlers/{outbox-dispatch,reminder-materialize,reminder-fire,notification-deliver,email-send,email-dead,workflow-auto-transition,overdue-sweep,grant-reconcile,calendar-autotransition,retention-run}.ts` (one default export per queue, file named after the queue with `.`→`-`), each opening `withTenantTx(job.data.departmentId, ...)` or, for faculty-wide crons, iterating `prismaRoot.department.findMany()`; `grant-reconcile.ts` and `retention-run.ts` use `withTenantBypass({worker:true, jobName}, ...)`. `overdue-sweep.ts`, `calendar-autotransition.ts` and `retention-run.ts` iterate **registered catch-up providers** (`registerOverdueProvider`, `registerCatchUpProvider`, `registerRetentionProvider`) so later phases add work by registration instead of new handlers. `apps/worker/src/schedules.ts` registers the crons with `tz: process.env.APP_TIMEZONE` exactly as the infra part (`outbox.dispatch * * * * *`, `reminder.materialize 0 * * * *`, `overdue.sweep 15 * * * *`, `calendar.autotransition */15 * * * *`, `grant.reconcile 0 3 * * *`, `retention.run 0 4 * * 0`, `worker.heartbeat * * * * *`) and removes stale schedules by comparing `boss.getSchedules()`.
  - Workflow effects made real (replacing P4's recorders): `notify`, `subscribeReminders`, `cancelReminders`/`cancelScheduled`, `scheduleAutoTransition` (`ScheduledJob` + queue `workflow.auto_transition` with `singletonKey wf:{instanceId}:{action}:{runAt}` and an `expectedState` no-op check). Email: `src/lib/mail/{transport.ts, render.ts}`, `src/emails/Notification.tsx`; P2's `mailer.sendSystem` now renders the seeded `auth.set_password` / `auth.reset_password` templates through the same path.
  - Seeds: `prisma/seed/reminder-schedules.ts` with the keys the seeded features reference — `default_7_3_1_0_overdue` (−7/−3/−1/0/+1, email+in_app, escalation after 3 overdue days to department_head), `short`, `portfolio_7_3_1_0_overdue`, `appointment_-1d_-2h`, `meeting_-1d_-1h`, `duty_-1d`; `prisma/seed/templates.ts` with the system message/reminder templates referenced by the seeded features (`task_assignment`, `task_update_request`, `case_assignment`, `deadline_reminder`, `deadline_overdue`, `committee_report_submitted`, `portfolio_submitted`, `portfolio_revision_requested`, `meeting_notification`, `minutes_circulated`, `minutes_approved`, `appointment_requested`, `appointment_confirmation`, `appointment_reschedule`, `invigilation_notification`, `lab_assignment`, `evaluation_invitation`, `campaign_invitation`, `campaign_reminder`, `preference_request`, `announcement`, `mention`, `report_ready`, `system_alert`, `auth.set_password`, `auth.reset_password`), each with declared variables validated at seed time. UI: `/d/[dept]/inbox` (unread / ack-required filters, read, acknowledge, decline with reason), `src/components/layout/InboxPopover.tsx`, `/d/[dept]/upcoming`, `/d/[dept]/settings/notifications`, `/admin/templates` and `/admin/templates/[key]` (variable chips, live preview, versions, activate), `/admin/reminders` (schedule editor + dry run for N days), `/admin/jobs` (ScheduledJob ledger with status filters, retry/cancel, outbox backlog with replay, dead events, worker heartbeat).
KEY FILES: prisma/schema/scheduler.prisma; prisma/schema/notification.prisma; prisma/schema/template.prisma; src/platform/scheduler/queues.ts; src/platform/scheduler/enqueue.ts; src/platform/scheduler/reminders.ts; src/platform/scheduler/ledger.ts; src/platform/scheduler/notify.ts; src/platform/scheduler/inbox.ts; src/platform/template/mustache-safe.ts; src/platform/template/variables.ts; apps/worker/src/schedules.ts; apps/worker/src/handlers/outbox-dispatch.ts; apps/worker/src/handlers/reminder-materialize.ts; apps/worker/src/handlers/reminder-fire.ts; apps/worker/src/handlers/email-send.ts; apps/worker/src/handlers/grant-reconcile.ts; prisma/seed/templates.ts; prisma/seed/reminder-schedules.ts; src/app/(app)/d/[dept]/inbox/page.tsx; src/app/(admin)/admin/templates/[key]/page.tsx; src/app/(admin)/admin/jobs/page.tsx
TESTS:
  - tests/unit/scheduler/offsets.test.ts — offsets −7/−3/−1/0/+1 produce the documented idempotency keys against a fixed `now`, past offsets are skipped, only offsets inside the 48 h horizon are materialised synchronously, anchor re-resolution cancels and creates only the changed keys, interval next-run arithmetic is timezone-safe
  - tests/unit/template/mustache-safe.test.ts — an undeclared variable is rejected at save, HTML is escaped for email and in_app but not for sms/document, sections and loops render, `{{> partial}}` and lambdas are rejected, a missing required variable fails at render
  - tests/unit/template/variables.test.ts — precedence subject > person > department > calendar on key collisions
  - tests/unit/scheduler/notify.test.ts — a `dedupeKey` collision returns the existing notification, a recipient without a User gets email-only deliveries, ChannelPreference disables a channel, the mass-send threshold requires explicit confirmation
  - tests/integration/scheduler/subscriptions.test.ts — `subscribeReminders` writes ScheduledJob rows and pg-boss jobs in the same transaction (a rollback leaves neither), moving the anchored calendar period cancels by prefix and re-materialises, `cancel(prefix)` cancels both, `upcoming()` lists the deadline for each audience member, `dryRun` matches what later fires
  - tests/integration/scheduler/ack.test.ts — acknowledge sets `acknowledgedAt` once, decline records the reason and publishes `notification.declined`, `ackStatus` aggregates per person, and notifications are RLS-isolated between departments
  - tests/integration/template/versions.test.ts — `newVersion` + `activate`, render uses the active version, a system template keeps its key and prior versions stay readable
  - tests/worker/enqueue-in-transaction.test.ts — a rolled-back transaction leaves no `pgboss.job` row and no ledger row; a committed one produces exactly one job with the expected `singletonKey` (the decision gate for `fromPrisma`)
  - tests/worker/outbox-dispatch.test.ts — two concurrent sends yield one active job under policy `short`, events published during a run are drained in the same run, receipts prevent duplicate side effects, a dead event is replayable
  - tests/worker/reminders.test.ts — `reminder.materialize` then `reminder.fire` notifies once even when materialised twice, a cancelled subscription or a state outside `whileInStates` is skipped and ends `cancelled`, a fired reminder writes a Notification whose dedupeKey equals the idempotency key and an email delivery reaches Mailpit
  - tests/worker/email.test.ts — `email.send` delivers to Mailpit (subject and body visible through the API), a transport failure retries with backoff, records `lastError` and finally lands in `email.dead` with an admin alert
  - tests/e2e/p5-inbox.spec.ts — a seeded ack-required notification appears in the DH inbox and the topbar badge, the DH acknowledges one and declines another with a reason, the admin edits `deadline_reminder`, previews, activates, runs the /admin/reminders dry run for 14 days, and the reminder mail in Mailpit contains the new wording
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p5_scheduler_notification_template
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p5_scheduler_notification_template/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p5-inbox.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(scheduler): pg-boss worker, ledgered reminders, inbox with ack and decline, mustache templates and email" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Confirm APP_TIMEZONE with the user before running (default Africa/Addis_Ababa in .env.example) — it is the single timezone for crons, reminders and rendering. Then `docker compose up -d db mailpit web worker`: /admin/jobs shows the heartbeat and the materialised `reminder.fire` rows with their runAt and keys; /admin/reminders dry run for 14 days lists the seeded subscription; edit and activate `deadline_reminder`, use 'fire now' on the ledger row and read the mail in Mailpit; the DH inbox shows the item with acknowledge and decline; `docker compose logs worker` shows one `outbox.dispatch` chain and no duplicate dispatchers after `docker compose restart worker`.
COMMIT: feat(scheduler): pg-boss worker, ledgered reminders, inbox with ack and decline, mustache templates and email

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P6 — Documents on a local-disk StorageProvider, polymorphic links, threads and comments
GOAL: (2 days) One file API: a multipart upload route with a size cap and magic-byte checks, immutable `DocumentVersion`s with sha256 checksums, polymorphic `DocumentLink`s with link roles and slot keys, access inherited through the SubjectRegistry, signed audited downloads, and version locking through a SECURITY DEFINER function that the approving transitions call; plus one `Thread`/`Comment` service for comments, revision notes and conversations with @mentions and an escalation seam.
DEPENDS: ['P5']
DELIVERABLES:
  - `prisma/schema/document.prisma` as the schema part §14 (`Document`, `DocumentVersion { …, storageKey @unique, checksum, extractedText?, lockedAt?, lockedByTransitionLogId?, @@unique([documentId, versionNo]) }`, `DocumentLink { …, REF(subject), linkRole LinkRole, slotKey @default(""), @@unique([documentId, subjectType, subjectId, linkRole, slotKey]) }`, `DocumentAccessGrant`) and `prisma/schema/thread.prisma` (`Thread { REF(subject)?, kind ThreadKind, participantGroupId?, sectionId?, status, escalatedToTaskId?, @@unique([subjectType, subjectId, kind]) }`, `Comment { threadId, authorPersonId, body, visibility Visibility, mentions String[] }`).
  - `prisma/migrations/<ts>_document_locks/migration.sql` (hand-written, snake_case, unqualified): `REVOKE UPDATE, DELETE ON document_version FROM dept_app; GRANT UPDATE (locked_at, locked_by_transition_log_id, extracted_text) ON document_version TO dept_app;` plus the `document_version_immutable` BEFORE UPDATE trigger (raises once `locked_at IS NOT NULL`) and the SECURITY DEFINER functions `document_version_lock(document_id, version_no, transition_log_id)` and `document_version_purge(document_id)` owned by dept_migrator with EXECUTE granted to dept_app.
  - `src/lib/storage/{provider.ts, local-disk.ts, s3-stub.ts}` — `StorageProvider { put(stream, {mimeType}) -> {key, sizeBytes, checksum}, get(key), delete(key), exists(key) }`; the local-disk provider writes under `UPLOAD_DIR` with `yyyy/mm/<random>` keys (never a path segment from user input), fsyncs and returns the sha256; the S3 stub throws NotImplemented but is shaped for `@aws-sdk/client-s3` with `forcePathStyle`. The `uploads` volume is already mounted on web, worker and (as /tmp/uploads) test.
  - `src/platform/document/{index.ts, service.ts, links.ts, access.ts, lock.ts, extract.ts, subject.ts}` — `upload(file, meta, links[])`, `addVersion`, `link`/`unlink`, `listFor(subjectRef, linkRole?, slotKey?, actor)`, `downloadUrl(documentId, versionNo, actor)` (a 5-minute HMAC-signed URL after `can(actor,'document.read', linkedSubject)` through each linked subject's `accessResolver` when `accessMode=inherit`, else `DocumentAccessGrant`), `storeGenerated(bytes, meta, links[])` (used by reporting in P11 and by document-rendering effects later), `lockVersion(documentId, versionNo, transitionLogId)` calling `document_version_lock()`, `softDelete`/`restore`, `extractText` (text/csv now), and `slotStatus(subjectRef, expectedSlots[]) -> [{slotKey, satisfied, documentId?}]` which the deliverable guard reads; the `document` SubjectRegistry registration, an `onDeleted` cascade that unlinks documents and closes threads, and a registered retention provider for `retention.run`.
  - Route handlers: `src/app/api/uploads/route.ts` (multipart `request.formData()`, cap from `SystemSetting upload.maxBytes` / `UPLOAD_MAX_MB`, `file-type` magic-byte check for xlsx/docx/pdf/png/jpg, CSV validated by parsing the first KB with papaparse, the `links[]` JSON field validated through `SubjectRegistry.exists`, rate-limited through `src/lib/rate-limit.ts` when unauthenticated) and `src/app/api/documents/[id]/v/[versionNo]/download/route.ts` (signature verification, streaming with `Content-Disposition: attachment` and `X-Content-Type-Options: nosniff`, `AuditEvent(download)`).
  - `src/platform/thread/{index.ts, service.ts, mentions.ts, escalate.ts, subject.ts}` — `getOrCreateThread(subjectRef, kind)`, `openConversation(participants|groupId, title, sectionId?, subjectRef?)`, `post(threadId, author, body, visibility, mentions[], attachments[])` publishing `comment.posted` (the mention subscriber notifies with template `mention`, dedupeKey `mention:{commentId}:{personId}`), `list(threadId, actor)` with visibility filtering, `close`, and the escalation seam `registerEscalationTarget(fn)` with `escalate(threadId, spec)` / `escalateAnswer(submissionId, questionKey, groupIndex, spec)` — unimplemented here and in P7 (typed `EscalationTargetNotRegistered`), implemented in P9 by `feature.createRecord('case', {parentRef: thread})`.
  - UI components reused by every later record page: `src/components/documents/{AttachmentSlots,DocumentList,UploadButton,VersionHistoryDialog}.tsx` (client upload to `/api/uploads` with progress, slot cards showing satisfied/missing) and `src/components/{ThreadPanel,CommentComposer}.tsx` (mention picker over the people directory); page `/d/[dept]/documents` (browse by subject type, my uploads) and a Documents + Comments tab on the person page as their first host.
KEY FILES: prisma/schema/document.prisma; prisma/schema/thread.prisma; prisma/migrations/<ts>_document_locks/migration.sql; src/lib/storage/provider.ts; src/lib/storage/local-disk.ts; src/platform/document/service.ts; src/platform/document/access.ts; src/platform/document/links.ts; src/platform/document/lock.ts; src/app/api/uploads/route.ts; src/app/api/documents/[id]/v/[versionNo]/download/route.ts; src/platform/thread/service.ts; src/platform/thread/escalate.ts; src/components/documents/AttachmentSlots.tsx; src/components/ThreadPanel.tsx; src/app/(app)/d/[dept]/documents/page.tsx
TESTS:
  - tests/unit/storage/local-disk.test.ts — random keys with no traversal from a hostile filename, the checksum equals sha256 of the bytes, `get` streams identical content, `delete` removes the object
  - tests/unit/document/access.test.ts — inherit allows when any linked subject grants `document.read` through a stubbed `can()`, explicit grants override, and signed-URL verification rejects a tampered or expired token
  - tests/integration/document/lifecycle.test.ts — upload creates Document + version 1 + link; `addVersion` increments `currentVersionNo`; `lockVersion` succeeds through the SECURITY DEFINER function and a second lock attempt fails; a direct `UPDATE document_version` as dept_app is denied; `slotStatus` reports satisfied and missing slots; a cross-department download is denied; the download is audited
  - tests/integration/uploads/route.test.ts — the route handler rejects a body above the cap, rejects an .xlsx whose bytes are HTML, accepts a real xlsx fixture and a valid CSV, and rejects links to a non-existent subject
  - tests/integration/thread/comments.test.ts — `getOrCreateThread` is idempotent per (subject, kind), a post with a mention creates the Notification after `dispatchPending`, `visibility=reviewers_only` is hidden from non-reviewers, a closed thread refuses posts, and `escalate` fails with EscalationTargetNotRegistered until P9
  - tests/e2e/p6-documents.spec.ts — an instructor uploads a PDF as evidence on their person page, downloads it, uploads a second version and sees it in the version history; posts a comment mentioning the DH; the DH sees the mention in the inbox; a stale download link is rejected
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p6_document_thread
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p6_document_thread/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p6-documents.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(documents): versioned documents on a storage provider, polymorphic links, threads and comments" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: On a person page upload a .docx, download it (the browser saves the file), upload a second version and open the version history; wait out the 5-minute signature and confirm the link is rejected; post a comment mentioning the DH and see the DH inbox item; /d/cs/documents lists the document under subject type person; `docker compose exec web ls -R /data/uploads` shows the yyyy/mm key layout and `docker compose exec db psql -U dept_app -d dept -c "update document_version set mime_type='x'"` is denied.
COMMIT: feat(documents): versioned documents on a storage provider, polymorphic links, threads and comments

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P7 — Work item service walking skeleton: assign, remind, acknowledge, comment, deliverable, complete
GOAL: (2 days) One `Task` table for every task-like thing with person/group/audience assignments, recurrence, the `Case` and `PlannedActivity` extensions, deliverable slots satisfied only by `DocumentLink(linkRole=deliverable, slotKey)` and acknowledgement only through the assignment `Notification` — the first end-to-end journey. The lifecycle runs on `src/platform/workflow/definitions/task.ts`, the single hand-written WorkflowDefinition in the plan (subject `task`, key `task`, listed in `PROVISIONAL_DEFINITION_KEYS`), whose states and transitions are written to match the seeded `task` feature of the builder part so P9 replaces it by compiling and backfilling instead of rewriting.
DEPENDS: ['P6']
DELIVERABLES:
  - `prisma/schema/workitem.prisma` as the schema part §8: `Task { Id, TENANT, title, description?, kind TaskKind, priority, REF(context)?, parentTaskId? (TaskTree), featureRecordId String? @unique, featureStepInstanceId String? @unique, createdBy, startDate?, dueAt?, deadlineAnchorJson?, expectedDeliverablesJson @default("[]"), progressPercent, recurrenceRuleId?, reminderScheduleKey?, completedAt? (derived cache), Ts, RV }` — no status column and no `workflowInstanceId`; `TaskAssignment { taskId, assigneeType, assigneeId, role AssignmentRole, sourceAudienceSpecJson?, @@unique([taskId, assigneeType, assigneeId, role]) }`, `RecurrenceRule`, `Case { taskId @id, requesterPersonId?, REF(origin)?, sectionId?, issue, issueCategory?, requiredAction?, waitingOn?, resolution?, resolvedAt? }`, `PlannedActivity { taskId @id, … }` (the extension tables are created now and used by their module phases). `prisma/migrations/<ts>_constraints_p7/migration.sql`: `CREATE INDEX task_open_due_idx ON task (department_id, due_at) WHERE completed_at IS NULL;` — `task_backing_check` is added in P9, once every Task has a feature backing.
  - `src/platform/workitem/{index.ts, service.ts, assignments.ts, recurrence.ts, extensions.ts, queries.ts, workflow-hooks.ts, subject.ts}` — `createTask(spec)` (audience assignees snapshotted through `snapshotAudienceAsGroup` into a `Group(kind=adhoc, context=task)`, the lifecycle started on the provisional definition, assignment notifications with `ackRequired` and `dedupeKey`, reminders through `subscribeReminders`), `transition(taskId, action, actor, payload)` delegating to the engine, `assign`/`reassign`, `setDeadline` (cancel by prefix + re-subscribe), `ackStatus`, `deliverableStatus` (from `document.slotStatus`), `requestUpdate`, `completeTask` (only through the workflow `setField completedAt`), `listTasks({assigneePersonId incl. via group, kind, contextRef, states, overdue, dueBetween})`, `myWork(personId)`; the guard `task.requiredDeliverablesLinked`, the effects `createTask` (now real) and `task.setCompletedAt`, the `notification.acknowledged` subscriber that starts an assigned task, the `notification.declined` subscriber that posts a thread comment and notifies the creator, and an overdue provider registered with `overdue.sweep`. Overdue is always computed (`dueAt < now` and non-terminal), never stored.
  - `src/platform/workflow/definitions/task.ts` — the provisional definition: states draft → assigned → in_progress → submitted → under_review → (revision_required → submitted) with terminals completed and cancelled, actor rules, `requiredAttachments` from `expectedDeliverablesJson`, guards and effects exactly as `seededFeatures.task` describes them, registered by `prisma/seed/workflows/index.ts` with `isSystem: true`; a header comment and a README note state that P9 compiles `feature:task` from `prisma/seed/features/task.ts`, proves equality against this file, backfills FeatureRecords and retires this key.
  - Worker: `apps/worker/src/handlers/recurrence-spawn.ts` and the queue `recurrence.spawn` appended to `src/platform/scheduler/queues.ts` with the cron `*/15 * * * *` (creates the next occurrence once per `nextSpawnAt`, ledger key `recurrence:{ruleId}:{isoNextSpawnAt}`).
  - The record/step UI shell that P9's generic runtime composes (written once, per the runtime part's component list): `src/components/feature/{RecordHeader,StateBadge,StepTimeline,ParallelLanes,AttachmentSlots wiring,FieldRenderer}.tsx` plus `src/components/feature/RecordDetail.tsx` (header, ActionBar from P4, tabs Details / Deliverables / Comments / History / Acknowledgements, `extras` render slots) and `src/components/data-table/DataTable.tsx` (columns from a list spec, URL-state filters). `src/app/(app)/d/[dept]/my-work/page.tsx` (one table over `task` + `task_assignment` for the actor, grouped with state and deadline) and the interim pages `/d/[dept]/tasks/{page.tsx,new/page.tsx,[taskId]/page.tsx}` with `src/modules/tasks/{actions.ts,queries.ts}` — P9 deletes these three pages and replaces them with the `next.config.ts` rewrite `/d/:dept/tasks/:path*` → `/d/:dept/f/task/:path*`, so there is exactly one implementation of every page from P9 on.
  - Seeds: `prisma/seed/demo/tasks.ts` (SEED_DEMO=1) — one task for Instructor One due in two days with a required `report` deliverable slot (producing the ack-required notification and the reminder ledger rows) and one department-wide task addressed to the audience `roles:[instructor]`.
KEY FILES: prisma/schema/workitem.prisma; prisma/migrations/<ts>_constraints_p7/migration.sql; src/platform/workitem/service.ts; src/platform/workitem/assignments.ts; src/platform/workitem/recurrence.ts; src/platform/workitem/queries.ts; src/platform/workitem/workflow-hooks.ts; src/platform/workitem/subject.ts; src/platform/workflow/definitions/task.ts; apps/worker/src/handlers/recurrence-spawn.ts; src/components/feature/RecordDetail.tsx; src/components/feature/StepTimeline.tsx; src/components/data-table/DataTable.tsx; src/app/(app)/d/[dept]/my-work/page.tsx; src/app/(app)/d/[dept]/tasks/[taskId]/page.tsx; src/modules/tasks/actions.ts; prisma/seed/demo/tasks.ts
TESTS:
  - tests/unit/workitem/recurrence.test.ts — next occurrences for daily, weekly (byWeekday), monthly, termly and yearly rules with `endsOn` and `count`, day boundaries computed in APP_TIMEZONE
  - tests/unit/workitem/guards.test.ts — `task.requiredDeliverablesLinked` passes only when every required slot is satisfied in `slotStatus` and ignores optional slots
  - tests/unit/workflow/definitions-task.test.ts — the provisional task definition parses against the State/Transition contract, every state is reachable from draft and both terminals are reachable (the snapshot this file produces is what P9's equality test compares the compiled feature against)
  - tests/integration/workitem/task-lifecycle.test.ts — `createTask` with the audience `roles:[instructor]` snapshots an adhoc Group, creates one ack-required assignment Notification per member plus the reminder ledger rows; acknowledging auto-applies `start`; `submit` is blocked until the deliverable DocumentLink for the required slot exists; submit → review → approve writes `completedAt` only through `setField`; the audit and transition log rows exist; `listTasks(assigneePersonId)` finds the task through group membership
  - tests/integration/workitem/decline-and-update.test.ts — declining the assignment notification posts a thread comment and notifies the creator; `requestUpdate` notifies the responsible assignee with a dedupeKey; `setDeadline` cancels the old reminder keys and creates the new ones
  - tests/worker/recurrence-spawn.test.ts — the job creates the next task exactly once for a given `nextSpawnAt` even when it runs twice, and advances `nextSpawnAt`
  - tests/components/record-detail.test.tsx — RecordDetail renders header, state badge, the action bar from `availableActions`, the attachment slots with satisfied/missing state and the acknowledgements tab from `ackStatus`
  - tests/e2e/p7-task-skeleton.spec.ts — the DH creates a task for Instructor One with a required deliverable due in two days; the instructor sees the inbox item and the mail in Mailpit, acknowledges (the task moves to In Progress), uploads the deliverable into the slot and submits; the DH reviews and approves; the history tab shows every transition and the acknowledgements tab who acknowledged when; the department-wide task appears in every seeded instructor's /d/cs/my-work
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p7_workitem
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p7_workitem/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p7-task-skeleton.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(workitem): task service with assignments, deliverable slots and acknowledgement via notifications" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: As dh.cs create a task with a two-day deadline and a required deliverable for an instructor; as that instructor acknowledge from the inbox (the task moves to In Progress), upload the deliverable and submit; as dh.cs approve; then move the deadline to today and confirm the reminder mail arrives in Mailpit after the worker fires it and that /admin/jobs shows the old keys cancelled and new ones scheduled; /d/cs/my-work lists the department-wide task for each instructor.
COMMIT: feat(workitem): task service with assignments, deliverable slots and acknowledgement via notifications

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P8 — Form engine, standalone submissions and campaign core with tokens, anonymity and aggregation
GOAL: (3 days) Versioned `FormDefinition`/`Question` rows with stable keys and every question type, Zod generated from the questions, the `FormRenderer`, drafts and standalone submissions bound to a subject or step; and the campaign core as a set of services the campaign feature's adapters will call in P14: Campaign/CampaignSubject/CampaignInvitation/AggregationResult, hashed one-time tokens behind the public `/c/[token]` route, the structural anonymity constraints, participation and generic aggregation with k-anonymity, csv/xlsx export, and the `campaign.open`/`close`/`aggregate` worker queues. No campaign lifecycle is invented here: the state machine arrives with the seeded `campaign` feature.
DEPENDS: ['P7']
DELIVERABLES:
  - `prisma/schema/forms.prisma` as the schema part §9: `FormDefinition { Id, TENANT?, key, version, kind FormKind, title, description?, scoringJson?, questionsHash, lockedPathsJson, isSystem, status FormStatus, createdBy?, Ts, @@unique([departmentId, key, version]) }`, `Question { Id, formDefinitionId, stableKey, order, type QuestionType, label, helpText?, optionsJson?, sourceBinding SourceBinding, bindingArgsJson?, constraintsJson?, scoreWeight?, aggregation, parentStableKey?, computedBy?, isLocked, @@unique([formDefinitionId, stableKey]) }` (GLOBAL child), `Submission { …, REF(subject)? (feature_step_instance for step forms), stepKey?, respondentPersonId?, invitationId String? /* no relation by design */, respondentGroup?, cohortAttributesJson?, pseudonymHash?, status, submittedAt? }`, `Answer { …, questionStableKey, groupIndex, valueJson, numericValue?, rank?, refType?, refId?, textValue?, @@unique([submissionId, questionStableKey, groupIndex]) }`.
  - `prisma/schema/campaign.prisma` as the schema part §10: `Campaign` (kind, formDefinitionId + formVersion, termId, windowAnchorJson, resolvedOpensAt/ClosesAt, anonymityMode, submissionRule, audienceSpecJson + audienceResolutionMode, subjectMode, invitationTemplateKey, reminderScheduleKey, aggregationSpecJson?, exportSpecId?, minResponsesForReport, optionsJson, `featureRecordId String? @unique` in this phase — tightened to NOT NULL by the phase that seeds the `campaign` feature and backfills), `CampaignSubject`, `CampaignInvitation` (`tokenHash @unique`, status, expiresAt, day-truncated `submittedAt` when anonymous), `AggregationResult`. `prisma/migrations/<ts>_constraints_p8/migration.sql`: the `enforce_submission_anonymity()` function and `submission_anonymity` BEFORE INSERT OR UPDATE trigger of the schema part §22.5 (an anonymous campaign's submission may carry neither `respondent_person_id` nor `invitation_id`), plus a CHECK that `submitted_at` is date-truncated for those rows.
  - `src/platform/forms/{index.ts, definitions.ts, zod-from-fields.ts, submissions.ts, bindings.ts, subject.ts}` — question types short_text, long_text, number, date, datetime, boolean, single_choice, multi_choice, likert, scale, the pickers (person, group, course, offering, section, term, resource, task, record, audience), ranked_list, file, repeating_group, section_header, computed, with constraints (required, min/max, regex, maxRank, maxItems, accept, visibleIf); `zodFromFields(fields|questions)` (ranked uniqueness, repeating-group arrays, conditional requirement) shared with the feature record forms in P9; `defineForm`/`newVersion`/`publishForm` (a new version only when `questionsHash` changes; locked question keys of a system form cannot be removed or retyped); `saveDraft`/`submitStandalone(formKey, answers, {subjectRef, stepKey, campaign?, token?})` normalising into `Answer` rows with the typed value columns; `answersAsVariables(submissionId)` for templates; `bindings.ts` resolvers for every `SourceBinding` value of the builder part §1, each scoped to the department; the `form_definition` and `submission` SubjectRegistry registrations.
  - `src/platform/campaign/{index.ts, service.ts, invitations.ts, tokens.ts, participation.ts, aggregation.ts, anonymity.ts, export.ts, subject.ts, kinds/survey.ts}` — the functions the P14 campaign adapters wrap as workflow effects: `createCampaign(spec)`, `publish(campaignId)` (resolve the audience per subject → `CampaignInvitation` rows with `tokenHash = sha256(token)`, `campaign.open`/`campaign.close` ledger jobs at the resolved window, reminders on `resolvedClosesAt` for `invitations.pending`), `open`, `close` (expire tokens, cancel reminders, enqueue aggregate), `reschedule` (also driven by the `calendar.period.changed` subscriber), `openLink(token)`, `submitViaToken` (identified: respondent + cohort attributes; anonymous: NULL respondent/invitation written in one statement with the invitation marked `submitted` in a second; pseudonymous: salted hash), `withdraw`/`edit` per `submissionRule`, `participation(campaignId)`, `aggregate(campaignId)` (mean/sd/distribution for likert/scale/number, counts for choices, Borda and first-choice for ranked_list, per subject and groupBy, suppression below `SystemSetting campaign.kThreshold`), `exportResults(format csv|xlsx)` via papaparse/exceljs-hardened stored through `document.storeGenerated`. Queues `campaign.open`, `campaign.close`, `campaign.aggregate` appended to the catalogue with handlers `apps/worker/src/handlers/{campaign-open,campaign-close,campaign-aggregate}.ts` (each re-checks state so a duplicate send is a no-op; from P14 they call `Workflow.apply` on the campaign's FeatureRecord instance).
  - Public route and shared UI: `src/lib/rate-limit.ts` (in-memory token bucket per IP + route, used by `/c/[token]`, the upload route and the auth routes), `src/app/c/[token]/page.tsx` (no session: `openLink`, renders the pinned form, CSRF token, rate limited) with its server action, `src/components/forms/FormRenderer.tsx` plus one component per question type in `src/components/forms/questions/*`, `src/components/forms/DraftAutosave.tsx`, `src/components/{RankedList,RepeatingGroup}.tsx`; `/admin/forms` and `/admin/forms/[key]` (question list editor with drag order, type settings, source-binding picker, locked badges, versions, publish, preview and sample submit); `/d/[dept]/campaigns/[campaignId]/results/page.tsx` — the read-only participation and results page that the runtime part lists as a module page (the campaign list, create and lifecycle actions come from the generic runtime in P14).
  - Seeds: `prisma/seed/forms/index.ts` (`seedForms(db)`, idempotent by key + version, called by `prisma/seed.ts` before `seedFeatures()`; module forms are added by their own phases) and, under SEED_DEMO=1, `prisma/seed/demo/forms.ts` (`demo_survey` with a likert, a choice, a ranked_list and a long_text question) plus `prisma/seed/demo/campaign.ts` (one open survey campaign over the audience `roles:[instructor]`, anonymous, window today..+3d, with invitations).
KEY FILES: prisma/schema/forms.prisma; prisma/schema/campaign.prisma; prisma/migrations/<ts>_constraints_p8/migration.sql; src/platform/forms/zod-from-fields.ts; src/platform/forms/submissions.ts; src/platform/forms/bindings.ts; src/platform/campaign/service.ts; src/platform/campaign/tokens.ts; src/platform/campaign/aggregation.ts; src/platform/campaign/anonymity.ts; apps/worker/src/handlers/campaign-open.ts; apps/worker/src/handlers/campaign-close.ts; apps/worker/src/handlers/campaign-aggregate.ts; src/lib/rate-limit.ts; src/app/c/[token]/page.tsx; src/components/forms/FormRenderer.tsx; src/app/(admin)/admin/forms/[key]/page.tsx; src/app/(app)/d/[dept]/campaigns/[campaignId]/results/page.tsx; prisma/seed/forms/index.ts; prisma/seed/demo/campaign.ts
TESTS:
  - tests/unit/forms/zod-from-fields.test.ts — every question type builds the expected schema: required, min/max, regex, ranked uniqueness and maxRank, repeating-group min/max, visibleIf-conditional requirement, file accept list
  - tests/unit/campaign/aggregation.test.ts — mean/sd/distribution for likert, counts for choices, Borda and first-choice for ranked_list, groupBy cells, suppression below k with free text withheld
  - tests/unit/campaign/tokens.test.ts — token entropy, sha256 hashing, single-use versus editable-until-close, expiry and status transitions
  - tests/components/form-renderer.test.tsx — each question type renders, validation errors from the generated schema are shown, and submit produces normalised answers including repeating groups and ranked lists
  - tests/integration/forms/submissions.test.ts — saveDraft then submit produce Answer rows with groupIndex and the typed value columns; the form version is pinned when v2 is published; bindings return only rows of the actor's department; removing a locked question key from a system form is rejected
  - tests/integration/campaign/lifecycle.test.ts — publish creates one invitation per (person, subject) and the open/close ledger jobs; moving the anchored period re-resolves the window and reschedules; open notifies with token links and mails persons without a User; an identified submit copies the cohort attributes; `editable_until_close` allows an edit before and refuses after close; close expires tokens and enqueues aggregate; aggregate writes AggregationResult
  - tests/integration/campaign/anonymity.test.ts — an anonymous submission has NULL respondent and invitation, the trigger rejects a row carrying either, `submitted_at` is day-truncated, the adversarial join across submission, campaign_invitation and audit_event by timestamp returns no respondent, and audit rows for anonymous submissions have `actor_user_id IS NULL`
  - tests/worker/campaign-jobs.test.ts — `campaign.open` sends invitations once under duplicate sends with the same singletonKey, `campaign.close` expires tokens and enqueues aggregation, and a job whose expected state no longer holds is a no-op
  - tests/e2e/public-links.spec.ts — Instructor One opens the invitation link from Mailpit in a fresh context without logging in and submits the anonymous survey; a second use of the single-use token is refused; the DH opens /d/cs/campaigns/<id>/results, sees participation 1/3 and downloads the csv export; results cells below k are shown as suppressed
  - tests/e2e/p8-forms.spec.ts — the admin adds a likert question to `demo_survey`, publishes v2, previews and submits sample answers, and the already-open campaign keeps rendering the pinned v1 for its invitations
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p8_forms_campaign
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p8_forms_campaign/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/public-links.spec.ts tests/e2e/p8-forms.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(forms): form engine, standalone submissions and campaign core with tokens, anonymity and aggregation" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: In /admin/forms open `demo_survey`, add a question, publish and submit the preview; open the invitation mail in Mailpit and complete the form at /c/<token> in a private window; /d/cs/campaigns/<id>/results shows participation, suppressed cells under k and working csv/xlsx downloads; move the anchored period in /admin/calendar and confirm the impact preview lists the campaign and that /admin/jobs shows the open/close jobs rescheduled; `docker compose exec db psql -U dept_app -d dept -c "insert into submission (id, department_id, form_definition_id, form_version, campaign_id, respondent_person_id) values (...)"` for an anonymous campaign is rejected by the trigger.
COMMIT: feat(forms): form engine, standalone submissions and campaign core with tokens, anonymity and aggregation

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P9 — Feature definition kernel, compiler, generic runtime, admin builder and the seeded task, case and generic_request features
GOAL: (3 days) A System Administrator composes a process feature in a seven-screen wizard and publishes it: the Zod `FeatureDefinition` aggregate, the fixed validation rule list, the pure compiler that emits one WorkflowDefinition (with compound states for parallel groups), per-step FormDefinitions and TaskTemplates, reminder templates, permission rows and grant requirements, the simulator, the version/migration wizard, and the generic runtime under `/d/[dept]/f/[featureKey]`. The built-ins `task`, `case` and the sample `generic_request` are seeded, the provisional task definition is retired by a backfill, and `/d/[dept]/tasks/*` becomes a rewrite so every page has exactly one implementation.
DEPENDS: ['P8']
DELIVERABLES:
  - `prisma/schema/feature.prisma` exactly as the builder part §2 / schema part §7: `FeatureDefinition` (key + nullable departmentId, navGroup/navOrder, scopeLevel, isSystem, `activeVersionId @unique`), `FeatureDefinitionVersion` (json, compiledJson, jsonHash, lockedHash, changeNote, workflowDefinitionId), `FeatureRecord` (number `F-<prefix>-<year>-<seq>`, data jsonb with a GIN JsonbPathOps index, presetKey, parent subject, scope, ownerPersonId, `workflowInstanceId @unique`, the derived caches currentStateKey / branchStatesCache / deadlineAt / firstEditedAt / closedAt, `taskId @unique`), `FeatureStepInstance` (stepKey, groupKey, branchKey, sequence, status FeatureStepStatus, assignee, `taskId @unique`, `submissionId @unique`, deadlineAt, outcomeActionKey), `FeatureNumberSequence`, `FeatureMigration` (stateMap, stepMap, plan, recordsTotal/Migrated/Blocked, blockedIds), `AdapterRegistration` (key @id, module, hook AdapterHook, simulable). There is no `FeatureVersion`, `FeatureStepRun` or `FeatureCounterCache`. `Task` gains nothing new (the columns exist since P7). `prisma/migrations/<ts>_constraints_p9/migration.sql`: `CREATE UNIQUE INDEX feature_definition_key_global ON feature_definition (key) WHERE department_id IS NULL;` and, after the backfill, `ALTER TABLE task ADD CONSTRAINT task_backing_check CHECK ((kind = 'feature_step') = (feature_step_instance_id IS NOT NULL) AND (kind <> 'feature_step') = (feature_record_id IS NOT NULL));`.
  - `src/platform/feature/schema.ts` — the canonical Zod aggregate of the builder part §1 verbatim: `schemaVersion`, key, name, labels, `navigation {group NavGroup, order, icon, visibleRoles (NavRoleKey only), showInDashboard, showCounterInNav}`, `scope {level ScopeLevel, scopeFrom, fieldKey?}`, `parentSubject {subjectType, featureKey?, relation, required, inheritPermissions, listUnderParent, createFromParent, allowedTypes?}`, `record {numberPrefix, titleTemplate, fields FieldDef[], canCreate ActorRule[], ownerRule AssigneeRule, backing feature_record|task{taskKind,extension?}|module{adapter}}`, `presets` (fieldDefaults, formKeys, adapters, permissionPrefix, parentSubjectType, visibleRoles, navLabel), `steps StepNode[]` (`StepDef` with stepType/form/attachments/assignee/actions/deadline/reminders/notifications/canView/canEdit/adapter/surface/workItem, `StepGroup`, `ParallelGroup` with `branches.mode static|dynamic` and `completion all|quorum(n)|any`, `onComplete`, `onAnyReject`), `terminalStates`, `listViews`, `dashboardCounters`, `report`, `permissions.defaults`, `lockedPaths`; `$next`/`$self` resolution rules included. `tests/unit/feature/enums-in-sync.test.ts` keeps its Zod enums aligned with the generated Prisma enums.
  - `src/platform/feature/validate.ts` — `validateDefinition(def, ctx) -> Issue[]` implementing exactly the rule codes of the builder part §4 and no others: KEY_FORMAT, KEY_UNIQUE, INITIAL_IS_STEP, TO_RESOLVES, NEXT_UNRESOLVED, REACHABLE, TERMINAL_EXISTS, DEAD_END, ACTOR_PRESENT, ASSIGNEE_RESOLVABLE, GRANT_SOURCE, PARALLEL_BRANCHES, PARALLEL_EXIT, REVISION_LOOP, REQUIRED_FIELDS, REQUIRED_ATTACHMENTS, FORM_REF, BINDING_ARGS, TEMPLATE_EXISTS, SCHEDULE_EXISTS, DEADLINE_FIELD, ROLE_EXISTS, PARENT_TYPE, SCOPE_CONSISTENT, ADAPTER_EXISTS, SURFACE_EXISTS, PRESET_CONSISTENT, LIST_COLUMNS, LOCK_VIOLATION, BACKING_LOCKED, WARN_NO_DEADLINE, WARN_NO_NOTIFY. `compile.ts` — the twelve named, individually unit-tested steps of §3 (normalise + implicit locks, flattenGroups, resolveNext, states incl. one compound state per parallel group with `BranchSpec`s and synthetic `$done`/`$rejected`, transitions with the always-prepended guards `feature.actorAllowed`/`feature.stepComplete` and the ordered exit/notify/custom/enter effects plus the synthetic `<group>.$join`/`$reject`, auto actions, forms per step with questionsHash reuse, taskTemplates for every leaf, reminderTemplates, permissions + rolePermissions + grantRequirements, report, nav/list/counters left in json) producing `CompiledFeature`. `simulate.ts` (`SimulationScript` → `SimulationTrace` with states, branchStates, steps, notifications, guards, grantsNeeded, issues) plus `scripts/feature-simulate.ts`. `publish.ts` — `publishVersion(definitionId, versionId, actor)` in one transaction (validate → compile → upsert WorkflowDefinition `feature:<key>` with `featureVersionId`, FormDefinitions, Permission/RolePermission, ReportDefinition → compiledJson, PUBLISHED, activeVersionId → audit → `feature.published` event with nav cache bust). `locks.ts` (`deriveImplicitLocks`, `assertLockedPathsUnchanged`, `lockedHash`), `migrate.ts` (`planMigration`, `runMigrationBatch`), `nav.ts` (`getNav(actor, dept)`), `counters.ts` (the grouped count over `(department_id, definition_id, current_state_key)` plus assignedToMe and overdue; no cache table).
  - `src/platform/feature/runtime/{create.ts, steps.ts, act.ts, assign.ts, deadline.ts, queries.ts, subject.ts, effects.ts}` — `createRecord` (definition resolution with department shadowing, `zodFromFields`, owner/scope resolution, number from `FeatureNumberSequence` with `SELECT ... FOR UPDATE`, backing adapter, `Workflow.start`, enterStep), `enterStep`/`enterParallel`/`completeBranch`/`exitStep` as compiled workflow effects (the only writers of the derived caches), `resolveAssignee`/`reassign`, deadline resolution through `academic.resolveAnchor`, `act()` as the single path into `Workflow.apply`, the registered guards `feature.actorAllowed`, `feature.stepComplete`, `feature.parallelComplete`, `feature.parentActive`, `feature.deadlineNotPassed`, `feature.autoOnEvent`, and the `feature_record` / `feature_step_instance` SubjectRegistry registrations. `src/platform/feature/adapters/{registry.ts, builtin.ts}` (`registerAdapter` with the AdapterHook, `assertExists`, boot writes `AdapterRegistration` rows from web and worker). `src/platform/feature/seed/{index.ts, merge.ts}` — `seedFeatures(db)` with the §9 rules (validate, `lockedHash` baseline, create + publish with `changeNote='seed:<hash>'`, no-op when the hash matches, otherwise a DRAFT N+1 from `mergeLocked(active.json, codeJson, allLocks)` with `changeNote='seed-upgrade:<hash>'`, published immediately when `SEED_AUTO_PUBLISH=1` as set for the `test` and `e2e` services).
  - Generic runtime routes and components: `src/app/(app)/d/[dept]/f/[featureKey]/{page.tsx, new/page.tsx, [recordId]/page.tsx, [recordId]/steps/[stepKey]/page.tsx, [recordId]/history/page.tsx, [recordId]/f/[childKey]/page.tsx, report/[reportKey]/page.tsx, actions.ts}` and `src/features/runtime/{list.tsx, detail.tsx, step-page.tsx, step-timeline.tsx, parallel-lanes.tsx, action-bar.tsx, actions.ts}` composed from the P7 shell components; `next.config.ts` rewrites `/d/:dept/tasks/:path*` → `/d/:dept/f/task/:path*` (and the placeholders for the later built-in URLs) with P7's three `tasks/*` pages deleted; `/d/[dept]/my-work` re-pointed to the one query over `task` + `task_assignment` joined to `feature_record.current_state_key` and `feature_step_instance.deadline_at`; the dashboard placeholder shows `featureCounters`.
  - Admin builder: `src/app/(admin)/admin/features/{page.tsx (with the "system update pending" badge for a `seed-upgrade:` draft), new/page.tsx, [key]/edit/page.tsx, [key]/versions/page.tsx, [key]/versions/[v]/simulate/page.tsx, [key]/migrate/page.tsx, [key]/records/page.tsx, actions.ts}` with the seven wizard screens in `src/components/admin/feature-wizard/{Wizard,FieldBuilder,StepTree,StepDrawer,TransitionGraph,PresetTable,ListViewBuilder,CounterBuilder,IssuesPanel,SimulatePanel,LockBadge,MigrationDiff,MigrationPreview}.tsx` (autosave with `assertLockedPathsUnchanged`, locked controls read-only) and `/admin/adapters` as a read-only AdapterRegistration list. Worker: queues `feature.migrate` and (from the compiler's auto actions) `workflow.auto_transition` wired to `apps/worker/src/handlers/feature-migrate.ts` (singleton per migration, batches of 200, resumable, blocked records recorded) and the existing auto-transition handler.
  - Seeds, module wiring and the retirement of the provisional definition: `prisma/seed/features/{index.ts, task.ts, case.ts, generic_request.ts}` typed exactly as the `seededFeatures` entries of the builder part (task: one preset per non-extension TaskKind with the documented permission prefixes and parent types; case: task-backed with the Case extension and the interval nudge and auto-close; generic_request: request → static parallel review quorum 1 → approval, the wizard's default clone source), `prisma/seed/adapters.ts` registering `task.backing`, `task.requiredDeliverablesLinked`, `task.autoStartOnAck`, `task.setCompletedAt`, `task.recurrenceSpawn`, `case.backing`, `case.subscribeIntervalNudge`, `case.cancelNudge`, `case.autoClose`, `case.setResolvedAt` and the `feature.*` builtins; `src/modules/tasks/{adapters.ts, surface.tsx, actions.ts, queries.ts}` and `src/modules/cases/{adapters.ts, surface.tsx}` with `src/modules/index.ts` + `src/modules/surfaces.ts` imported by `src/lib/bootstrap.ts` and the worker; the thread escalation target now implemented as `feature.createRecord('case', {parentRef: thread})`; and `prisma/scripts/backfill-task-feature-records.ts` (idempotent: one FeatureRecord per existing Task, workflow instance re-pointed from `task:<id>` to `feature_record:<id>` with the state map, `Task.featureRecordId` set, definition key `task` retired and removed from `PROVISIONAL_DEFINITION_KEYS`), run once in this phase's verify before the constraint is added.
KEY FILES: prisma/schema/feature.prisma; prisma/migrations/<ts>_constraints_p9/migration.sql; src/platform/feature/schema.ts; src/platform/feature/validate.ts; src/platform/feature/compile.ts; src/platform/feature/simulate.ts; src/platform/feature/publish.ts; src/platform/feature/locks.ts; src/platform/feature/migrate.ts; src/platform/feature/nav.ts; src/platform/feature/counters.ts; src/platform/feature/runtime/act.ts; src/platform/feature/runtime/steps.ts; src/platform/feature/adapters/registry.ts; src/platform/feature/seed/index.ts; src/platform/feature/seed/merge.ts; apps/worker/src/handlers/feature-migrate.ts; src/app/(app)/d/[dept]/f/[featureKey]/[recordId]/steps/[stepKey]/page.tsx; src/features/runtime/step-page.tsx; src/components/admin/feature-wizard/StepTree.tsx; src/components/admin/feature-wizard/SimulatePanel.tsx; src/app/(admin)/admin/features/[key]/migrate/page.tsx; prisma/seed/features/task.ts; prisma/seed/features/case.ts; prisma/seed/features/generic_request.ts; prisma/seed/adapters.ts; src/modules/tasks/surface.tsx; prisma/scripts/backfill-task-feature-records.ts; next.config.ts
TESTS:
  - tests/unit/feature/schema.test.ts — defaults, recursion and the `$next`/`$self` rules; a definition using `hierarchy`, `terminals` or a dynamic branch without a person source fails to parse
  - tests/unit/feature/validate.test.ts — one failing fixture in `tests/fixtures/feature-definitions/invalid-*.json` per rule code of §4 and zero issues for the valid `generic_request` fixture
  - tests/unit/feature/compile.test.ts — snapshots of the compiled workflow, forms, `taskTemplates[stepKey]`, reminderTemplates, permission keys and grantRequirements for `generic_request`, `task` and `case`; a StepGroup flattens into consecutive states joined by auto `$next`; a parallel group with quorum 1 compiles to one compound state with two BranchSpecs, the synthetic `$done`/`$rejected`/`$join`/`$reject` and no `joinPolicy`; a dynamic group compiles one `$person` branch; every leaf gets a TaskTemplate and task-backed features get `createTask:false`; a revision action targets an earlier state
  - tests/unit/feature/simulate.test.ts — the happy path and a rejection path for each seeded feature yield the expected states, branch states, tasks, notifications, reminders and `grantsNeeded`; acting on an inactive step is reported as an issue
  - tests/unit/feature/locks.test.ts — implicit locks are derived for `/record/backing`, adapters, guards, effects, surfaces, sourceBindings, autos and preset adapters; a changed locked pointer is detected and changes outside the locks are ignored; `lockedHash` is stable under key reordering
  - tests/integration/feature/runtime.test.ts — createRecord under a parent inherits scope and numbers `F-GR-<year>-0001`; step 1 submit is blocked until the required attachment DocumentLink exists; the backing Task appears in `listTasks` for the assignee and its ack auto-starts the step; the parallel quorum 1 joins on the first approval with the sibling instance skipped and its Task cancelled; group sub-steps run consecutively; `currentStateKey` equals `WorkflowInstance.currentState` at every point; counters match live counts; two departments stay RLS-isolated
  - tests/integration/feature/versioning.test.ts — publishing v2 leaves v1 records pinned with their v1 forms; `planMigration` reports removed states, counts and blocked records; the migration job re-pins and maps in batches and is idempotent on re-run; a record inside a compound state is blocked unless every branch is mapped; editing a locked pointer of the system `task` feature is refused with LOCK_VIOLATION while adding a notification template is accepted
  - tests/integration/feature/seed.test.ts — seeding twice is idempotent; an admin edit to an unlocked label survives re-seeding; a changed locked value in code produces a `seed-upgrade:<hash>` DRAFT that `SEED_AUTO_PUBLISH=1` publishes; every adapter key referenced by a seed exists in `AdapterRegistration` with the matching hook
  - tests/integration/feature/seed-task.test.ts — the compiled `feature:task` workflow has the same states, transitions, guards and effects as the retired `src/platform/workflow/definitions/task.ts` snapshot, the backfill script creates exactly one FeatureRecord per pre-existing Task and is idempotent, and `task_backing_check` holds for every row afterwards
  - tests/integration/feature/nav.test.ts — `getNav` filters by `navigation.visibleRoles` against membership roles (never `committee_chair`), a department definition shadows the faculty one by key, and `feature.published` busts the nav cache tag
  - tests/worker/feature/auto-transition.test.ts — an `auto` action fires exactly once through the ledger key and an `EventHandlerReceipt` prevents a duplicate `feature.autoOnEvent` run
  - tests/worker/feature/migrate.test.ts — the migration resumes after a simulated crash mid-batch without double-migrating and reports blocked ids
  - tests/e2e/feature-builder.spec.ts — the admin builds `leave_request` in the wizard (two parallel reviewers quorum 1 plus a revision loop), sees validation issues for a dangling `to`, fixes them, runs Simulate, publishes, and the entry appears in the sidebar for the chosen roles; opening the system `task` feature shows LockBadges on the backing and adapter fields and refuses a locked edit
  - tests/e2e/feature-runtime.spec.ts — an instructor files a `leave_request` record with an attachment, one of the two reviewers approves from the inbox step task, the DH completes the approval step, the dashboard counter updates, and the P7 task journey still passes unchanged through `/d/cs/tasks` (now a rewrite to `/f/task`)
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p9_feature
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p9_feature/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web npx tsx prisma/scripts/backfill-task-feature-records.ts
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/feature-builder.spec.ts tests/e2e/feature-runtime.spec.ts tests/e2e/p7-task-skeleton.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(feature): feature definition kernel, generic runtime and admin builder" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Admin > Features > New: build a feature with a sub-step group and a parallel group, watch the transition graph update, fix the reported issues, run Simulate (states, tasks, notifications, grants needed) and publish; the sidebar shows the entry with its counter. Create a record as an instructor, act as each reviewer from the inbox and watch the parallel lanes on the detail page. Open /admin/features/task: backing, adapters and guards show LockBadges while notification templates are editable; edit a label, publish v2 and migrate the existing records from the wizard. /d/cs/tasks still behaves exactly as in P7 but the URL bar shows the rewrite is served by the runtime, and `docker compose exec db psql -U dept_migrator -d dept -c "select key, feature_version_id from workflow_definition"` shows only `feature:` keys.
COMMIT: feat(feature): feature definition kernel, generic runtime and admin builder

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P10 — Tabular import pipeline as a seeded feature, availability ledger and conflict service
GOAL: (2 days) The shared staged import pipeline — download template, parse xlsx/csv or a manual grid, map columns with saved profiles, validate with a registered kind validator, preview and fix rows, commit through a registered committer — delivered as the seeded `import_batch` feature (presets `roster` and `class_timetable`; the assessment and attendance presets are added by the portfolio phase as a `seed-upgrade`), plus the single busy-time ledger: `AvailabilityPolicy` and `AvailabilityBlock` with a gist EXCLUDE constraint on hard blocks for persons and resources, conflict checks, free slots, workload and the timetable feed.
DEPENDS: ['P9']
DELIVERABLES:
  - `prisma/schema/import.prisma` as the schema part §17: `ImportBatch { Id, TENANT, kind ImportKind, REF(context), sourceDocumentId?, mappingProfileId?, mode ImportMode, featureRecordId String @unique, summaryJson, uploadedBy, committedAt? (derived cache), replacesBatchId? (BatchChain), Ts }`, `ImportRow { batchId, rowNo, rawJson, normalizedJson?, errorsJson, warningsJson, disposition Disposition, @@unique([batchId, rowNo]) }`, `ColumnMappingProfile { Id, TENANT?, kind, name, sheetName?, headerRowIndex, mappingsJson, headerAliasesJson, @@unique([departmentId, kind, name]) }`.
  - `prisma/schema/availability.prisma` as the schema part §18: `AvailabilityPolicy` (ownerPersonId, purpose PolicyPurpose, weeklyWindowsJson, slotMinutes, maxPerPeriod, breakWindowsJson, blackoutPeriodsJson, validFrom/validTo) and `AvailabilityBlock` (ownerType OwnerType, ownerId, startAt, endAt, weekday?, weekPattern?, recurringFrom?/recurringUntil?, kind BlockKind incl. `blackout`, severity Severity, `REF(source)`, termId?). `prisma/migrations/<ts>_constraints_p10/migration.sql` (snake_case): `ALTER TABLE availability_block ADD CONSTRAINT block_range_valid CHECK (end_at > start_at); ALTER TABLE availability_block ADD CONSTRAINT no_hard_overlap EXCLUDE USING gist (owner_type WITH =, owner_id WITH =, tstzrange(start_at, end_at) WITH &&) WHERE (severity = 'hard' AND weekday IS NULL);` — it covers persons and resources alike, and weekly recurring blocks are expanded into concrete rows per week by `materialiseRecurring(termId)` so they participate in the constraint.
  - `src/platform/import/{index.ts, pipeline.ts, excel.ts, csv.ts, mapping.ts, templates.ts, validators/registry.ts, validators/{roster,class_timetable}.ts, committers/registry.ts, committers/{roster,class_timetable}.ts, subject.ts}` — `downloadTemplate(kind, contextRef)` (exceljs-hardened workbook with the kind's columns and context prefill), `createBatch(kind, contextRef, file|manualRows, mappingProfileId?)` (source Document linked with `linkRole=source`, the `import_batch` FeatureRecord created through the backing adapter, `import.parse` enqueued for files over 500 rows and run inline below that), `parse` (exceljs-hardened with the row cap from `SystemSetting import.maxRows`, papaparse for csv, header row detection), `applyMapping` (header aliases, unmapped required columns as batch-level errors), `validate` (the registered kind validator writes `ImportRow.errorsJson`/`warningsJson`), `preview(batchId, page)`, `fixRow(batchId, rowNo, patch)` + revalidate, `commit(batchId, actor)` (the registered committer in one transaction, `committedAt` through `setField`, the previous batch of the same context marked replaced, `import.committed {kind, contextRef, batchId}` published), `discard`. The roster validator/committer (unknown program or section, duplicate student number in the file, bad email, trigram name-mismatch warning → Person/DepartmentPerson/Student/StudentSectionMembership/Enrollment upserts) and the class_timetable validator/committer (unknown offering, section, room or instructor, overlapping rows in the file, end before start, week pattern → `academic.upsertTimetableSlots`). Queues `import.parse` and `import.validate` appended to the catalogue with handlers `apps/worker/src/handlers/{import-parse,import-validate}.ts`.
  - `prisma/seed/features/import_batch.ts` — the seeded feature exactly as the `seededFeatures.import_batch` entry (navigation group admin, order 50, `showInDashboard:false`; scope from parent; `record.backing = {kind:'module', adapter:'import_batch.backing'}`; steps uploaded → parsed → validated → committed with the rejected/preview loop and the discarded terminal; surfaces grid / mapping / preview) with only the `roster` and `class_timetable` presets registered in this phase — each fixing `fieldDefaults.kind`, the `parsed.validate.effect` / `validated.commit.effect` adapter overrides, `parentSubjectType` and the template export. The portfolio phase adds the assessment and attendance presets, which changes the locked `/presets/*` subtree and therefore arrives through the designed `seed-upgrade` draft. `src/modules/imports/{adapters.ts, surface.tsx}` registers `import_batch.backing`, `import.parse`, `import.applyMapping`, `import.validate.roster`, `import.validate.class_timetable`, `import.commit.roster`, `import.commit.class_timetable`, `import.sectionNotLocked` (guard over `academic.isSectionLocked`, used by the later assessment presets), `import.actorMayCommit`, `import.template.<kind>` and the grid/mapping/preview surfaces; `prisma/seed/features/index.ts` and `tests/unit/feature/seeds.test.ts` gain the new entry.
  - `src/platform/availability/{index.ts, blocks.ts, policies.ts, conflicts.ts, free-slots.ts, workload.ts, suggest.ts, feeds.ts, subject.ts}` — `registerBlocks(sourceRef, blocks[])` (delete-and-insert per source in one transaction, a typed `HardConflictError` listing the colliding rows on an exclusion violation), `removeBySource`, `checkConflicts(candidates, interval|recurring, {purpose?, ignoreSource?}) -> Conflict[]` (hard overlap, soft overlap, `maxPerPeriod` exceeded, outside the declared policy windows), `freeSlots(ownerPersonId, range, purpose)`, `definePolicy` (blackouts become hard `blackout` blocks), `workload(personId, termId)`, `suggestAssignees(interval, pool, constraints)` (no hard conflict, fewest soft conflicts, balanced counts), `scheduleFor(ownerRef, range)`, `materialiseRecurring(termId)`; the outbox feeds `timetable.changed` → hard teaching blocks for instructor and room and `availability.policy.changed` → leave blocks; the workflow effects `registerBlocks`/`removeBlocks` become real; `availability.checkConflicts` and `availability.suggestAssignees` are registered as locked compute adapters for the later schedule features.
  - UI: `/d/[dept]/imports` and `/d/[dept]/imports/new?kind=` are the generic runtime list and create pages of the `import_batch` feature (no bespoke pages); the richer step surfaces live in `src/modules/imports/components/{ManualGrid,MappingEditor,PreviewTable}.tsx` (download template, upload or manual grid, column mapping with profile save, validation preview with inline row fixes and error/warning filters, commit gated by `availableActions`); entry points from `/admin/offerings/[id]` (roster) and the `/admin/calendar` term page (timetable); `/d/[dept]/availability` (my policy per purpose with weekly windows, breaks and blackouts plus a free-slot preview) and `src/components/availability/WeekCalendar.tsx` used on person and resource pages. `tests/fixtures/build-fixtures.ts` generates `tests/fixtures/{roster-valid.xlsx, roster-errors.xlsx, roster.csv, timetable-valid.xlsx, timetable-overlap.csv}` at test setup so no binaries are committed.
KEY FILES: prisma/schema/import.prisma; prisma/schema/availability.prisma; prisma/migrations/<ts>_constraints_p10/migration.sql; src/platform/import/pipeline.ts; src/platform/import/mapping.ts; src/platform/import/excel.ts; src/platform/import/validators/roster.ts; src/platform/import/validators/class_timetable.ts; src/platform/import/committers/roster.ts; src/platform/import/committers/class_timetable.ts; apps/worker/src/handlers/import-parse.ts; prisma/seed/features/import_batch.ts; src/modules/imports/adapters.ts; src/modules/imports/components/ManualGrid.tsx; src/platform/availability/blocks.ts; src/platform/availability/conflicts.ts; src/platform/availability/free-slots.ts; src/platform/availability/feeds.ts; src/app/(app)/d/[dept]/availability/page.tsx; tests/fixtures/build-fixtures.ts
TESTS:
  - tests/unit/import/mapping.test.ts — header aliases resolve case- and space-insensitively, a saved profile takes precedence, an unmapped required column is reported as a batch error
  - tests/unit/import/validators/roster.test.ts — unknown program or section, duplicate student number, malformed email, trigram name-mismatch warning and an empty required cell produce the expected error/warning severities and counts
  - tests/unit/import/validators/class_timetable.test.ts — unknown offering, room or instructor, rows overlapping inside the file, end before start and invalid week patterns
  - tests/unit/import/parse.test.ts — the xlsx and csv fixtures parse to identical row arrays, the row cap is enforced, and a zip-bomb-shaped workbook is refused by exceljs-hardened
  - tests/unit/availability/overlap.test.ts — absolute and weekly recurring overlap detection, hard versus soft classification, `maxPerDay` exceeded, outside the policy windows, and `ignoreSource` excluding the candidate's own blocks
  - tests/unit/availability/free-slots.test.ts — weekly windows minus breaks, blackouts and blocks sliced by `slotMinutes`, UTC arithmetic, empty result without a policy
  - tests/integration/import/pipeline.test.ts — `createBatch` stores the source Document and creates the `import_batch` FeatureRecord in `uploaded`; parse and validate move it to `validated` or `rejected`; `fixRow` revalidates; committing a roster upserts Persons, DepartmentPersons, Students, memberships and Enrollments atomically and marks the previous batch replaced; committing a rejected batch is refused; `import.committed` is published
  - tests/integration/import/timetable-feed.test.ts — committing a timetable batch creates the slots and, after `dispatchPending`, hard teaching blocks for the instructor and the room; recommitting replaces the blocks per source
  - tests/integration/availability/exclusion.test.ts — two overlapping hard blocks for the same person and for the same resource are both rejected by `no_hard_overlap`, a hard plus soft overlap is allowed, `registerBlocks` returns HardConflictError with the colliding rows, and a policy blackout creates hard `blackout` blocks that `freeSlots` excludes
  - tests/e2e/p10-import-availability.spec.ts — the admin downloads the roster template for CS-Y2-A, uploads `roster-errors.xlsx`, sees two errors and one warning, fixes the rows inline, commits, and the roster shows the new students; the DH defines Tue/Thu 09:00–12:00 appointment windows with a lunch break and a leave day, and the free-slot preview excludes the leave day and the seeded Tuesday teaching slot
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p10_import_availability
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p10_import_availability/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx tsx tests/fixtures/build-fixtures.ts
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p10-import-availability.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(import,availability): staged import pipeline as a seeded feature, busy-time ledger and conflict service" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Upload `tests/fixtures/timetable-overlap.csv` for term 1 from the /admin/calendar term page: the preview flags the overlapping rows; fix one and commit; the instructor's person page week calendar now shows hard teaching blocks and Lab A shows its room blocks. In /d/cs/availability add a blackout overlapping a teaching slot and see the hard-conflict error naming the colliding block; the free-slot preview respects windows, breaks and blocks. /d/cs/imports lists the batches through the generic runtime with the preset names, and /admin/features/import_batch shows the two presets with LockBadges.
COMMIT: feat(import,availability): staged import pipeline as a seeded feature, busy-time ledger and conflict service

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P11 — Reporting and export framework, search v1, dashboard projections — kernel complete
GOAL: (3 days) One rendering path for every report: a `ReportDefinition` registry with data sources and HTML templates, PDF through a single shared Playwright Chromium in the worker, xlsx through exceljs-hardened, csv through papaparse, output stored as a locked-capable Document; admin-editable versioned `ExportFormatSpec`s with golden-sample validation; ACL-filtered search v1 over a tsvector generated column maintained from the outbox; event-maintained `DashboardProjection` read models with rebuild; and the role dashboards' first real widgets. After this phase a module phase only adds tables, seeds, adapters, surfaces and registrations.
DEPENDS: ['P10']
DELIVERABLES:
  - `prisma/schema/reporting.prisma` (`ReportDefinition { key @id, title, dataSourceKey, templateKey, parametersSchemaJson, supportedFormats ReportFormat[], requiredPermission, scheduleSpecJson?, isSystem, featureKey? }` GLOBAL, `GeneratedReport { Id, TENANT, reportKey, paramsJson, format, documentId?, requestedBy, status ReportStatus, error?, generatedAt?, Ts }`, `ExportFormatSpec { Id, TENANT?, name, targetSystem, version, fileFormat, sheetName?, headerRowIndex, columnsJson, rowSource ExportRowSource, goldenSampleDocumentId?, status }`), `prisma/schema/search.prisma` (`SearchIndexEntry { subjectType, subjectId, TENANT, title, bodyText, facetsJson, aclTokens String[], search Unsupported("tsvector")?, updatedAt, @@id([subjectType, subjectId]) }`) and `prisma/schema/dashboard.prisma` (`DashboardProjection { projectionKey, rowKey, TENANT, dimensionsJson, valuesJson, updatedAt, @@id([projectionKey, rowKey]) }`).
  - `prisma/migrations/<ts>_search/migration.sql` (hand-written, snake_case, exactly the schema part §22.4): the `search` tsvector generated column on `search_index_entry` (`setweight(to_tsvector('simple', coalesce(title,'')),'A') || setweight(to_tsvector('simple', coalesce(body_text,'')),'B')`) with its GIN index, the GIN index on `acl_tokens`, `course_title_trgm` and `document_title_trgm` trigram indexes and the `document_version` extracted-text GIN index (`person_full_name_trgm` already exists from P3).
  - `src/platform/reporting/{index.ts, registry.ts, generate.ts, html.ts, xlsx.ts, csv.ts, export-spec.ts, templates/print.css, templates/<key>.tsx}` — `registerReport(key, { dataSource(params, actor), templateKey, formats, requiredPermission, parametersSchema })`; `generate(reportKey, params, format, actor)` (permission check, `GeneratedReport` queued, `enqueue('report.generate', …, {singletonKey: 'report:{key}:{sha256(params)}:{format}'})`, html and csv rendered synchronously for small outputs); `html.ts` (React server-rendered to string with the print stylesheet, mustache only for admin-editable `document` templates); `xlsx.ts` (typed columns, number and date formats, multiple sheets, exceljs-hardened); `csv.ts` (papaparse unparse with BOM and escaping); `export-spec.ts` (render rows through a versioned `ExportFormatSpec` and validate against its golden sample — used by the load phase); output stored through `document.storeGenerated` with `DocumentLink(linkRole=generated_output)`. Worker: `apps/worker/src/handlers/report-generate.ts` and `apps/worker/src/pdf/browser.ts` (one lazily launched Chromium per process relaunched on crash, a new context per job, `page.setContent(html, {waitUntil:'load'})`, `page.pdf({format:'A4', printBackground:true, margin:'15mm', displayHeaderFooter:true})`, `p-limit(PDF_CONCURRENCY)`, closed on SIGTERM), with the requester notified through the `report_ready` template. Seeded system reports proving each format: `department_activity` (pdf/xlsx/csv/html), `task_list` (xlsx/csv/pdf) and `audit_extract` (csv, admin only).
  - `src/platform/search/{index.ts, indexer.ts, query.ts, acl.ts, rebuild.ts}` — `index(subjectRef)` building title/bodyText/facets from `SubjectRegistry.indexDoc` and `aclTokens` from `accessResolver` (`dept:<id>`, `role:<roleKey>@dept:<id>`, `person:<id>`, `group:<id>`), `remove`, `search(query, actor, {types, facets, limit})` through `$queryRaw` with `websearch_to_tsquery('simple', $1)` and `ts_rank`, filtered by `acl_tokens && $actorTokens` and post-checked with `can(actor, '<type>.view', ref)`, `suggest(prefix, types)` over the trigram indexes, `rebuild(types)`; the incremental path is an outbox subscriber registered in `src/platform/audit/subscribers.ts` (idempotent per (subjectType, subjectId, updatedAt)), and the bulk path is the new queue `search.reindex` with `apps/worker/src/handlers/search-reindex.ts`. Indexed in this phase: person, staff_profile, course, course_offering, group, task, document (title + extracted text), feature_record, campaign, resource.
  - `src/platform/dashboard/{index.ts, projections/registry.ts, projections/{openIssues,staffActivity,campaignProgress,portfolioStatus}.ts, widgets.ts, rebuild.ts}` — `registerProjection(key, {events, apply(event, tx), rebuild(departmentId, tx)})` with the incremental `apply` running inside the outbox dispatcher's receipt transaction and the bulk `rebuild` behind the new queue `projection.rebuild` (`apps/worker/src/handlers/projection-rebuild.ts`) and `scripts/rebuild-projections.ts`; projections live now: openIssues (non-terminal case records and open conversation threads by section and responsible person), staffActivity (task completions, comments, submissions and profile items per person per calendar period), campaignProgress (invitations pending/submitted per campaign), portfolioStatus registered as a stub for the portfolio phase; `getDashboard(actor)` returning the widgets gated by `can()` — pendingApprovals (`Workflow.listInstances` in waiting states where the actor may act), myTasks and overdueTasks, upcomingDeadlines (`scheduler.upcoming`), inboxSummary, featureCounters, openIssues, staffActivities (DH/DPT), campaignProgress — with `src/modules/dashboard/{layouts.ts, widgets/*.tsx}` defining the DH/DPT, instructor, student-rep and lab-staff layouts that later phases extend by registration.
  - UI and hardening: `/d/[dept]/reports` (the catalogue the actor may run, a parameter form generated from `parametersSchemaJson`, format buttons, generation status with download links) plus Export buttons on the my-work table and the campaign results page reusing `generate()`; `/d/[dept]/search?q=` (type facets, grouped results) and `src/components/layout/CommandSearch.tsx` in the topbar with `suggest`, with the PersonPicker and CoursePicker switching to `suggest` for large lists; `/d/[dept]/page.tsx` rendering the role layout; `/admin/export-specs` (ExportFormatSpec versions, column editor, golden-sample upload and validation) and the search/projection rebuild actions on `/admin/jobs`; security headers in `next.config.ts` (`headers()` with a CSP that allows inline styles only on the report routes, `X-Frame-Options: DENY`, `Referrer-Policy`, `nosniff`) and the P8 rate limiter applied to the auth routes; a README section "Kernel complete: how to add a module" listing every registration point (SubjectRegistry, feature seed + presets, adapters, surfaces, reports, projections, search docs, templates, reminder schedules, queues, permissions).
KEY FILES: prisma/schema/reporting.prisma; prisma/schema/search.prisma; prisma/schema/dashboard.prisma; prisma/migrations/<ts>_search/migration.sql; src/platform/reporting/registry.ts; src/platform/reporting/generate.ts; src/platform/reporting/html.ts; src/platform/reporting/xlsx.ts; src/platform/reporting/csv.ts; src/platform/reporting/export-spec.ts; src/platform/reporting/templates/print.css; apps/worker/src/handlers/report-generate.ts; apps/worker/src/pdf/browser.ts; src/platform/search/query.ts; src/platform/search/acl.ts; apps/worker/src/handlers/search-reindex.ts; src/platform/dashboard/projections/registry.ts; src/platform/dashboard/widgets.ts; apps/worker/src/handlers/projection-rebuild.ts; scripts/rebuild-projections.ts; src/app/(app)/d/[dept]/reports/page.tsx; src/app/(app)/d/[dept]/search/page.tsx; src/app/(app)/d/[dept]/page.tsx; src/app/(admin)/admin/export-specs/page.tsx; src/modules/dashboard/layouts.ts; README.md
TESTS:
  - tests/unit/reporting/xlsx.test.ts — a workbook built from typed columns round-trips through exceljs-hardened, multiple sheets are written, and csv escaping handles quotes, commas, newlines and the BOM
  - tests/unit/reporting/html.test.ts — the `department_activity` template renders its sections and tables from a fixture ReportData and `parametersSchemaJson` rejects an invalid period
  - tests/unit/reporting/export-spec.test.ts — a versioned ExportFormatSpec maps rows to the declared headers and order, a missing required column fails, and a diff against the golden sample is reported per cell
  - tests/unit/search/query.test.ts — `websearch_to_tsquery` input sanitisation, the facet SQL builder, and ACL token derivation from a stubbed accessResolver
  - tests/unit/dashboard/projections.test.ts — openIssues increments on case open and decrements on resolve and close, staffActivity buckets by calendar period, campaignProgress follows invitation status events, and `apply` is idempotent for a replayed event
  - tests/integration/reporting/generate.test.ts — `generate` enforces `requiredPermission`, html returns synchronously, running the handler directly produces the GeneratedReport row plus the Document and its `generated_output` link for xlsx, and two concurrent requests with the same params dedupe on the singleton key
  - tests/integration/search/acl.test.ts — indexing a task then searching as its assignee finds it while an unrelated instructor does not and another department never does; suggest matches a person-name prefix; `rebuild` reproduces byte-identical entries; a document's extracted text is searchable
  - tests/integration/dashboard/rebuild.test.ts — after a sequence of task, case and campaign events drained through the outbox the projection rows equal a fresh rebuild, and `getDashboard` hides staffActivities from an instructor while showing it to the DH
  - tests/integration/tenancy/rls-coverage.test.ts (re-run) — the reporting, search and dashboard tables are classified and policed, and `ReportDefinition` is confirmed GLOBAL
  - tests/worker/report-pdf.test.ts — `report.generate` for `department_activity` in pdf yields a Document whose bytes start with `%PDF-` and whose extracted text contains the report title; the xlsx and csv variants parse; the browser relaunches after a simulated crash and `PDF_CONCURRENCY` is respected
  - tests/e2e/p11-reports-search-dashboard.spec.ts — the DH opens /d/cs/reports, generates the department activity report as PDF and as Excel and downloads both; searches a course code in the topbar and finds the course, its offering and a task mentioning it, while an instructor's search does not reveal the DH's private case; the DH dashboard shows overdue tasks, pending approvals, upcoming deadlines and feature counters consistent with the seeded data, and each tile links to a filtered list with matching counts; the admin triggers a projection rebuild from /admin/jobs
  - tests/e2e regression — every earlier spec is re-run in the same Playwright invocation
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p11_reporting_search_dashboard
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p11_reporting_search_dashboard/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(reporting): report framework with worker pdf, acl-filtered search and dashboard projections" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
  $ docker compose run --rm web git tag -a v0.11-kernel -m "platform kernel complete: P0-P11"
MANUAL: Reports > Department activity > term 1 > PDF: `docker compose logs -f worker` shows one Chromium launch and the render, and the download opens a paginated A4 PDF with the print stylesheet; the Excel variant opens with typed columns and the csv with a BOM. Type a course code in the topbar search box and see grouped results with suggestions; sign in as an instructor and confirm the DH's case is absent. The DH dashboard tiles link to filtered lists whose counts match, /admin/jobs rebuild finishes with identical projection counts, and /admin/export-specs validates an uploaded golden sample. `.\scripts\verify.ps1` runs all five guard-rail steps green from the host.
COMMIT: feat(reporting): report framework with worker pdf, acl-filtered search and dashboard projections

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

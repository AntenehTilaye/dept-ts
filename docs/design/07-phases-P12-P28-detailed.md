SOURCE: repair2

######## P12 — Committees and committee reports (first module on the feature runtime)
GOAL: (3 days) Deliver the first two seeded module features as a pure delta on the P0-P11 kernel: `committee` (module-backed on Group) and `committee_report` (child of a committee, standalone Submission under a review loop with issue escalation to a case). Only the module tables, adapters, surfaces, registry, seeds, templates, permission keys, report registrations and the /committees rewrite are new; the generic runtime and surfaces registry (P9), reporting core and `report.generate` queue (P11), uploads (P6), derived-grant subscribers (P3) and the `case`/`task` seeds (P9) are consumed unchanged.
DEPENDS: ['P11']
DELIVERABLES:
  - Schema delta `prisma/schema/committee.prisma` (the file exists empty from P1): `Committee { Id, TENANT, name, purpose?, type?, startDate? @db.Date, endDate? @db.Date, chairPersonId FK? Person, groupId String @unique + group Group, responsibilitiesText?, featureRecordId String @unique, Ts }` and `CommitteeReport { Id, TENANT, committeeId FK Committee, periodFrom/periodTo @db.Date, authorPersonId FK Person, submissionId String @unique, featureRecordId String @unique, submittedAt? (derived cache), @@index([committeeId]) }` exactly as schema §20 - no `workflowInstanceId`, no `ops.prisma`. Migration `p12_committee`: `migrate dev --create-only`, then `gen-rls.ts --append` over the globbed migration file (both tables are TENANT), then `migrate dev` + `prisma generate`.
  - Seeds `prisma/seed/features/committee.ts` and `prisma/seed/features/committee_report.ts`, typed `FeatureDefinition` objects exactly per builder seededFeatures, appended to `prisma/seed/features/index.ts` after `task`/`case` (parents first). committee: navigation {group:'operations', order:10, icon:'users', visibleRoles:[department_head,deputy_head,instructor,committee_member]}, scope {level:'department'}, record {numberPrefix:'CM', backing:{kind:'module', adapter:'committee.backing'}}, steps setup (form name/purpose/type/startDate/endDate/chair person_picker/members person_picker multi/responsibilities; slot `tor` required; assignee role department_head|deputy_head; activate -> active) -> active (stepType wait, surface 'activity_history'; deactivate -> inactive requiredComment; update_members -> $self) -> inactive (wait; reactivate -> active; dissolve -> dissolved), terminalStates [dissolved (cancelled)], listViews all|active|mine, dashboardCounters active_committees, pending_reports, overdue_committee_tasks, permissions.defaults {department_head:full, deputy_head:full, instructor:assigned, committee_member:assigned, committee_chair:assigned}. committee_report: navigation {group:'operations', order:11, icon:'file-text', visibleRoles:[department_head,deputy_head,instructor,committee_member]}, scope {level:'department', scopeFrom:'parent'}, parentSubject {subjectType:'committee', relation:'parent', required:true, inheritPermissions:true, listUnderParent:true, createFromParent:true}, record {numberPrefix:'CR', backing:{kind:'module', adapter:'committee_report.backing'}}, steps draft (inline form period_from, period_to, progress_summary, completed_tasks task_picker sourceBinding tasks_in_context, recommendations, issues_requiring_attention repeating_group{issue, urgency}; slot attachments; assignee relationship parent_member; deadline relative 0d from record_field period_to; reminders `default_7_3_1_0_overdue` (P5 kernel key); submit -> submitted guard committee.memberGuard) -> submitted (assignee role department_head|deputy_head; onEnter notify committee_report_submitted to role:department_head; review -> reviewed) -> reviewed (approve -> approved; request_revision -> revision_required requiredComment) -> revision_required (assignee owner; resubmit -> submitted), terminalStates [approved (success)], explicit permissions committee.report.submit (submit/resubmit) and committee.manage (review/approve/request_revision), listViews by_committee|pending_review, report {key:'committee_report', formats:['pdf','html'], templateKey:'report.committee_report', dataSource:'records_with_answers'}, lockedPaths add `/record/fields/0` (committeeId comes from the parent) on top of the implicit locks.
  - `prisma/seed/features/index.ts` also gains the header comment that carries the single builder-§9-placement -> plan-phase mapping table, so the builder's §9 sentence and the phase numbering can never drift again: generic_request/task/case P9, import_batch (presets roster, class_timetable) P10, committee + committee_report P12, course_offering + import presets assessment/attendance/students P13, portfolio + cqi P14, campaign (add_drop, elective, preference) P15, campaign presets evaluation + survey P16, meeting P17, appointment P18, annual_plan/planned_activity/quarterly_report P20, exam_schedule + exam_timetable preset P21, lab_schedule + lab_schedule preset P22, assets preset P23, staff preset P24, load_cycle + load_result preset P25, student_issue + announcement P26.
  - `src/modules/committees/adapters.ts` (module added to `src/modules/index.ts`; boot writes `AdapterRegistration` rows): backing `committee.backing` (creates `Committee` + `Group(kind=committee)`, syncs `GroupMembership`/chair from the record fields on every save, writes the ids into `FeatureRecord.data`), backing `committee_report.backing` (`CommitteeReport` row + binds the step `Submission`), effect `committee.syncGroupStatus` (on_enter active/inactive -> `Group.status`, emits `group.status.changed` so the P3 `src/platform/identity/derive.ts` outbox subscribers expire/restore COMMITTEE_MEMBER/COMMITTEE_CHAIR grants and the nightly `grant.reconcile` repairs them), guard `committee.chairIsMember`, guard `committee.memberGuard`, effect `committee.escalateIssueToCase` (per `issues_requiring_attention` row: `Thread.escalateAnswer` -> a `case` FeatureRecord (P9 seed) whose parent/origin is this committee_report, priority from urgency), effect `committee.completeReportedTasks`, effect `committee_report.setSubmittedAt`, source_binding `members_of_parent`, source_binding `tasks_in_context`.
  - `src/modules/committees/surface.tsx`: `surfaces.committee = { stepRenderers: { activity_history: ActivityHistory }, detailExtras: CommitteeOverview }` and `surfaces.committee_report = { detailExtras: IssueTable }`; components in `src/modules/committees/components/{ActivityHistory.tsx, CommitteeOverview.tsx, IssueTable.tsx}`; both collected by P9's `src/modules/surfaces.ts`.
  - `src/modules/committees/registry.ts` (`SubjectRegistry.register('committee')` and `('committee_report')`: label, snapshot, contextOf -> {departmentId, committeeId, groupId}, relationships parent_member|parent_chair|owner from `GroupMembership`, variables committee_name/chair_name/period_from/period_to, indexDoc, url via the canonical `/d/<dept>/f/committee/<id>`, accessResolver). This registration is also what resolves the `committee` parent that P9's `task` preset `committee_task` declares (`PARENT_TYPE` reads `SubjectRegistry.types()` through `src/modules/index.ts`; meeting P17, lab_session P22 and asset P23 close the same way). Plus `queries.ts` (`activityHistory(committeeId)` = merged `WorkflowTransitionLog` + `Submission` + `Comment` + `DocumentLink` rows whose contextRef is the committee, ordered by time) and `actions.ts` (member/chair edits through `safeAction`).
  - Seeds: `prisma/seed/templates/committee.ts` (registered in P5's `prisma/seed/templates/index.ts`; this phase is the ONLY source of these keys - P5 seeds kernel keys only): `committee_assignment`, `committee_report_submitted`, `committee_report_revision_required` (kind message) and `report.committee_report` (kind report, mustache, rendered by P11's `src/platform/reporting/html.ts`). `prisma/seed/permissions.ts` gains `committee.manage`, `committee.report.submit`, `committee.view`. No reminder-schedule change.
  - Report registrations `src/modules/committees/reports.ts` calling P11's `src/platform/reporting/registry.ts`: `committee` (members, tasks by state, reports, documents; pdf|xlsx|csv|html; requiredPermission committee.view) and `committee_task` (tasks with assignee, due, state, ack status). No new reporting platform files, no new queue and no new worker handler: generation runs on P11's `report.generate` queue via `apps/worker/src/handlers/report-generate.ts`.
  - Routing: `next.config.ts` `rewrites()` gains `/d/:dept/committees/:path*` -> `/d/:dept/f/committee/:path*` (afterFiles semantics, so later `(modules)` pages still win). No page files: list, detail, child lists and step pages are the P9 generic runtime; counters come from the seed's `dashboardCounters` through `featureCounters()`. Demo seed `prisma/seed/demo/committees.ts` (SEED_DEMO=1): two committees with chair and members, one ToR PDF stored through `StorageProvider`, three `task` records with preset `committee_task` and parent = committee.
KEY FILES: prisma/schema/committee.prisma; prisma/migrations/<stamp>_p12_committee/migration.sql; prisma/seed/features/committee.ts; prisma/seed/features/committee_report.ts; prisma/seed/features/index.ts; src/modules/committees/adapters.ts; src/modules/committees/surface.tsx; src/modules/committees/registry.ts; src/modules/committees/queries.ts; src/modules/committees/reports.ts; prisma/seed/templates/committee.ts; prisma/seed/permissions.ts; src/modules/index.ts; next.config.ts; prisma/seed/demo/committees.ts
TESTS:
  - tests/unit/feature/seeds.test.ts (extended): `committee` and `committee_report` parse with `FeatureDefinitionSchema`, `validateDefinition` returns zero errors, the `compile()` snapshots match (states setup/active/inactive/dissolved and draft/submitted/reviewed/revision_required/approved, a `taskTemplates` entry per leaf step, `reminderTemplates.draft`, permission keys committee.report.submit / committee.manage, `grantRequirements` COMMITTEE_MEMBER@committee for the `parent_member` assignee), `lockedHash` equals the recomputed baseline, and the placement mapping comment in `index.ts` lists exactly the keys the seed list contains.
  - tests/unit/committees/activity-history.test.ts: the pure merge of transitions, submissions, comments and document links orders by time and labels each row kind, with an empty-history case.
  - tests/integration/committees/committee.test.ts: creating a `committee` record creates `Committee` + `Group(kind=committee)` + chair `GroupMembership`, and after outbox dispatch a derived COMMITTEE_CHAIR@committee and COMMITTEE_MEMBER@committee `RoleGrant`; `deactivate` sets `Group.status=inactive` and `validTo` on those grants; a committee `task` record is visible to a member at level `assigned` and invisible to an unrelated instructor; departments CS and EE stay isolated under RLS.
  - tests/integration/committees/report.test.ts: a non-member submit fails `committee.memberGuard`; a member submit creates exactly one `Task(kind=feature_step)` for `submitted` assigned to the DH, one `Submission` bound to the step instance and `CommitteeReport.submittedAt` written only by the effect; `request_revision` without a comment is refused; DH escalation creates a `case` record whose parent is the committee_report and whose `Case.issue` is the answer text; resubmit reaches `approved`.
  - tests/integration/feature/seed.test.ts (extended): re-seeding is idempotent, renaming a step label creates v2 and survives re-seed, editing `/steps/0/actions/0/guards` or `/record/backing` is refused with LOCK_VIOLATION, and every adapter key of both seeds exists in `AdapterRegistration` with the matching hook.
  - tests/worker/report-pdf.test.ts (extended): `report.generate` for `committee` in pdf yields a `Document` whose bytes start with `%PDF` and whose extracted text contains the committee name; the xlsx variant opens with exceljs-hardened; a duplicate enqueue with the same params is dropped by `singletonKey report:{key}:{sha256(params)}:{format}`.
  - tests/components/committees/ActivityHistory.test.tsx: grouped rows and the empty state render from a fixture.
  - tests/e2e/p12-committees.spec.ts: DH creates 'Curriculum Committee' with a chair and two members, uploads the ToR through `/api/uploads`, activates it and creates a committee task; the chair sees the task in `/my-work` and the inbox and submits a committee report with one high-urgency issue; DH requests revision, the chair resubmits, DH escalates the issue to a case and approves; the DH dashboard `active_committees` counter reads 1 and the committee report PDF downloads from `/d/cs/f/committee_report/<id>/report/committee_report`.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p12_committee
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p12_committee/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p12-committees.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(committees): seeded committee and committee_report features with group backing, issue escalation and committee reports" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Create a committee with a ToR and members, assign a committee task, submit a committee report as the chair from the committee's Reports tab, escalate an issue to a case during DH review, then open /admin/features/committee_report: adding a question to the draft form is allowed while deleting `issues_requiring_attention` or editing the `committee.memberGuard` guard shows the LockBadge and is refused at publish; finally generate the committee report as PDF and xlsx.
COMMIT: feat(committees): seeded committee and committee_report features with group backing, issue escalation and committee reports

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P13 — Assessment import presets, results and metrics, plus the course_offering seed
GOAL: (3 days) Bind P10's import pipeline to the builder with a delta, not a second implementation: P10's `import_batch` seed gains the assessment, attendance and students presets, `src/modules/assessment/adapters.ts` registers only the new per-kind validators/committers/templates, and this phase adds the assessment module tables, grade/metric computation, the `snapshot.compute` queue and handler, the `course_offering` seed with its backfill and the NOT NULL tightening of `CourseOffering.featureRecordId`.
DEPENDS: ['P12']
DELIVERABLES:
  - Schema delta `prisma/schema/portfolio.prisma` (assessment half): `AssessmentScheme`, `AssessmentComponent`, `AssessmentRecord`, `StudentAttendanceSummary`, `StudentCourseResult`, `CourseMetricsSnapshot` exactly as schema §20 (Portfolio/PortfolioScope/CQIReport/CQIItem follow in P14). Migration `p13_assessment`: `migrate dev --create-only`, `gen-rls.ts --append` (six TENANT tables), then hand-append `CREATE TRIGGER course_metrics_snapshot_frozen BEFORE UPDATE ON course_metrics_snapshot FOR EACH ROW WHEN (OLD.frozen) EXECUTE FUNCTION raise_immutable();` (the function ships with P4's `<stamp>_append_only` migration, schema §22 item 5), then `migrate dev` + `prisma generate`.
  - Delta on P10's seed: `prisma/seed/features/import_batch.ts` gains the presets `assessment`, `attendance` and `students` (each fixing locked `fieldDefaults.kind`, the `parsed.validate.effect` / `validated.commit.effect` adapter overrides, `parentSubjectType` = section_offering for assessment/attendance and term for students, and the template export). P10 stays the single owner of the seed's lifecycle and of the presets roster and class_timetable. Because `/presets/*` is a locked subtree, re-seeding produces a `seed-upgrade:<lockedHash>` DRAFT; `SEED_AUTO_PUBLISH=1` (test and e2e services, P0) publishes it immediately, and this phase's dev seed therefore runs with `-e SEED_AUTO_PUBLISH=1`.
  - New seed `prisma/seed/features/course_offering.ts` per builder seededFeatures (navigation {group:'academic', order:5, icon:'graduation-cap'}, parentSubject course, record {numberPrefix:'CO', backing:{kind:'module', adapter:'course_offering.backing'}, fields termId term_picker (locked), coordinatorPersonId, decisionNote}, steps planned -> confirmed -> running with calendar-anchored auto transitions, terminals completed/cancelled, permission academic.manage) plus `prisma/seed/features/backfill-offering-records.ts` (called by `prisma/seed.ts` after `seedFeatures`, idempotent: one `course_offering` record per existing offering, state `running` when the term is current else `planned`). `CourseOffering.featureRecordId` has been nullable since P3; after the seed and backfill this phase edits `prisma/schema/academic.prisma` to `featureRecordId String @unique` and creates the migration `constraints_p13`, which is the prisma-generated `SET NOT NULL` that brings the file in line with schema §5.
  - `src/modules/assessment/adapters.ts` registers ONLY new keys: validate `import.validate.{assessment,attendance,students}` and effect `import.commit.{assessment,attendance,students}` into P10's `src/platform/import/validators/registry.ts` / `committers/registry.ts`, export `import.template.{assessment,attendance}`, and the course_offering adapters backing `course_offering.backing` (calls `academic.ensureOffering`), auto `offering.autoStart` / `offering.autoComplete` (calendar-anchored `workflow.auto_transition` jobs), effects `offering.freezeSnapshots`, `offering.setSchemeStructureLock`, `offering.notifyCancelled`, `offering.decisionFromCampaign` (invoked by the P15 campaign decision effects) and source_binding `offerings_in_term`. `import_batch.backing`, `import.parse`, `import.applyMapping`, `import.setCommittedAt`, the guards `import.sectionNotLocked` / `import.actorMayCommit` and the `grid`/`mapping`/`preview` surfaces are P10's registrations, reused verbatim; the only surface added here is `surfaces.course_offering = { detailExtras: OfferingSummary }` in `src/modules/assessment/surface.tsx`.
  - `src/modules/assessment/{service.ts (offering scheme CRUD, per-section override components, recomputeSchemeStructureLock), validators.ts (one issue code per rule written into ImportRow.errorsJson: UNKNOWN_STUDENT, DUPLICATE_STUDENT, NAME_MISMATCH warning, NON_NUMERIC, OUT_OF_RANGE, MISSING_COMPONENT, ROSTER_STUDENT_MISSING, TOTAL_MISMATCH, ATTENDANCE_OVER_HELD), committers.ts (replace all AssessmentRecord rows of the section in one withTenantTx, mark the prior ImportBatch replaced, write StudentAttendanceSummary / Student + StudentSectionMembership through src/platform/people per kind, emit assessment.committed), results.ts (StudentCourseResult from components and the program grade scale), metrics.ts (average, pass/fail, distribution, component stats, completion and attendance rate -> CourseMetricsSnapshot at section and offering level), consolidate.ts (course-level consolidation flagging incomplete component mappings and unlocked sections), queries.ts, actions.ts, registry.ts, reports.ts}`.
  - Queue append: `src/platform/scheduler/queues.ts` gains `snapshot.compute` (policy `singleton`, retryLimit 2) and `apps/worker/src/handlers/snapshot-compute.ts` is delivered: on `assessment.committed` / `attendance.committed` / term close it recomputes `StudentCourseResult` and both snapshot levels, skips `frozen` rows and is idempotent through `EventHandlerReceipt` and the ledger key `snapshot:{sectionOfferingId}:{importBatchId}` under `singletonKey sectionOfferingId`.
  - Module page `src/app/(app)/d/[dept]/(modules)/assessment/[sectionOfferingId]/page.tsx` (the only page under `(modules)/assessment`, richer than the runtime: scheme editor with section overrides, template download, upload to `/api/uploads`, mapping profiles, validation preview with inline row fixes, manual grid using the same validator, commit, batch history with replaced badges). The `/d/:dept/imports/:path*` -> `/d/:dept/f/import_batch/:path*` rewrite belongs to P10 and is not re-added here.
  - Report registration `src/modules/assessment/reports.ts`: `course_performance` (pdf|xlsx|csv, per offering with per-section rows) on P11's registry; `prisma/seed/templates/assessment.ts` (`report.course_performance`, message `assessment_committed`) registered in P5's templates index; `prisma/seed/permissions.ts` gains `academic.manage`, `assessment.import`, `assessment.view`.
  - Fixtures and demo: `tests/fixtures/build-fixtures.ts` (P10) is extended to generate `assessment-valid.xlsx`, `assessment-errors.xlsx`, `assessment.csv`, `attendance.xlsx` and `students.csv` at test setup, so no workbooks are committed; `prisma/seed/demo/imports.ts` adds a default `ColumnMappingProfile` for the assessment kind and a previous-term CS201 offering with committed results and frozen snapshots (the baseline P14's CQI comparison needs).
KEY FILES: prisma/schema/portfolio.prisma; prisma/migrations/<stamp>_p13_assessment/migration.sql; prisma/migrations/<stamp>_constraints_p13/migration.sql; prisma/seed/features/import_batch.ts; prisma/seed/features/course_offering.ts; prisma/seed/features/backfill-offering-records.ts; src/modules/assessment/adapters.ts; src/modules/assessment/validators.ts; src/modules/assessment/committers.ts; src/modules/assessment/metrics.ts; src/modules/assessment/results.ts; src/modules/assessment/consolidate.ts; src/platform/scheduler/queues.ts; apps/worker/src/handlers/snapshot-compute.ts; src/app/(app)/d/[dept]/(modules)/assessment/[sectionOfferingId]/page.tsx; src/modules/assessment/reports.ts; tests/fixtures/build-fixtures.ts
TESTS:
  - tests/unit/assessment/validators.test.ts: every generated fixture row produces the exact issue code (UNKNOWN_STUDENT, DUPLICATE_STUDENT, NAME_MISMATCH as warning, NON_NUMERIC, OUT_OF_RANGE, MISSING_COMPONENT, ROSTER_STUDENT_MISSING, TOTAL_MISMATCH) and attendance rejects sessionsAttended > sessionsHeld.
  - tests/unit/assessment/mapping.test.ts: assessment header aliases ('Student ID', 'ID No.', 'Reg No') resolve to studentNumber through P10's alias resolution, and a saved `ColumnMappingProfile` wins over fuzzy matching.
  - tests/unit/assessment/metrics.test.ts: averages, pass/fail against two grade scales, distribution, component stats, completion and attendance rates.
  - tests/unit/feature/seeds.test.ts (extended): the `import_batch` seed now compiles five presets, each emitting its own permission keys and a locked `fieldDefaults.kind`; `course_offering` validates and compiles and its calendar-anchored auto actions compile to `scheduleAutoTransition` effects.
  - tests/integration/import/commit.test.ts: commit replaces the section's records atomically, marks the prior batch replaced, keeps the source `Document`, and `assessment.committed` triggers `snapshot.compute` exactly once (EventHandlerReceipt); commit is refused by P10's `import.sectionNotLocked` once the section is locked while a sibling section still commits; a person without a `TeachingAssignment` or `academic.manage` is refused by `import.actorMayCommit`.
  - tests/integration/academic/offering-lifecycle.test.ts: `course_offering` records auto-start at the teaching period start and auto-complete at its end through `workflow.auto_transition`, `schemeStructureLockedAt` is written only by the effect, the backfill script is a no-op on a second run, and after `constraints_p13` an offering cannot be inserted without a `featureRecordId`.
  - tests/worker/snapshot-compute.test.ts (new): the handler runs under `singletonKey sectionOfferingId`, is idempotent on re-enqueue and skips frozen snapshots.
  - tests/e2e/p13-assessment.spec.ts: the instructor downloads the roster template for CS201-A, uploads the generated `assessment-errors.xlsx`, sees per-row issues, fixes two rows in the preview and commits; the section shows committed status and the recomputed results; a second upload creates a new batch and marks the first replaced; DH downloads `course_performance.xlsx`.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p13_assessment
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p13_assessment/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web npx prisma migrate dev --create-only --name constraints_p13
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p13-assessment.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(assessment): assessment import presets, validators, committers, metric snapshots and the seeded course_offering feature" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: In /admin/features/import_batch publish the pending 'system update' draft (the three new presets) and confirm the preset `kind` and the validator/committer adapters render with LockBadges; then define the CS201 scheme (Quiz 10, Mid 30, Final 60), download the roster template, upload the generated assessment-errors workbook, fix the flagged rows in the preview, commit, and confirm the recomputed results and the section snapshot.
COMMIT: feat(assessment): assessment import presets, validators, committers, metric snapshots and the seeded course_offering feature

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P14 — Course portfolios and CQI (parallel quorum review, comparison chain)
GOAL: (3 days) Seed the `portfolio` feature (sequential prepare group, parallel dh_review/dpt_review with quorum 1, revision loop, approval that locks sections and freezes snapshots) and its child `cqi` feature (comparison, prior items, narrative, action tasks), add the portfolio/CQI tables, append the `portfolio.provision` queue and cron, and register the portfolio/CQI/consolidated reports. The parallel-state engine (P4), reporting core (P11) and import pipeline (P10/P13) are reused unchanged.
DEPENDS: ['P13']
DELIVERABLES:
  - Schema delta `prisma/schema/portfolio.prisma` (second half): `Portfolio`, `PortfolioScope`, `CQIReport`, `CQIItem` exactly as schema §20 (`Portfolio.featureRecordId @unique`, `CQIReport.featureRecordId @unique`, no workflow instance ids). Migration `p14_portfolio_cqi`: `migrate dev --create-only`, `gen-rls.ts --append` (four TENANT tables), `migrate dev`, `prisma generate`.
  - Seeds `prisma/seed/features/portfolio.ts` and `prisma/seed/features/cqi.ts` per builder seededFeatures, appended to `prisma/seed/features/index.ts` (portfolio before cqi). portfolio: navigation {group:'academic', order:10, icon:'book-open', visibleRoles:[department_head,deputy_head,instructor]}, scope {level:'department', scopeFrom:'parent'}, parentSubject {subjectType:'course_offering', relation:'parent', required:true, createFromParent:false}, record {numberPrefix:'PF', backing:{kind:'module', adapter:'portfolio.backing'}, fields courseOfferingId offering_picker, sectionOfferingIds section_picker multi, termId term_picker, studentCount computed `portfolio.studentCount`} with `lockedPaths` on those three id fields; steps = StepGroup `prepare` { course_info (inline form objectives/outcomes/teaching_activities/assessment_methods/completion_status; continue -> $next) -> assessment (stepType adapter, surface 'assessment'; continue -> $next guard portfolio.allScopesCommitted) -> narrative (inline form sessionsHeld/averageAttendancePercent/attendanceNotes/challenges/reflection/recommendations; continue -> $next) -> cqi (stepType wait, surface 'cqi_summary'; continue -> $next guard portfolio.cqiComplete) } -> ready (stepType review, assignee owner, deadline calendar {periodKind:'portfolio_submission', edge:'end', termFrom:'record_field', fieldKey:'termId'}, reminders `portfolio_7_3_1_0_overdue`; submit -> submitted) -> submitted (assignee role department_head; onEnter notify portfolio_submitted; mark_reviewed -> review) -> ParallelGroup `review` { static branches dh_review, dpt_review; completion quorum 1; onComplete approved; onAnyReject revision_required } -> revision_required (assignee owner; resubmit -> submitted); terminalStates approved (success; effects portfolio.setApprovedAt, portfolio.lockScopes, portfolio.freezeSnapshots) plus the extra `reopen -> narrative` action for admin|department_head; explicit permissions portfolio.submit_own / portfolio.review / portfolio.approve (DPT excluded through `SystemSetting rbac.manageExcludedPermissions`); listViews my_portfolios|by_term|not_started; counters portfolios_pending_review, my_portfolio_due. cqi: navigation {group:'academic', order:11, icon:'trending-up'}, parentSubject {subjectType:'feature_record', featureKey:'portfolio'}, record {numberPrefix:'CQ', backing:{kind:'module', adapter:'cqi.backing'}}, steps comparison (stepType adapter, adapter.onEnter cqi.metrics + cqi.compare, surface 'comparison', recompute auto on event `assessment.committed` -> $self) -> prior_items (form bound to sourceBinding `prior_cqi_items`; continue -> $next guard cqi.allPriorItemsAssessed) -> narrative (problems/actions_taken/recommendations/planned_actions repeating groups; complete -> complete), terminalStates complete (success; effects cqi.itemsFromAnswers, cqi.spawnActionTasks).
  - `src/modules/portfolios/adapters.ts`: backing `portfolio.backing` (`Portfolio` + `PortfolioScope` rows), auto `portfolio.provision`, compute `portfolio.studentCount`, guards `portfolio.allScopesCommitted` and `portfolio.cqiComplete`, effects `portfolio.setSubmittedAt`, `portfolio.setApprovedAt`, `portfolio.lockScopes` (per scope `academic.lockSectionAssessment` + `recomputeSchemeStructureLock` from P13), `portfolio.freezeSnapshots`, export `portfolio.reportData`. `src/modules/cqi/adapters.ts`: backing `cqi.backing`, computes `cqi.metrics` and `cqi.compare` (strategies previous_semester|previous_year|all through `Academic.courseLineage`, explicit `{snapshotId: null, reason: 'no_data'}` rows), auto `cqi.recomputeOnCommit` (event `assessment.committed`, matched on the record's sectionOfferingIds, guard `feature.parentActive`), guard `cqi.allPriorItemsAssessed`, effects `cqi.itemsFromAnswers` (CQIItem rows with the `referencesItemId` carry-forward chain), `cqi.spawnActionTasks` (`task` records with preset `cqi_action`), `cqi.freeze`, source_binding `prior_cqi_items`, export `cqi.reportData`.
  - `src/modules/portfolios/{provision.ts (one portfolio record + child cqi record per (instructor, courseOffering) from TeachingAssignments valid in the term, scopes = that instructor's section offerings, idempotent), queries.ts (mine, by_term, not_started from `firstEditedAt IS NULL`), actions.ts, surface.tsx (`surfaces.portfolio = { stepRenderers: { assessment: AssessmentGrid, cqi_summary: CqiSummary }, detailExtras: ScopeLockBadges }`), registry.ts, reports.ts}` and `src/modules/cqi/{surface.tsx (`surfaces.cqi = { stepRenderers: { comparison: ComparisonCharts } }`, recharts), registry.ts, queries.ts, reports.ts}`.
  - Queue and cron append: `src/platform/scheduler/queues.ts` gains `portfolio.provision` (policy `singleton`), `apps/worker/src/schedules.ts` gains the cron `portfolio.provision 0 2 * * *` (tz `APP_TIMEZONE`) and the `calendar.period.changed` re-anchor subscription, and `apps/worker/src/handlers/portfolio-provision.ts` is delivered: it provisions every term whose `portfolio_submission` period has started, subscribes the deadline reminders, notifies instructors, uses the ledger key `provision:{termId}` and is also enqueueable from `/admin/jobs`.
  - Module page `src/app/(app)/d/[dept]/(modules)/portfolios/consolidated/[offeringId]/page.tsx` (course-level consolidation with incomplete-mapping and unlocked-section flags); `next.config.ts` rewrite `/d/:dept/portfolios/:path*` -> `/d/:dept/f/portfolio/:path*`.
  - Report registrations `src/modules/portfolios/reports.ts` (`portfolio` pdf|html, `consolidated_course` xlsx|pdf) and `src/modules/cqi/reports.ts` (`cqi` pdf|xlsx) on P11's registry; `prisma/seed/templates/portfolio.ts` (report.portfolio, report.cqi, report.consolidated_course, messages portfolio_submitted / portfolio_revision_requested) registered in P5's templates index - this phase is the only source of those keys; `prisma/seed/reminder-schedules.ts` gains `portfolio_7_3_1_0_overdue` (offsets -7/-3/-1/0/+1 with escalation to department_head after 3 overdue days) and is its only source; `prisma/seed/permissions.ts` gains portfolio.submit_own, portfolio.review, portfolio.approve, cqi.manage.
  - Demo seed `prisma/seed/demo/portfolios.ts`: an approved previous-term portfolio with CQI items so the carry-forward and comparison paths have data.
KEY FILES: prisma/schema/portfolio.prisma; prisma/migrations/<stamp>_p14_portfolio_cqi/migration.sql; prisma/seed/features/portfolio.ts; prisma/seed/features/cqi.ts; src/modules/portfolios/adapters.ts; src/modules/portfolios/provision.ts; src/modules/portfolios/surface.tsx; src/modules/cqi/adapters.ts; src/modules/cqi/surface.tsx; src/platform/scheduler/queues.ts; apps/worker/src/schedules.ts; apps/worker/src/handlers/portfolio-provision.ts; src/app/(app)/d/[dept]/(modules)/portfolios/consolidated/[offeringId]/page.tsx; src/modules/portfolios/reports.ts; prisma/seed/templates/portfolio.ts; prisma/seed/reminder-schedules.ts
TESTS:
  - tests/unit/cqi/compare.test.ts: previous_semester picks the last offering of the same course, previous_year follows `predecessorCourseId`, `all` lists every snapshot offering, and a missing baseline yields `{snapshotId: null, reason: 'no_data'}`.
  - tests/unit/feature/seeds.test.ts (extended): `portfolio` compiles the `prepare` StepGroup to consecutive leaf states with resolved `$next`, the `review` ParallelGroup to one compound state with two BranchSpecs, quorum 1, synthetic `review.dh_review.$done`/`$rejected` and the `review.$join`/`review.$reject` system transitions; `request_revision` inside a branch targets `review.<branch>.$rejected`; `cqi` compiles its `assessment.committed` auto action to an outbox subscription with handler key `feature.autoOnEvent:cqi:comparison:recompute`.
  - tests/unit/portfolios/provision.test.ts: the (instructor, offering) grouping and scope selection from teaching assignments, including an instructor teaching two sections of one offering.
  - tests/integration/portfolios/workflow.test.ts: submit is blocked until every scope has a committed assessment batch and the child cqi record is terminal; `mark_reviewed` enters the compound state and creates one `Task(kind=feature_step)` per branch; one branch approval satisfies quorum 1, marks the sibling SKIPPED, enters `approved`, locks only this portfolio's section offerings, freezes their snapshots and writes `approvedAt` through the effect only; a branch `request_revision` sends the record to `revision_required` and keeps the answers; DPT cannot approve by default but can after the exclusion setting is edited; two parallel transactions on the two branches serialise on the `WorkflowInstance` row.
  - tests/integration/portfolios/provision.test.ts: provisioning creates portfolio + cqi records with pinned definition versions, reminders and notifications; a second run creates nothing; `not_started` is derived from `firstEditedAt`.
  - tests/integration/cqi/chain.test.ts: `cqi.itemsFromAnswers` writes items with the prior-item reference chain, `cqi.spawnActionTasks` creates `task` records with preset `cqi_action`, and `assessment.committed` recomputes the comparison until the parent portfolio is approved (then `cqi.freeze` stops it).
  - tests/worker/portfolio-provision.test.ts: the cron handler is idempotent under `singletonKey provision:{termId}` and re-anchors after a calendar period move.
  - tests/components/portfolios/AssessmentGrid.test.tsx and tests/components/cqi/ComparisonCharts.test.tsx: per-section commit/lock badges and the comparison table render from fixtures, including the 'no data' baseline.
  - tests/e2e/p14-portfolio.spec.ts: the instructor opens the provisioned portfolio, walks course_info -> assessment (links to the P13 import) -> narrative -> CQI (assesses a carried-forward item against the seeded previous term) -> submits; DH marks reviewed, DPT requests revision, the instructor resubmits, DH approves; the section shows a lock badge, a further upload for that section is refused while the sibling section still accepts, and the portfolio PDF plus the consolidated course xlsx download.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p14_portfolio_cqi
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p14_portfolio_cqi/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p14-portfolio.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(portfolios): seeded portfolio feature with parallel quorum review and child cqi comparison chain" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Trigger 'Provision portfolios' from /admin/jobs, open the provisioned portfolio as the instructor, view the CQI comparison against the seeded previous offering, submit, then approve from one of the two parallel review lanes and confirm the other lane is skipped, the sections are locked and the consolidated report flags a still-open section.
COMMIT: feat(portfolios): seeded portfolio feature with parallel quorum review and child cqi comparison chain

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P15 — Campaign feature with add/drop, elective and preference presets
GOAL: (3 days) Seed the single `campaign` feature with its first three presets (add_drop, elective, preference) over the P8 forms/campaign core and P8's `/c/[token]` page: kind adapters, aggregation math, participation and results surfaces, offering decisions through P13's `course_offering` adapters, demand/preference reports, plus the backfill and NOT NULL tightening of `Campaign.featureRecordId`. No campaign tables, token page, aggregation core or reporting files are re-delivered.
DEPENDS: ['P14']
DELIVERABLES:
  - No new tables: `prisma/schema/campaign.prisma` (Campaign, CampaignSubject, CampaignInvitation, AggregationResult) and the anonymity CHECK/trigger ship with P8. `Campaign.featureRecordId` has been nullable since P8; this phase adds `prisma/seed/features/backfill-campaign-records.ts` (called by `prisma/seed.ts` after `seedFeatures`: one `campaign` FeatureRecord per existing Campaign, including P8's demo campaign, preset = kind, state derived from the window) and then edits `prisma/schema/campaign.prisma` to `featureRecordId String @unique` with the prisma-generated migration `constraints_p15` (`SET NOT NULL`), matching schema §10. No `gen-rls` run (no new tenant tables).
  - Seed `prisma/seed/features/campaign.ts` per builder seededFeatures: navigation {group:'academic', order:20, icon:'megaphone', visibleRoles:[department_head,deputy_head]}, scope {level:'department'}, parentSubject {subjectType:'term', relation:'parent', required:true}, record {numberPrefix:'CP', backing:{kind:'module', adapter:'campaign.backing'}, fields kind (locked by preset), title, formKey form picker filtered by preset kind, windowAnchor|opensAt/closesAt, audienceSpec audience_picker, anonymityMode, submissionRule, subjectMode, minResponsesForReport}, steps setup (form campaign_setup with per-preset override; publish -> published guards campaign.formPublished + campaign.audienceNonEmpty + campaign.windowValid, effect campaign.publish; cancel -> cancelled) -> published (wait; `open` auto field_datetime opensAt -> in_progress effect campaign.open; cancel) -> in_progress (wait, surface 'participation'; extend -> $self effect campaign.reschedule; close -> closed manual or auto field_datetime closesAt, effect campaign.close) -> closed (analyze -> analyzed with the preset aggregation effect; reopen -> in_progress) -> analyzed (surface 'results'; decide -> $self with the preset decision effect; archive -> archived), terminalStates archived (success) / cancelled (cancelled); presets add_drop, elective, preference with `permissionPrefix` = the kind, their per-step form keys and adapter overrides (evaluation and survey are appended in P16); listViews by_preset|active|results; counters campaigns_in_progress, evaluation_low_participation. Appended to `prisma/seed/features/index.ts`.
  - `src/modules/campaigns/adapters.ts` (shared core binding): backing `campaign.backing` (Campaign + CampaignSubject + CampaignInvitation rows through P8's `src/platform/campaign/*`), guards `campaign.formPublished`, `campaign.audienceNonEmpty`, `campaign.windowValid`, effects `campaign.publish` (audience resolution, hashed one-time tokens, `campaign:{id}:open|close` ScheduledJobs on P8's `campaign.open`/`campaign.close` queues, `invitations.pending` reminder subscription), `campaign.open`, `campaign.close`, `campaign.reschedule` (re-anchored on `calendar.period.changed` by cancel-by-prefix), export `campaign.export`; source_bindings `courses_in_program`, `own_enrollments`, `electives_in_campaign`.
  - Pure kind modules `src/platform/campaign/kinds/{add_drop.ts, elective.ts, preference.ts}` (option sources, aggregation specs, Borda and first-choice counting, rank matrix, capacity-aware offer list) built on P8's `src/platform/campaign/aggregation.ts`; module adapters `src/modules/campaigns/add-drop/adapters.ts` (effect `addDrop.aggregate`, action effect `addDrop.offeringDecision` calling `academic.ensureOffering`, then P13's `offering.decisionFromCampaign`, writing `Enrollment` rows with source `add_drop_decision`), `src/modules/campaigns/elective/adapters.ts` (effect `elective.borda`, action effect `elective.capacityOffer`), `src/modules/campaigns/preference/adapters.ts` (effect `preference.rankMatrix`). Seeding `ReviewedPreference` rows is NOT done here: P25's `load_cycle` feature subscribes to `campaign.closed` and runs `load.seedReviewedPreferences`.
  - Surfaces `src/modules/campaigns/surface.tsx`: `surfaces.campaign = { stepRenderers: { participation: ParticipationMonitor, results: ResultsPanel, subjects: SubjectsEditor }, detailExtras: WindowCard, createExtras: KindWizard }` plus per-preset panels in `src/modules/campaigns/{add-drop,elective,preference}/surface.tsx` (DemandDashboard + OfferingDecisionTable, RankingTable + OfferList, RankMatrix).
  - Module page: P8's `src/app/(app)/d/[dept]/(modules)/campaigns/[campaignId]/results/page.tsx` is EXTENDED (no new route) with the demand, ranking and preference analysis views and the export menu; the segment resolves either a `Campaign.id` or its `featureRecordId`. `next.config.ts` rewrite `/d/:dept/campaigns/:path*` -> `/d/:dept/f/campaign/:path*` (the module page still wins because rewrites run afterFiles).
  - Seeds: campaign forms `prisma/seed/forms/campaign-add-drop.ts`, `campaign-elective.ts`, `campaign-preference.ts` (standalone published `FormDefinition` rows, because the campaign record picks its form through a record field); `prisma/seed/templates/campaign.ts` (campaign_invitation, campaign_reminder, add_drop_request, elective_invitation, course_preference_request, report.add_drop_demand, report.elective_preference, report.course_preference) registered in P5's templates index and the only source of those keys; `prisma/seed/permissions.ts` gains campaign.manage plus the add_drop.*, elective.*, preference.* prefixed keys.
  - Report registrations `src/modules/campaigns/reports.ts` on P11's registry: `add_drop_demand` (xlsx|pdf|csv with the trend join against the previous add/drop campaign), `elective_preference` (xlsx|pdf), `course_preference` (xlsx|pdf). Demo seed `prisma/seed/demo/campaigns.ts`: an add_drop period in the current term, 20 students with invitations and four candidate elective courses.
KEY FILES: prisma/seed/features/campaign.ts; prisma/seed/features/backfill-campaign-records.ts; prisma/migrations/<stamp>_constraints_p15/migration.sql; src/modules/campaigns/adapters.ts; src/modules/campaigns/surface.tsx; src/platform/campaign/kinds/add_drop.ts; src/platform/campaign/kinds/elective.ts; src/platform/campaign/kinds/preference.ts; src/modules/campaigns/add-drop/adapters.ts; src/modules/campaigns/elective/adapters.ts; src/modules/campaigns/preference/adapters.ts; src/app/(app)/d/[dept]/(modules)/campaigns/[campaignId]/results/page.tsx; src/modules/campaigns/reports.ts; prisma/seed/forms/campaign-add-drop.ts; prisma/seed/templates/campaign.ts; prisma/seed/demo/campaigns.ts
TESTS:
  - tests/unit/campaign/kinds.test.ts: add/drop demand grouping by program/year/section, Borda and first-choice ranking, capacity-aware offer list, preference rank matrix with uncovered offerings and oversubscribed first choices honouring maxTeachingLoad.
  - tests/unit/feature/seeds.test.ts (extended): `campaign` validates and compiles once with three presets; per-preset permission keys `{prefix}.{action}` are emitted with the `campaign.manage` fallback; the `open`/`close` auto actions compile to `scheduleAutoTransition` with the `campaign:{id}:open|close` keys; `/record/fields/0` (kind) and every preset adapter pointer are locked.
  - tests/integration/campaign/preset-lifecycle.test.ts (new): publish creates one `CampaignInvitation` per (person, subject) with hash-only tokens plus the two ScheduledJob rows; moving the anchored add_drop period re-resolves the window and re-materialises jobs and reminders; open notifies (email-only for persons without a User); an identified submit copies the coarse cohort attributes; editing before close is allowed and after close refused; close expires tokens, cancels reminders and enqueues aggregation; analyze writes `AggregationResult` rows; DPT may close add_drop through the `campaign.manage` fallback.
  - tests/integration/campaign/offering-decision.test.ts: confirming demand for a catalogue course with no offering calls `ensureOffering` and drives the `course_offering` record to `confirmed`; `Enrollment` rows carry source `add_drop_decision`; cancelling notifies the affected instructors.
  - tests/integration/campaign/backfill.test.ts: the backfill creates exactly one `campaign` FeatureRecord per pre-existing Campaign (preset = kind), is a no-op on a second run, and after `constraints_p15` a Campaign cannot be inserted without a `featureRecordId`.
  - tests/worker/campaign-jobs.test.ts (extended): open sends invitations once under its singleton key, close then aggregate run in order, and a re-enqueued close is a no-op.
  - tests/components/campaigns/DemandDashboard.test.tsx: chart and table render from an aggregation fixture including suppression-free cells.
  - tests/e2e/p15-campaigns.spec.ts: DH creates an add/drop campaign anchored to the add_drop period and publishes; a student without a login opens the `/c/[token]` link from Mailpit, adds CS305, drops CS201 and edits before close; DH closes, reads the demand chart and confirms the CS305 offering; DH then runs an elective campaign with capacity (two students rank, the offer list shows the top two) and an instructor submits course preferences that appear in the DH rank matrix; `add_drop_demand.xlsx` downloads.
VERIFY:
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web npx prisma migrate dev --create-only --name constraints_p15
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p15-campaigns.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(campaigns): single seeded campaign feature with add/drop, elective and preference presets and offering decisions" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Create an elective campaign for next term with four candidate courses, publish, respond as two students from their Mailpit links, move the elective_selection period in /admin/calendar and confirm the impact preview lists the campaign and its reminders, close and read the ranking analysis; in /admin/features/campaign change the invitation template (allowed) and try to change the preset `kind` (refused with LOCK_VIOLATION).
COMMIT: feat(campaigns): single seeded campaign feature with add/drop, elective and preference presets and offering decisions

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P16 — Evaluation and survey presets with structural anonymity and k-anonymity
GOAL: (3 days) Add the anonymous `evaluation` preset and the generic `survey` preset to the existing `campaign` feature: subject derivation per evaluator group, question-bank preset forms with scoring, day-truncated timestamps, k-anonymity suppression in P8's aggregation using the single `campaign.kThreshold` setting, DH-only close/analyze/report keys and the four evaluation reports. P8's anonymity CHECK and trigger are extended, never replaced.
DEPENDS: ['P15']
DELIVERABLES:
  - Migration `p16_evaluation_anonymity` (created as an empty migration with `migrate dev --create-only`, then hand-written; no new tables, so no `gen-rls` run): `CREATE TRIGGER submission_truncate_submitted_at BEFORE INSERT OR UPDATE ON submission` and the matching trigger on `campaign_invitation` (both `date_trunc('day', submitted_at)` when the campaign's `anonymity_mode = 'anonymous'`), plus the column-guard pair that actually restricts identity edits - `REVOKE UPDATE ON campaign_invitation FROM dept_app; GRANT UPDATE (status, reminders_sent, last_reminder_at, submitted_at, expires_at) ON campaign_invitation TO dept_app;` (a bare `REVOKE UPDATE (person_id)` cannot subtract from a table-level grant) - and a comment referencing P8's `enforce_submission_anonymity()`, which already refuses respondent identity.
  - `prisma/seed/features/campaign.ts` gains the `evaluation` and `survey` presets (locked `fieldDefaults.kind` and `anonymityMode`, `permissionPrefix: 'evaluation'` so `evaluation.close`/`evaluation.analyze` stay DH-only, `formKeys.setup` per kind, adapter overrides `closed.analyze.effect` = evaluation.analyze | survey.aggregate, `navLabel` 'Evaluations' | 'Surveys'); the shared lifecycle is untouched, which is what proves the preset mechanism. Editing the locked `/presets/*` subtree produces a `seed-upgrade` DRAFT, published immediately under `SEED_AUTO_PUBLISH=1`.
  - `src/modules/campaigns/evaluation/adapters.ts`: effect `evaluation.deriveSubjects` (one `CampaignSubject` per (instructor, sectionOffering) for the students group with that section's enrolled audience; one per instructor for the colleagues group excluding self; one identified subject for the dh group), locked effect `evaluation.anonymousSubmit` (the structural-anonymity write path: no respondent, no invitation link, coarse cohort only), effect `evaluation.normaliseScores` (0-100 plus weighted category scores), effect `evaluation.kAnonymity`, export `evaluation.export`; `src/modules/campaigns/survey/adapters.ts`: effect `survey.aggregate`.
  - `src/platform/campaign/kinds/{evaluation.ts, survey.ts}` (pure: evaluator-group matrices, release policy after_close|after_analyze|manual, free-text policy withheld_below_k|always|never) and an extension of P8's `src/platform/campaign/aggregation.ts` with the k-anonymity pass reading `SystemSetting('campaign.kThreshold')` (seeded = 5 in P1, the single key used by P8, P16 and every report): cells with n < k are written `suppressed = true` and free text is withheld.
  - `src/modules/campaigns/evaluation/{results.ts (individual per subject, department summary, question-level, historical compare across campaigns by subject person), anonymity.ts (the adversarial join helpers the integration suite uses), surface.tsx (extends `surfaces.campaign.stepRenderers.results` with the evaluation panels: suppression badges, response rate per evaluator group), reports.ts}`.
  - Seeds: `prisma/seed/forms/evaluation-presets.ts` (published `FormDefinition` rows for the teaching, assessment, communication and professionalism categories: likert 1-5 with `scoreWeight` plus free text); `prisma/seed/templates/evaluation.ts` (evaluation_invitation, evaluation_reminder, report.evaluation_individual, report.evaluation_department, report.evaluation_question_level, report.evaluation_historical), the only source of those keys; `prisma/seed/permissions.ts` gains evaluation.create/manage/close/analyze/view_subject_report/participate with close, analyze and view_subject_report added to the default `rbac.manageExcludedPermissions` list.
  - Report registrations `src/modules/campaigns/evaluation/reports.ts` on P11's registry: `evaluation_individual` (pdf|xlsx, requiredPermission evaluation.view_subject_report), `evaluation_department` (pdf|xlsx), `evaluation_question_level` (xlsx|csv), `evaluation_historical` (pdf) - suppression is applied inside each data source, never in the template.
  - Module page `src/app/(app)/d/[dept]/(modules)/campaigns/my-results/page.tsx` (an instructor's own released evaluation results per release policy; a static segment beside P8's `[campaignId]`, so no route collision); the extended results page from P15 gains the evaluation tabs.
KEY FILES: prisma/migrations/<stamp>_p16_evaluation_anonymity/migration.sql; prisma/seed/features/campaign.ts; src/modules/campaigns/evaluation/adapters.ts; src/modules/campaigns/evaluation/results.ts; src/modules/campaigns/evaluation/anonymity.ts; src/modules/campaigns/evaluation/reports.ts; src/modules/campaigns/survey/adapters.ts; src/platform/campaign/kinds/evaluation.ts; src/platform/campaign/aggregation.ts; prisma/seed/forms/evaluation-presets.ts; prisma/seed/templates/evaluation.ts; src/app/(app)/d/[dept]/(modules)/campaigns/my-results/page.tsx
TESTS:
  - tests/unit/campaign/k-anonymity.test.ts: cells with n < k suppressed, free text withheld below k, the threshold read from `campaign.kThreshold`, score normalisation and weighted category scores.
  - tests/unit/campaign/evaluation-subjects.test.ts: subjects per (instructor, section offering) for students, per instructor for colleagues excluding self, one identified dh subject.
  - tests/unit/feature/seeds.test.ts (extended): the campaign seed now compiles five presets; `evaluation` emits evaluation.close/analyze keys while add_drop falls back to campaign.manage; `anonymityMode` is locked per preset.
  - tests/integration/campaign/anonymity.test.ts (extended, adversarial): an attempt to store a respondent or invitation id on an anonymous submission raises from P8's trigger; the invitation is marked submitted in a separate statement; a join across every FK column of submission/answer/audit recovers no respondent; `submittedAt` is day-truncated on both rows; the AuditEvent has a NULL actor; insertion order does not correlate with invitation order; a direct UPDATE of `campaign_invitation.person_id` is refused by the column grant.
  - tests/integration/campaign/evaluation-permissions.test.ts: DPT can publish but not close, analyze or open a subject report; DH can; an instructor sees own results only once the release policy is met; a subject with two responses is suppressed.
  - tests/integration/campaign/evaluation-results.test.ts: individual, department and question-level aggregates over a 12-response fixture; the historical compare joins two campaigns by subject person.
  - tests/e2e/p16-evaluation.spec.ts: DH builds an evaluation from the teaching preset for one instructor with students and colleagues and publishes; three students and two colleagues respond through Mailpit `/c/[token]` links; DPT sees no Close action; DH closes and analyzes; the colleague cell is suppressed at n=2 and becomes visible after lowering `campaign.kThreshold` to 3 in /admin/settings; `evaluation_individual.pdf` downloads and the instructor sees the released results.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p16_evaluation_anonymity
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p16-evaluation.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(evaluations): anonymous evaluation and survey campaign presets with structural anonymity, k-anonymity and reports" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Publish the pending 'system update' draft in /admin/features/campaign (the evaluation and survey presets), run an evaluation for one instructor with three student respondents and two colleagues, confirm suppression as DH until `campaign.kThreshold` is lowered in /admin/settings, confirm Close and Analyze are hidden for DPT, and check in psql that the submission rows carry no respondent id and day-granular timestamps.
COMMIT: feat(evaluations): anonymous evaluation and survey campaign presets with structural anonymity, k-anonymity and reports

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P17 — Meetings and minutes (dynamic per-participant circulation)
GOAL: (3 days) Seed the `meeting` feature: room hard-conflict guard and attendee soft blocks over P10's availability ledger, agenda, attendance, decisions and action-item tasks, minutes rendered into a versioned Document, a dynamic parallel circulation with one branch per participant (completion all, onAnyReject changes_requested), DH approval that locks the document version through P6's `document_version_lock()`, previous-decisions query and the minutes PDF.
DEPENDS: ['P16']
DELIVERABLES:
  - Schema delta `prisma/schema/meeting.prisma`: `Meeting`, `AgendaItem`, `Decision`, `MeetingAttendance` exactly as schema §20 (`Meeting.featureRecordId @unique`, `participantGroupId FK Group`, `REF(context)?`, `minutesDocumentId?`, `nextMeetingId` self relation `MeetingChain`); `Appointment` is added to the same file in P18. Migration `p17_meeting`: `migrate dev --create-only`, `gen-rls.ts --append` (four TENANT tables), `migrate dev`, `prisma generate`.
  - Seed `prisma/seed/features/meeting.ts` per builder seededFeatures: navigation {group:'operations', order:40, icon:'presentation', visibleRoles:[department_head,deputy_head,instructor,committee_member]}, scope {level:'department'}, parentSubject {subjectType:'committee', relation:'context', required:false, inheritPermissions:true}, record {numberPrefix:'MT', backing:{kind:'module', adapter:'meeting.backing'}, fields title, startsAt, endsAt, room resource_picker (sourceBinding resources_of_kind, bindingArgs {kind:'meeting_room'}), participants audience_picker, agenda repeating_group{title, presenter person_picker, attachments file}, minutesTemplateKey}, steps scheduled (assignee creator; guard meeting.roomFree; onEnter effects meeting.registerBlocks + notify meeting_notification, reminders `meeting_-1d_-1h`; record_held -> held; cancel -> cancelled) -> held (surface 'attendance_and_minutes'; generate_minutes -> minutes_draft guards meeting.attendanceRecorded + meeting.actionItemsValid, effects meeting.renderMinutes + meeting.createActionItemTasks) -> minutes_draft (circulate -> circulation; regenerate -> $self) -> ParallelGroup `circulation` { branches mode dynamic, perPerson relationship participant, branch review = step participant_review (assignee the branch person; onEnter notify minutes_circulated; acknowledge -> $done; request_changes -> reject requiredComment); completion all; onComplete approval; onAnyReject changes_requested } -> changes_requested (assignee creator; regenerate -> minutes_draft) -> approval (assignee role department_head | relationship parent_chair; approve -> minutes_approved effect meeting.lockVersion; request_changes -> changes_requested requiredComment), terminalStates minutes_approved (success; notify participants) and cancelled (cancelled; effect meeting.removeBlocks); explicit permissions meeting.manage and minutes.approve (DPT-excluded); listViews upcoming|mine|awaiting_minutes; counters meetings_awaiting_minutes, minutes_to_review. Appended to `prisma/seed/features/index.ts`.
  - `src/modules/meetings/adapters.ts`: backing `meeting.backing` (Meeting rows + participant `Group(kind=meeting)` from the audience spec), guard `meeting.roomFree` (P10's `availability.checkConflicts`, hard, for the resource), effects `meeting.registerBlocks` (room hard, attendees soft) and `meeting.removeBlocks`, guards `meeting.attendanceRecorded` and `meeting.actionItemsValid` (every action item has an owner and a deadline), effect `meeting.renderMinutes` (renders the ten-section `meeting_minutes` Template through P11's document renderer into a new `DocumentVersion` linked with linkRole `minutes`), effect `meeting.createActionItemTasks` (`task` records with preset `action_item`, parent = the meeting - this registration also closes the `meeting` parent pointer the P9 task preset declares), effect `meeting.lockVersion` (P6 `document_version_lock()`), source_binding `participants_of_parent`, export `meeting.minutesPdf`.
  - `src/modules/meetings/{registry.ts (SubjectRegistry 'meeting': contextOf -> {groupId, committeeId?}, relationships organizer|participant, variables meeting_title/date/location/agenda_items/decisions/action_items), service.ts (agenda CRUD, attendance, decisions -> action-item task creation, `previousDecisions(contextRef, unresolvedOnly)`, next-meeting linkage), minutes-context.ts (the ten fixed sections including the approval payload read from the transition log), surface.tsx (`surfaces.meeting = { stepRenderers: { attendance_and_minutes: AttendanceAndMinutes, previous_decisions: PreviousDecisions }, detailExtras: RoomConflictBadge + MinutesVersions }`), queries.ts, actions.ts, reports.ts}`.
  - `src/modules/resources/adapters.ts` is created here with the single `source_binding resources_of_kind` adapter (the `Resource` table ships with the foundation academic registry; P23 extends the same file with the asset adapters) so the meeting seed's room picker passes `BINDING_ARGS` and `ADAPTER_EXISTS`.
  - Seeds: `prisma/seed/templates/meeting.ts` (meeting_notification, meeting_reminder, minutes_circulated, minutes_approved (kind message) and `meeting_minutes` (kind document: the ten sections with the `{{#action_items}}` loop, declared variables only)) registered in P5's templates index and the only source of those keys; `prisma/seed/reminder-schedules.ts` gains `meeting_-1d_-1h` and is its only source; `prisma/seed/permissions.ts` gains meeting.manage and minutes.approve.
  - Routing and feeds: `next.config.ts` rewrite `/d/:dept/meetings/:path*` -> `/d/:dept/f/meeting/:path*`; P5's `src/app/(app)/d/[dept]/upcoming/page.tsx` feed gains the meeting layer through `Scheduler.upcoming` (no new calendar route is invented). Report registration `minutes` (pdf|html) in `src/modules/meetings/reports.ts` on P11's registry.
KEY FILES: prisma/schema/meeting.prisma; prisma/migrations/<stamp>_p17_meeting/migration.sql; prisma/seed/features/meeting.ts; src/modules/meetings/adapters.ts; src/modules/meetings/minutes-context.ts; src/modules/meetings/surface.tsx; src/modules/meetings/service.ts; src/modules/meetings/registry.ts; src/modules/meetings/reports.ts; src/modules/resources/adapters.ts; prisma/seed/templates/meeting.ts; prisma/seed/reminder-schedules.ts; src/app/(app)/d/[dept]/upcoming/page.tsx
TESTS:
  - tests/unit/meetings/minutes-context.test.ts: all ten sections assemble from a fixture meeting including the approval payload and empty-state rows, and the template renders with no unknown variables (mustache-safe).
  - tests/unit/feature/seeds.test.ts (extended): `meeting` compiles the `circulation` group to one compound state with a single `$person` BranchSpec, `dynamic: true`, completion `all` and `onReject` = changes_requested; the branch `request_changes` action targets `circulation.$person.$rejected`.
  - tests/integration/meetings/workflow.test.ts: scheduling into a room with an overlapping hard block fails `meeting.roomFree`; scheduling registers the room hard block, attendee soft blocks and the two reminders; `generate_minutes` is blocked without attendance or with an action item missing an owner or deadline; decisions create `task` records with preset `action_item`; `circulate` instantiates one branch and one `Task(kind=feature_step)` per resolved participant; one `request_changes` moves the record to `changes_requested` and marks the sibling branches SKIPPED; approval locks the `DocumentVersion` (a later `addVersion` is refused), records the approval in the transition payload and writes the derived cache through the effect only; cancel removes the blocks.
  - tests/integration/meetings/previous-decisions.test.ts: decisions of the previous meeting in the same committee context are returned with their action-task state.
  - tests/components/meetings/AttendanceAndMinutes.test.tsx: attendance toggles, decision rows and action-item validation errors render from a fixture.
  - tests/worker/report-pdf.test.ts (extended): the `minutes` report renders a locked version to PDF and the extracted text contains a decision.
  - tests/e2e/p17-meetings.spec.ts: DH schedules a committee meeting in Room 101 (a second meeting at the same time is refused with the conflict badge), adds the agenda, records attendance and two decisions with owners and deadlines, generates and circulates the minutes; one participant requests changes; DH regenerates, re-circulates, every participant acknowledges, DH approves; the minutes PDF downloads, the version is locked, and the follow-up meeting shows the previous decisions with their task status.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p17_meeting
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p17_meeting/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p17-meetings.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(meetings): seeded meeting feature with agenda, decisions, action items and dynamic per-participant minutes circulation" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Schedule a committee meeting in Room 101, attempt a clashing second booking and see the hard-conflict refusal, record attendance and two decisions with owners, generate and circulate the minutes, request changes as a participant, regenerate, acknowledge as every participant and approve as DH; open the locked minutes version, the previous-decisions panel in a follow-up meeting and the meeting entries in /d/cs/upcoming.
COMMIT: feat(meetings): seeded meeting feature with agenda, decisions, action items and dynamic per-participant minutes circulation

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P18 — Appointments and case intake wiring
GOAL: (2 days) Seed the `appointment` feature with the host slot guard, hard/soft availability blocks, confirmation and reminder mail, the reschedule exchange and escalation to a case, plus the public `/a/[hostSlug]` intake and signed status link; finish wiring the P9 `case` feature's intake sources (appointment, thread, committee_report) and extend P10's availability page with the appointments policy tab. P10's person/resource EXCLUDE constraint and free-slot engine are reused, never re-added.
DEPENDS: ['P17']
DELIVERABLES:
  - Schema delta `prisma/schema/meeting.prisma`: `Appointment { Id, TENANT, hostPersonId + host Person @relation("Host"), requesterPersonId + requester Person @relation("Requester") (NOT NULL per schema §20 - the public intake creates a `Person(type=external)` first), requestSubmissionId String @unique, confirmedStartAt?, durationMinutes?, featureRecordId String @unique, outcomeNote?, followUpCaseTaskId?, Ts, @@index([hostPersonId, confirmedStartAt]) }`. No public token column: the status link is a `jose` HS256 JWT signed with BETTER_AUTH_SECRET. Migration `p18_appointment`: `migrate dev --create-only`, `gen-rls.ts --append` (one TENANT table), `migrate dev`, `prisma generate`.
  - Seed `prisma/seed/features/appointment.ts` per builder seededFeatures: navigation {group:'people', order:20, icon:'calendar-clock', visibleRoles:[department_head,deputy_head,instructor,student_rep]}, scope {level:'department'}, parentSubject {subjectType:'person', relation:'host', required:true, inheritPermissions:false, listUnderParent:true, createFromParent:true}, record {numberPrefix:'AP', backing:{kind:'module', adapter:'appointment.backing'}, fields hostPersonId person_picker (sourceBinding staff_in_department, locked), requesterPersonId optional, confirmedStartAt, durationMinutes, proposedStartAt, outcomeNote}, steps requested (inline form requester_name, requester_role, contact, reason, preferred_times repeating{datetime}; slot documents; surface 'slot_picker'; assignee record_field hostPersonId; onEnter notify appointment_requested; confirm -> confirmed requiredFields confirmedStartAt+durationMinutes guard appointment.hostSlotFree; reject -> rejected requiredComment; cancel -> cancelled) -> confirmed (onEnter effects appointment.registerBlocks + notify appointment_confirmation, reminders `appointment_-1d_-2h`; complete -> completed with optional effect appointment.escalateToCase; no_show -> no_show; reschedule -> rescheduled requiredFields proposedStartAt; cancel -> cancelled effect appointment.removeBlocks) -> rescheduled (assignee relationship requester; accept -> confirmed; decline -> cancelled), terminalStates completed (success), rejected (rejected), no_show (cancelled), cancelled (cancelled); listViews host_agenda|my_requests|pending; counters pending_requests, today_appointments. Appended to `prisma/seed/features/index.ts`.
  - `src/modules/appointments/adapters.ts`: backing `appointment.backing`, backing `appointment.publicIntake` (creates `Person(type=external)` + `DepartmentPerson`, the request Submission and the record, all inside `withTenantTx` resolved from the host slug), guard `appointment.hostSlotFree` (P10's `availability.checkConflicts` hard for the host; requester soft overlaps returned as warnings), compute `appointment.freeSlots` (P10's `src/platform/availability/free-slots.ts` with `AvailabilityPolicy(purpose=appointments)`), effects `appointment.registerBlocks` / `appointment.removeBlocks`, effect `appointment.escalateToCase` (a `case` record whose parent/origin is this appointment), source_binding `staff_in_department`.
  - `src/modules/appointments/{registry.ts (SubjectRegistry 'appointment': relationships host|requester, variables appointment_reason/requester_name/requester_role/contact/confirmed_start), service.ts (request, freeSlots, proposeReschedule/accept, hostAgenda), public-link.ts (`verifyPublicLink(token)` -> {featureRecordId, departmentId} then `withTenantTx`), surface.tsx (`surfaces.appointment = { stepRenderers: { slot_picker: SlotPicker, host_agenda: HostAgenda }, detailExtras: RescheduleExchange + EscalateToCase }`), queries.ts, actions.ts}`.
  - Public routes `src/app/a/[hostSlug]/page.tsx` (request form gated by `SystemSetting('appointments.publicRequests')`, already excluded from the `src/proxy.ts` matcher) and `src/app/a/[hostSlug]/[token]/page.tsx` (read-only status with cancel / accept-reschedule); both audited with a NULL actor. Rate limiting for these two routes is applied in P28 with P11's `src/lib/rate-limit.ts`.
  - Case intake completion: the P9 `case` seed's `allowedTypes` are exercised end to end - `appointment.escalateToCase` (this phase), `committee.escalateIssueToCase` (P12) and `Thread.escalate` (P9's thread service) all create `case` records with the correct origin; nothing in the `case` definition and none of its `case.*` adapters or its `case_assignment` template (P9) are re-seeded here.
  - Module page: P10's `src/app/(app)/d/[dept]/(modules)/availability/page.tsx` is EXTENDED (no new route) with the actor's `AvailabilityPolicy` editor for purpose `appointments` (weekly windows, slot minutes, max per period, breaks, blackouts); P21 adds the invigilation tab to the same file. `next.config.ts` rewrites `/d/:dept/appointments/:path*` -> `/d/:dept/f/appointment/:path*` and `/d/:dept/cases/:path*` -> `/d/:dept/f/case/:path*`.
  - Seeds: `prisma/seed/templates/appointment.ts` (appointment_requested, appointment_confirmation, appointment_reschedule, appointment_reminder), the only source of those keys; `prisma/seed/reminder-schedules.ts` gains `appointment_-1d_-2h` and is its only source; `prisma/seed/permissions.ts` gains appointment.manage and case.manage.
KEY FILES: prisma/schema/meeting.prisma; prisma/migrations/<stamp>_p18_appointment/migration.sql; prisma/seed/features/appointment.ts; src/modules/appointments/adapters.ts; src/modules/appointments/service.ts; src/modules/appointments/public-link.ts; src/modules/appointments/surface.tsx; src/app/a/[hostSlug]/page.tsx; src/app/a/[hostSlug]/[token]/page.tsx; src/app/(app)/d/[dept]/(modules)/availability/page.tsx; prisma/seed/templates/appointment.ts; prisma/seed/reminder-schedules.ts
TESTS:
  - tests/unit/appointments/free-slots.test.ts: slot generation from a weekly policy with breaks, blackouts, max-per-period and an existing confirmed appointment.
  - tests/unit/feature/seeds.test.ts (extended): `appointment` validates and compiles; `ASSIGNEE_RESOLVABLE` accepts the `record_field hostPersonId` assignee; the reschedule loop and the four terminals are reachable.
  - tests/integration/appointments/confirm.test.ts: confirm fails when the host has an overlapping hard block and P10's EXCLUDE constraint refuses a direct overlapping person hard block; confirm registers host hard + requester soft blocks and the two ScheduledJob reminder rows; a requester soft overlap produces a warning but succeeds; reschedule re-registers and cancel removes the blocks; reject requires a comment; an external requester through the public intake gets `Person(type=external)`, a `DepartmentPerson` row and an email-only confirmation.
  - tests/integration/cases/intake.test.ts: cases created from an appointment, a thread escalation and a committee report all carry the right origin parent; the interval nudge is subscribed on assignment and cancelled on resolve; the requester at level `limited` can read, comment and reopen but not assign; `case.autoClose` fires once after `SystemSetting('case.autoCloseDays')`.
  - tests/integration/appointments/public-links.test.ts (new): the signed status token resolves to the right department and record, a tampered or expired token is refused, and every public write is audited with a NULL actor.
  - tests/worker/feature/auto-transition.test.ts (extended): the resolved case auto-closes exactly once and an `expectedState` mismatch is a no-op.
  - tests/e2e/p18-appointments-cases.spec.ts: DH sets Tue/Thu availability with a lunch break; an instructor requests Tuesday 10:00 and DH confirms (confirmation mail in Mailpit, the slot disappears from the picker); DH completes the appointment and escalates a follow-up case, assigns it to DPT who waits then resolves it; an external visitor books through `/a/[hostSlug]`, receives the mail and opens the signed status page; the reopened case appears in the DH case list.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p18_appointment
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p18_appointment/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p18-appointments-cases.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(appointments): seeded appointment feature with availability guards, public intake and case escalation" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: As DH set availability windows and a blackout day on the appointments tab of /d/cs/availability; as an instructor request an appointment, confirm it as DH and check the mail; try the same slot again and confirm it is gone; escalate a follow-up case from the completed appointment and walk it to Resolved; in /admin/features/case confirm the nudge adapter is locked while the nudge interval and notification template stay editable.
COMMIT: feat(appointments): seeded appointment feature with availability guards, public intake and case escalation

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P19 — Milestone 1 close: role dashboards, module projections, regression and v0.1.0
GOAL: (2 days) Register the milestone-1 module projections as `projection`-hook adapters on P11's projection registry, fill in the per-role layouts in P11's `src/platform/dashboard/layouts.ts` and dashboard route, add the unresolved-issues report, run the whole P12-P19 journey suite against the production build, write README/CHANGELOG and tag v0.1.0 inside the container. No dashboard platform file, widget catalogue or route is re-delivered.
DEPENDS: ['P18']
DELIVERABLES:
  - No schema change and no new platform file: `DashboardProjection` (`prisma/schema/dashboard.prisma`), `src/platform/dashboard/projections/registry.ts`, `src/platform/dashboard/widgets.ts`, `src/platform/dashboard/layouts.ts`, the `outbox.dispatch` projection subscriber and the route `src/app/(app)/d/[dept]/dashboard/page.tsx` all ship with P11; this phase only registers projections and fills the layouts.
  - Projection registrations as `projection`-hook adapters in the owning modules (the only implementation of each projection): `case.openIssues` in `src/modules/cases/adapters.ts` (non-terminal case and, from P26, student_issue records plus open conversation threads, keyed by section and responsible), `portfolio.portfolioStatus` in `src/modules/portfolios/adapters.ts` (per term, not_started from `firstEditedAt`), `campaign.campaignProgress` in `src/modules/campaigns/adapters.ts` (invited/opened/submitted per campaign). Each declares the `DomainEvent` names it consumes so P11's subscriber routes them with `EventHandlerReceipt` idempotency; `staff.staffActivity` follows in P24.
  - `src/platform/dashboard/layouts.ts` (P11's file) gains the six per-role layouts (department_head, deputy_head, instructor, committee_member, student_rep, student) composed from P11's widget catalogue plus `featureCounters(actor, dept)`; `src/app/(app)/d/[dept]/dashboard/page.tsx` (P11) gains the role switcher for multi-role users and links every widget to its filtered list view. The announcements widget renders an empty state until P26 and the `staffActivities` widget stays hidden until P24 registers its projection.
  - Report registration: `unresolved_issues` (pdf|xlsx by section and responsible, read from the `openIssues` projection) in `src/modules/cases/reports.ts` on P11's registry, with `report.unresolved_issues` added to `prisma/seed/templates/case.ts` (the P9 case template file). `department_activity` is P11's registration and is not registered again. Projection and search rebuilds are exposed on the existing `/admin/jobs` page (no new admin route).
  - `playwright.config.ts` gains the project `milestone-1` (`dependencies: ['setup']`, the p12-p19 specs in order against one seeded `dept_e2e` database) beside the existing `setup` and `chromium` projects; `tests/e2e/p19-dashboards.spec.ts`; `README.md` (Docker-only workflow, service map, command cheat sheet, how to build a feature in the wizard, how to register an adapter and a surface); `CHANGELOG.md` for v0.1.0.
  - Milestone hygiene: `docker compose -f compose.prod.yaml build web worker` is part of this phase's verification (guard rail 5), and the phase ends with the commit plus the in-container annotated tag `v0.1.0`.
KEY FILES: src/platform/dashboard/layouts.ts; src/app/(app)/d/[dept]/dashboard/page.tsx; src/modules/cases/adapters.ts; src/modules/cases/reports.ts; src/modules/portfolios/adapters.ts; src/modules/campaigns/adapters.ts; prisma/seed/templates/case.ts; playwright.config.ts; tests/e2e/p19-dashboards.spec.ts; README.md; CHANGELOG.md
TESTS:
  - tests/unit/dashboard/module-projections.test.ts: openIssues increments on case open and decrements on resolve/close and thread close; portfolioStatus derives not_started from `firstEditedAt`; campaignProgress ratios.
  - tests/unit/dashboard/layouts.test.ts: each role layout contains only widgets whose required permission the role can hold, and multi-role actors get the union without duplicates.
  - tests/integration/dashboard/module-projections.test.ts (new): after a scripted event sequence each incrementally maintained projection equals a full rebuild for every key, a replayed event is ignored through `EventHandlerReceipt`, and widgets are filtered by `can()` (an instructor never receives evaluationStatus; a student rep receives only own cases).
  - tests/integration/dashboard/counters.test.ts: every dashboard count matches a direct query and `featureCounters` matches the grouped count over `(department_id, definition_id, current_state_key)`.
  - tests/worker/outbox-dispatch.test.ts (extended): a module projection event dispatched through pg-boss updates its row exactly once under retry.
  - tests/e2e/p19-dashboards.spec.ts: every DH/DPT, instructor, committee-chair and student-rep widget count matches the seeded state and navigates to its list; 'Rebuild openIssues' from /admin/jobs leaves the counts unchanged; the unresolved-issues PDF downloads.
  - Full milestone regression: the `milestone-1` Playwright project runs tests/e2e/p12..p19 in order against the production build.
VERIFY:
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test --project milestone-1
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(milestone-1): role dashboards, module projections, unresolved-issues report and full journey regression" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
  $ docker compose run --rm web git tag -a v0.1.0 -m "Milestone 1: foundation, feature builder, committees, assessment, portfolios/CQI, campaigns, evaluations, meetings, appointments and cases"
MANUAL: Log in as each seeded role and walk the dashboard at /d/cs/dashboard: every widget count matches its list page; rebuild the openIssues projection from /admin/jobs and confirm the counts are unchanged; generate the unresolved-issues PDF; confirm `docker compose run --rm web git tag` lists v0.1.0.
COMMIT: feat(milestone-1): role dashboards, module projections, unresolved-issues report and full journey regression

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P20 — Annual plan, planned activities and quarterly reports
GOAL: (3 days) Seed the three planning features (`annual_plan`, task-backed `planned_activity` with its stored Delayed state, `quarterly_report`) on the planning tables, derive quarters from `AcademicYear.quarterBoundariesJson`, generate quarter snapshots on P11's existing `report.generate` queue with narrative-preserving regeneration, and render the approved quarterly report as a locked Document version.
DEPENDS: ['P19']
DELIVERABLES:
  - Schema delta `prisma/schema/planning.prisma`: `AnnualPlan { Id, TENANT, academicYearId FK AcademicYear, title, ownerPersonId FK Person, featureRecordId String @unique, approvedAt?, @@unique([departmentId, academicYearId]) }` and `QuarterlyReport { Id, TENANT, annualPlanId FK AnnualPlan, quarter Int, computedSnapshotJson, narrativeSubmissionId? @unique, documentId?, featureRecordId String @unique, generatedAt?, generatedBy?, @@unique([annualPlanId, quarter]) }` per schema §20; `prisma/schema/workitem.prisma` completes `PlannedActivity.annualPlanId` (schema §8 declares it NOT NULL, but P7 ships the extension without it because `AnnualPlan` does not exist yet). Migration `p20_planning` adds the two tables and the NOT NULL FK on the still-empty `planned_activity` table (no backfill), then `gen-rls.ts --append` (three TENANT tables), `migrate dev`, `prisma generate`.
  - Seeds `prisma/seed/features/{annual_plan.ts, planned_activity.ts, quarterly_report.ts}` per builder seededFeatures, appended to `prisma/seed/features/index.ts` in that order. annual_plan: navigation {group:'planning', order:10, icon:'calendar-range', visibleRoles:[department_head,deputy_head]}, parentSubject academic_year, record {numberPrefix:'AP', backing:{kind:'module', adapter:'annual_plan.backing'}}, steps draft -> submitted -> approved (effect annual_plan.setApprovedAt) | return -> draft requiredComment, approved -activate-> active (effect annual_plan.activateActivities), active (wait, surface 'plan_board'; close -> closed guard annual_plan.allQuartersPublishedOrWaived), terminal closed (success), permissions plan.manage / plan.approve, counters plan_pending_approval, delayed_activities. planned_activity: navigation {group:'planning', order:11, icon:'list-checks'}, parentSubject {subjectType:'feature_record', featureKey:'annual_plan'}, record {numberPrefix:'PA', backing:{kind:'task', taskKind:'planned_activity', extension:'planned_activity'}, fields name, objective, responsible person_picker|group_picker, expectedOutput, plannedStart, plannedEnd, performanceIndicator, requiredResources, budgetAmount, challengeNote, correctiveActions}, steps not_started (assignee record_field responsible; deadline relative 0d from plannedEnd; reminders default_7_3_1_0_overdue; start -> in_progress, also auto event `annual_plan.activated`; delay -> delayed auto on deadline or manual with requiredFields challengeNote; cancel) -> in_progress (complete -> completed with the inline progress form; delay; cancel) -> delayed (resume -> in_progress; complete -> completed requiredFields correctiveActions), terminals completed (success) / cancelled, listViews by_plan|mine|by_quarter. quarterly_report: navigation {group:'planning', order:12, icon:'file-bar-chart'}, parentSubject annual_plan, record {numberPrefix:'QR', field quarter (locked, unique per plan)}, steps generated (stepType adapter, adapter.onEnter quarterly.generateSnapshot + quarterly.renderDocument; inline narrative form; review -> under_review; regenerate -> $self; waive -> waived) -> under_review (approve -> approved effects quarterly.renderFinal + quarterly.lockVersion; regenerate -> generated; waive) -> approved (publish -> published effect quarterly.notifyAudience), terminals published (success) / waived (cancelled).
  - `src/modules/planning/adapters.ts`: backings `annual_plan.backing`, `quarterly_report.backing` and `planned_activity.backing` (Task kind=planned_activity + the `PlannedActivity` extension row), effects `annual_plan.setApprovedAt`, `annual_plan.activateActivities` (starts every child activity record and its reminders), guard `annual_plan.allQuartersPublishedOrWaived`, compute `planning.quarterOf`, auto `activity.autoDelay` (a `workflow.auto_transition` job at plannedEnd), auto `activity.startOnPlanActivate` (event `annual_plan.activated`), compute `activity.quarterClassification`, auto `planning.scheduleQuarterlyGeneration` (a `report.generate`-backed job at each quarter end that creates the missing `quarterly_report` record; P5's `calendar.autotransition` cron is the catch-up path), effects `quarterly.generateSnapshot`, `quarterly.renderDocument`, `quarterly.mergeNarrative`, `quarterly.renderFinal`, `quarterly.lockVersion` (P6's `document_version_lock()`), `quarterly.notifyAudience`, exports `annual_plan.reportData` and `quarterly_report.pdf`. No new queue: generation runs on P11's `report.generate` / `apps/worker/src/handlers/report-generate.ts`.
  - `src/modules/planning/{registry.ts, service.ts, quarters.ts (classification planned|completed|in_progress|delayed|not_started per activity per quarter from the record state and the planned dates), snapshot.ts (quarter snapshot: activities by classification, achievements/challenges/corrective actions from the progress answers, KPI and budget totals), surface.tsx (`surfaces.annual_plan = { stepRenderers: { plan_board: PlanBoard } }`, `surfaces.quarterly_report = { detailExtras: QuarterTabs + VersionList }`), queries.ts, actions.ts, reports.ts}`.
  - Seeds: `prisma/seed/templates/planning.ts` (document templates `quarterly_report` (planned/completed/in progress/delayed/not started/achievements/challenges/corrective actions/KPIs/budget) and `annual_report`, message `activity_delayed`, report templates `report.quarterly_activity` and `report.annual_activity`), registered in P5's templates index and the only source of those keys; `prisma/seed/permissions.ts` gains plan.manage and plan.approve (plan.approve in the DPT exclusion list).
  - Routing and reports: `next.config.ts` rewrites `/d/:dept/plans/:path*` -> `/d/:dept/f/annual_plan/:path*` and `/d/:dept/activities/:path*` -> `/d/:dept/f/planned_activity/:path*`; report registrations `quarterly_activity` (pdf|xlsx) and `annual_activity` (pdf) in `src/modules/planning/reports.ts` on P11's registry.
KEY FILES: prisma/schema/planning.prisma; prisma/schema/workitem.prisma; prisma/migrations/<stamp>_p20_planning/migration.sql; prisma/seed/features/annual_plan.ts; prisma/seed/features/planned_activity.ts; prisma/seed/features/quarterly_report.ts; src/modules/planning/adapters.ts; src/modules/planning/quarters.ts; src/modules/planning/snapshot.ts; src/modules/planning/surface.tsx; src/modules/planning/reports.ts; prisma/seed/templates/planning.ts; next.config.ts
TESTS:
  - tests/unit/planning/quarters.test.ts: `quarterOf` against the stored boundaries; the classification for every combination of record state and planned dates; an activity spanning two quarters appears in both.
  - tests/unit/planning/snapshot.test.ts: snapshot totals, achievements/challenges/corrective actions extracted from the progress answers, KPI and budget sums.
  - tests/unit/feature/seeds.test.ts (extended): the three planning seeds validate and compile; `planned_activity` compiles with `createTask:false` task templates because it is task-backed, and `delayed` is a stored state with the auto action attached.
  - tests/integration/planning/plan.test.ts: activate starts every child activity record and its reminders; the auto-transition delays an activity whose plannedEnd has passed; completing from delayed requires correctiveActions; close is blocked until all four quarters are published or waived.
  - tests/integration/planning/quarterly.test.ts: generate writes the snapshot, the narrative Submission and DocumentVersion 1; regenerate replaces the snapshot, keeps the narrative through `quarterly.mergeNarrative` and adds version 2; approve renders the final version and locks it; waive requires a comment; the scheduled generation creates each missing quarter exactly once.
  - tests/worker/report-pdf.test.ts (extended): the quarterly generation job is idempotent across two runs under its `report:{key}:{sha256(params)}:{format}` singleton key and the rendered PDF contains the quarter heading.
  - tests/e2e/p20-annual-plan.spec.ts: DPT drafts the plan with three activities and submits; DH approves and activates; the responsible instructor completes one activity through its progress form; another is auto-delayed after its planned end passes and the sweep runs from /admin/jobs; DH generates Q1, adds the narrative, regenerates (narrative survives), approves, publishes and downloads the PDF; Q2 is waived and plan close stays blocked until Q3/Q4 are handled.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p20_planning
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p20_planning/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p20-annual-plan.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(planning): seeded annual plan, planned activity and quarterly report features with generated quarterly document" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Create and activate the annual plan, complete one activity through its progress form, let another pass its planned end and run the sweep from /admin/jobs, generate the Q1 report, add the narrative, regenerate and confirm the narrative survived, then approve, publish and open the locked PDF version.
COMMIT: feat(planning): seeded annual plan, planned activity and quarterly report features with generated quarterly document

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P21 — Invigilation: exam schedules, conflict-aware duties and publication
GOAL: (3 days) Seed the `exam_schedule` feature over the new scheduling tables and the one `DutyAssignment` table: session editor and exam_timetable import preset, an assignment board with hard/soft conflict badges and fairness-based suggestions from P10's suggest engine, publication blocked on hard conflicts with ack-required declinable duty notifications, amendment that re-notifies only changed persons, and the invigilation schedule report.
DEPENDS: ['P20']
DELIVERABLES:
  - Schema delta `prisma/schema/scheduling.prisma`: `ExamSchedule`, `ExamSession` and `DutyAssignment` exactly as schema §20 (`ExamSchedule.featureRecordId @unique`, `DutyAssignment` with `REF(duty)`, the `DutyChain` self relation and `endReason EndReason?`). Migration `p21_invigilation`: `migrate dev --create-only`, `gen-rls.ts --append` (three TENANT tables), `migrate dev`, `prisma generate`; then the hand-written migration `constraints_p21` with `CREATE UNIQUE INDEX duty_active_person ON duty_assignment (duty_type, duty_id, person_id) WHERE ended_at IS NULL;` per schema §22 (later phases append `constraints_<phase>`).
  - Seed `prisma/seed/features/exam_schedule.ts` per builder seededFeatures: navigation {group:'academic', order:30, icon:'clipboard-list', visibleRoles:[department_head,deputy_head,instructor]}, scope {level:'department', scopeFrom:'parent'}, parentSubject term, record {numberPrefix:'ES', backing:{kind:'module', adapter:'exam_schedule.backing'}, fields examType single_choice, calendarPeriodId}, steps draft (surface 'board'; assignee role deputy_head|department_head; publish -> published guard schedule.noHardConflicts, effects schedule.setPublishedAt + schedule.registerBlocks + schedule.notifyDuties, reminders `duty_-1d`) -> published (wait; amend -> amended; supersede -> superseded) -> amended (surface 'board'; publish -> published effect schedule.notifyDeltaDuties), terminal superseded (cancelled), permission invigilation.manage, listViews by_term|my_duties, counters duties_unacknowledged, my_upcoming_duties. `prisma/seed/features/import_batch.ts` gains the `exam_timetable` preset (parentSubjectType term) - a locked-subtree change, so it arrives as a `seed-upgrade` draft published under `SEED_AUTO_PUBLISH=1`.
  - `src/modules/invigilation/adapters.ts`: backing `exam_schedule.backing`, guard `schedule.noHardConflicts` (P10's `availability.checkConflicts`, hard, over sessions and duties), effects `schedule.registerBlocks` (session rooms hard, duty persons hard), `schedule.setPublishedAt`, `schedule.notifyDuties` (ack-required declinable `invigilation_notification`, one dedupeKey per assignment) and `schedule.notifyDeltaDuties` (only changed persons, ack reset, changed blocks re-registered), auto `schedule.dutyDeclineSubscriber` (outbox event `notification.declined` -> `DutyAssignment.endedAt` with endReason `declined` plus a notice to invigilation.manage holders, idempotent through `EventHandlerReceipt`), validate `import.validate.exam_timetable` + effect `import.commit.exam_timetable` + export `import.template.exam_timetable`, export `invigilation.schedule`. `availability.suggestAssignees` is NOT re-registered: P10's single registration over `src/platform/availability/suggest.ts` is called with `{ purpose: 'invigilation', excludeTeachingIn: sessionSectionOfferingIds, maxPerDay, maxPerTerm }` from `src/modules/invigilation/duties.ts`. All `schedule.*` adapters here are shared verbatim with P22.
  - `src/modules/invigilation/{registry.ts (SubjectRegistry 'exam_schedule' and 'exam_session'; variables exam_date/course/room/role), schedule.ts (session CRUD, room and time overlap checks), duties.ts (assign, replace = end + supersede row, endDuty, `dutiesOf(person, term)`, fairness counts, the suggest call above), conflicts.ts (hard vs soft badge classification for absolute and recurring blocks), surface.tsx (`surfaces.exam_schedule = { stepRenderers: { board: AssignmentBoard, my_duties: MyDuties }, detailExtras: ConflictSummary + DeclinedAlerts }`), queries.ts, actions.ts, reports.ts}`.
  - Module pages `src/app/(app)/d/[dept]/(modules)/invigilation/[recordId]/board/page.tsx` (sessions grid, assignment with badges, Suggest, fairness column) and `(modules)/duties/page.tsx` (the actor's duties with acknowledge/decline through the inbox), both new; P10's `(modules)/availability/page.tsx` is EXTENDED with the `invigilation` purpose tab (maxPerDay/maxPerTerm) beside the appointments tab P18 added. `next.config.ts` rewrite `/d/:dept/invigilation/:path*` -> `/d/:dept/f/exam_schedule/:path*`.
  - Seeds: `prisma/seed/templates/invigilation.ts` (invigilation_notification, invigilation_amended, report.invigilation_schedule), the only source of those keys; `prisma/seed/reminder-schedules.ts` gains `duty_-1d` and is its only source; `prisma/seed/permissions.ts` gains invigilation.manage and invigilation.view_own. Report registration `invigilation_schedule` (xlsx with one sheet per person including ack status, plus pdf) on P11's registry. `tests/fixtures/build-fixtures.ts` is extended to generate `exam-timetable.xlsx` at test setup (no committed workbooks).
KEY FILES: prisma/schema/scheduling.prisma; prisma/migrations/<stamp>_p21_invigilation/migration.sql; prisma/migrations/<stamp>_constraints_p21/migration.sql; prisma/seed/features/exam_schedule.ts; prisma/seed/features/import_batch.ts; src/modules/invigilation/adapters.ts; src/modules/invigilation/duties.ts; src/modules/invigilation/conflicts.ts; src/modules/invigilation/surface.tsx; src/modules/invigilation/reports.ts; src/app/(app)/d/[dept]/(modules)/invigilation/[recordId]/board/page.tsx; src/app/(app)/d/[dept]/(modules)/duties/page.tsx; src/app/(app)/d/[dept]/(modules)/availability/page.tsx; prisma/seed/templates/invigilation.ts; tests/fixtures/build-fixtures.ts
TESTS:
  - tests/unit/availability/suggest.test.ts (extended): with `{purpose:'invigilation', excludeTeachingIn}` the shared suggest engine excludes instructors teaching a section in the session and anyone with a hard conflict, respects the policy windows and maxPerDay/maxPerTerm, and balances counts across the pool.
  - tests/unit/invigilation/conflicts.test.ts: hard versus soft badge classification for absolute and recurring blocks; the exam_timetable validator flags an unknown room, an unknown course and two overlapping sessions in one room.
  - tests/unit/feature/seeds.test.ts (extended): `exam_schedule` validates and compiles; the `import_batch` seed now carries six presets and each still emits its own permission keys and locked `fieldDefaults.kind`.
  - tests/integration/invigilation/publish.test.ts: publish is blocked while any duty has a hard conflict; publish registers room and person hard blocks (a direct overlapping insert is refused by P10's EXCLUDE constraint), creates ack-required declinable notifications with dedupeKeys and the -1d reminders, and writes `publishedAt` only through the effect; a decline ends the duty and notifies the DPT; a replacement creates a superseding row and `duty_active_person` prevents two active duties for the same person and session; amend + publish re-notifies only the changed persons and resets their acknowledgement.
  - tests/worker/outbox-dispatch.test.ts (extended): the `notification.declined` subscriber runs once per event and ends exactly one duty.
  - tests/components/invigilation/AssignmentBoard.test.tsx: hard and soft badges, fairness column and the Suggest result list render from a fixture.
  - tests/e2e/p21-invigilation.spec.ts: DPT imports the generated exam timetable workbook, assigns two invigilators (one shows a red teaching-clash badge and publication is refused until replaced through Suggest) and publishes; an instructor acknowledges one duty from the inbox and declines another with a reason; DPT sees the declined alert, replaces, amends and republishes so only the replaced person gets a new mail; the invigilation xlsx has one sheet per instructor with ack status.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p21_invigilation
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p21_invigilation/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma migrate dev --create-only --name constraints_p21
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p21-invigilation.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(invigilation): exam schedules, conflict-aware duty assignment and seeded schedule publication with acknowledgements" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Publish the pending 'system update' draft in /admin/features/import_batch (the exam_timetable preset), build a three-session final exam schedule, drag instructors onto sessions and see the hard (teaching clash) and soft (own section) badges, fill the rest with Suggest, publish, acknowledge and decline from an instructor inbox, replace and republish, then export the xlsx and the PDF.
COMMIT: feat(invigilation): exam schedules, conflict-aware duty assignment and seeded schedule publication with acknowledgements

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P22 — Laboratory schedules and lab staff
GOAL: (2 days) Seed the `lab_schedule` feature reusing P21's locked `schedule.*` adapters: import the laboratory schedule through P10's pipeline into LabSchedule/LabSession rows, assign lab instructor/RA/responsible duties with recurring-block conflict detection, publish with derived LAB_STAFF@lab_schedule grants, give lab staff a weekly view and `lab_activity` tasks, and ship the lab schedule report.
DEPENDS: ['P21']
DELIVERABLES:
  - Schema delta `prisma/schema/scheduling.prisma`: `LabSchedule { Id, TENANT, termId FK Term, version Int @default(1), featureRecordId String @unique, publishedAt?, @@unique([termId, version]) }` and `LabSession { Id, TENANT, labScheduleId FK LabSchedule (Cascade), sectionOfferingId FK SectionOffering, resourceId FK Resource, weekday, startTime, endTime, weekPattern, source RowSource, importBatchId?, @@index([labScheduleId, weekday]) }` per schema §20. Migration `p22_lab`: `migrate dev --create-only`, `gen-rls.ts --append` (two TENANT tables), `migrate dev`, `prisma generate`.
  - Seed `prisma/seed/features/lab_schedule.ts` per builder seededFeatures: navigation {group:'resources', order:10, icon:'flask-conical', visibleRoles:[department_head,deputy_head,instructor,lab_staff]}, scope {level:'department', scopeFrom:'parent'}, parentSubject term, record {numberPrefix:'LS', backing:{kind:'module', adapter:'lab_schedule.backing'}}, steps draft (surface 'board'; publish -> published guard schedule.noHardConflicts, effects schedule.setPublishedAt + schedule.registerBlocks + schedule.notifyDuties + lab.syncDerivedGrants) -> published (wait; amend -> amended; supersede -> superseded) -> amended (publish -> published effect schedule.notifyDeltaDuties), terminal superseded (cancelled), permission lab.manage, listViews by_term|my_lab, counters lab_duties_unacknowledged, my_lab_sessions_this_week. `prisma/seed/features/import_batch.ts` gains the `lab_schedule` preset (parentSubjectType feature_record) as a seed-upgrade draft.
  - `src/modules/labs/adapters.ts`: backing `lab_schedule.backing`, validate `import.validate.lab_schedule` (a context provider resolving lab names to `Resource(kind=computer_lab)`, course/section codes to `SectionOffering`, weekday/time parsing, odd/even week patterns and per-lab overlap checks) + effect `import.commit.lab_schedule` (LabSessions with source `import`, replacing the previous import of the same schedule) + export `import.template.lab_schedule`, effect `lab.syncDerivedGrants` (emits `duty.assigned`/`duty.ended` so the P3 `src/platform/identity/derive.ts` subscriber writes LAB_STAFF@lab_schedule and the nightly `grant.reconcile` repairs it), export `lab.schedule`. The `schedule.noHardConflicts`, `schedule.registerBlocks`, `schedule.notifyDuties`, `schedule.notifyDeltaDuties`, `schedule.setPublishedAt` and `schedule.dutyDeclineSubscriber` adapters are P21's registrations, reused unchanged (recurring sessions are registered as weekday-pattern hard blocks).
  - `src/modules/labs/{registry.ts (SubjectRegistry 'lab_schedule' and 'lab_session' - the latter also closes the parent pointer of P9's `task` preset `lab_activity`; contextOf -> {resourceId, labScheduleId}; relationships lab_staff from the active DutyAssignment), duties.ts (lab_instructor|ra|responsible roles over P21's duties service), views.ts (weekly grid per lab and per person), surface.tsx (`surfaces.lab_schedule = { stepRenderers: { board: LabAssignmentBoard, my_lab: WeeklyGrid } }`), queries.ts, actions.ts, reports.ts}`. Lab activity work uses the P9 `task` seed's `lab_activity` preset (permissionPrefix `lab.task`, parentSubjectType lab_session) - no new feature definition.
  - Module pages `src/app/(app)/d/[dept]/(modules)/labs/[recordId]/board/page.tsx` and `(modules)/labs/my/page.tsx` (my lab sessions, my lab tasks, create a lab activity); `next.config.ts` rewrite `/d/:dept/labs/:path*` -> `/d/:dept/f/lab_schedule/:path*`.
  - Seeds: `prisma/seed/templates/lab.ts` (lab_assignment message, report.lab_schedule), the only source of those keys; `prisma/seed/permissions.ts` gains lab.manage, lab.view_own and lab.task.manage (lab_staff at `assigned`, scoped to lab_schedule). Report registration `lab_schedule` (xlsx per lab and per person with ack status, plus pdf) on P11's registry. `tests/fixtures/build-fixtures.ts` gains the generated `lab-schedule.xlsx`.
KEY FILES: prisma/schema/scheduling.prisma; prisma/migrations/<stamp>_p22_lab/migration.sql; prisma/seed/features/lab_schedule.ts; prisma/seed/features/import_batch.ts; src/modules/labs/adapters.ts; src/modules/labs/duties.ts; src/modules/labs/views.ts; src/modules/labs/surface.tsx; src/modules/labs/reports.ts; src/app/(app)/d/[dept]/(modules)/labs/[recordId]/board/page.tsx; src/app/(app)/d/[dept]/(modules)/labs/my/page.tsx; prisma/seed/templates/lab.ts; tests/fixtures/build-fixtures.ts
TESTS:
  - tests/unit/labs/validator.test.ts: unknown lab, unknown section, bad time format, overlapping slots in one lab; odd/even week patterns parsed.
  - tests/unit/feature/seeds.test.ts (extended): `lab_schedule` validates and compiles to the same state graph as `exam_schedule` (asserted by comparing the two compiled `statesJson`), its adapter pointers resolve to the shared `schedule.*` registrations, and the import_batch seed now carries seven presets.
  - tests/integration/labs/schedule.test.ts: commit creates LabSessions and replaces the previous import; assigning an RA yields a derived LAB_STAFF@lab_schedule grant after outbox dispatch and ending the duty sets `validTo`; publish registers recurring hard blocks for the lab rooms and duty persons so a meeting booked in that lab at that weekday/time reports a clash; an RA may create a `task` record with preset `lab_activity` under `lab.task.manage` while an unrelated instructor may not.
  - tests/integration/labs/conflicts.test.ts: assigning a person whose `ClassTimetableSlot` overlaps a weekly lab session produces a hard badge and blocks publication.
  - tests/e2e/p22-labs.spec.ts: DPT imports the generated lab schedule workbook, opens Lab A's weekly grid, assigns a lab instructor and an RA (one clash resolved by choosing another RA) and publishes; the RA logs in, sees My Lab, acknowledges the notification, creates a lab activity task and completes it; DPT exports the lab schedule xlsx.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p22_lab
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p22_lab/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p22-labs.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(labs): lab schedule import, duty assignments with derived lab staff grants and seeded lab schedule publication" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Publish the pending system update for the lab_schedule import preset, import the lab schedule fixture, open Lab A's weekly grid, assign an RA whose timetable overlaps and see the hard badge, fix it, publish, then log in as the RA to see My Lab, the ack-required notice and a new lab activity task.
COMMIT: feat(labs): lab schedule import, duty assignments with derived lab staff grants and seeded lab schedule publication

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P23 — Resources, assets and maintenance
GOAL: (2 days) Add the asset inventory and maintenance ledger on top of the foundation `Resource` table, the `assets` import preset, maintenance work as the P9 `task` preset `maintenance` with due and licence-expiry reminders, the composed lab profile view and the inventory report. No new feature definition is invented: maintenance is a task preset per the builder's seeded list.
DEPENDS: ['P22']
DELIVERABLES:
  - Schema delta `prisma/schema/resource.prisma`: `Asset { Id, TENANT, assetTag, name, category AssetCategory, serialNumber?, resourceId FK? Resource, responsiblePersonId FK? Person, purchaseDate? @db.Date, condition?, status AssetStatus @default(in_use), warrantyUntil? @db.Date, licenseJson?, importBatchId?, @@unique([departmentId, assetTag]) }` and `MaintenanceRecord { Id, TENANT, REF(target), performedAt, type, description?, performedBy?, cost? @db.Decimal(12,2), nextDueAt?, taskId?, @@index([nextDueAt]) }` per schema §20 (`Resource.computerCount`, `softwareList` and `attributesJson` already exist from the foundation academic registry). Migration `p23_resource`: `migrate dev --create-only`, `gen-rls.ts --append` (two TENANT tables), `migrate dev`, `prisma generate`.
  - `src/modules/resources/adapters.ts` (created in P17 with `resources_of_kind`) gains: validate `import.validate.assets` (category enum, duplicate assetTag inside the file and against the database, unknown location resource, unknown responsible person, date parsing), effect `import.commit.assets` (upsert by assetTag, keep the source Document), export `import.template.assets`, compute `resources.labProfile`, effect `resources.createMaintenanceTask` (a `task` record with preset `maintenance` and parent = the asset or resource, deadline = nextDueAt), effect `resources.recordMaintenance` (writes the `MaintenanceRecord` and the next due date when the maintenance task completes), auto `resources.licenceExpiryReminders` (`Scheduler.subscribeReminders` on `Asset.warrantyUntil`/licence expiry with schedule `asset_expiry`). `prisma/seed/features/import_batch.ts` gains the `assets` preset (parentSubjectType resource) as a seed-upgrade draft.
  - `src/modules/resources/{registry.ts (SubjectRegistry 'asset' and 'maintenance_record' - 'asset' closes the parent pointer of P9's `task` preset `maintenance`; resource contextOf -> responsiblePersonId), service.ts (asset CRUD, maintenance record creation and completion), lab-profile.ts (Resource + assets grouped by category + software list + maintenance history + P22's weekly sessions + responsible staff), surface.tsx (`surfaces.task` gains a `maintenance` preset detail extra: the target asset card and maintenance history), queries.ts, actions.ts, reports.ts}`.
  - Module pages `src/app/(app)/d/[dept]/(modules)/resources/page.tsx` (rooms and labs by kind and status), `(modules)/resources/[resourceId]/page.tsx` (lab profile tabs overview / assets / maintenance / schedule / documents) and `(modules)/resources/assets/page.tsx` + `(modules)/resources/assets/[assetId]/page.tsx` (list, create, import through P10's pipeline, maintenance requests as nested `task` records) - all new routes under `(modules)`.
  - Seeds: `prisma/seed/reminder-schedules.ts` gains `asset_expiry` (-30/-7/0) and is its only source; `prisma/seed/templates/resource.ts` (asset_expiring message, report.inventory, report.lab_profile); `prisma/seed/permissions.ts` gains resource.manage and resource.view. Report registrations `inventory` (xlsx|pdf|csv with resource/category/status filters) and `lab_profile` (pdf) on P11's registry. `tests/fixtures/build-fixtures.ts` gains the generated `assets.xlsx`.
KEY FILES: prisma/schema/resource.prisma; prisma/migrations/<stamp>_p23_resource/migration.sql; prisma/seed/features/import_batch.ts; src/modules/resources/adapters.ts; src/modules/resources/service.ts; src/modules/resources/lab-profile.ts; src/modules/resources/registry.ts; src/modules/resources/reports.ts; src/app/(app)/d/[dept]/(modules)/resources/[resourceId]/page.tsx; src/app/(app)/d/[dept]/(modules)/resources/assets/page.tsx; prisma/seed/templates/resource.ts; prisma/seed/reminder-schedules.ts; tests/fixtures/build-fixtures.ts
TESTS:
  - tests/unit/resources/assets-validator.test.ts: invalid category, duplicate assetTag inside the file and against the database, unknown location and responsible person, date parsing.
  - tests/unit/resources/lab-profile.test.ts: the pure composition groups assets by category, merges the software list and orders the maintenance history.
  - tests/integration/resources/maintenance.test.ts: a `MaintenanceRecord` with a next due date creates a `task` record with preset `maintenance` plus its ScheduledJob reminder rows; a licence asset gets the `asset_expiry` jobs; completing the maintenance task writes the follow-up record through the effect; the asset import upserts by assetTag and retains the source document.
  - tests/integration/resources/lab-profile.test.ts: the profile query returns assets, software, maintenance history and this term's lab sessions, and a cross-department resource is invisible under RLS.
  - tests/e2e/p23-resources.spec.ts: DPT imports the generated assets workbook (one duplicate tag fixed in the preview) and adds a projector to Lab A; an instructor files a maintenance task with a photo, the responsible RA completes it, DPT sees the maintenance record; Lab A's profile shows assets, software and this week's sessions; a licence expiring in 7 days appears in the DH widget and the inventory xlsx downloads.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p23_resource
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p23_resource/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p23-resources.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(resources): asset inventory, maintenance ledger with task-backed maintenance and inventory report" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Publish the pending system update for the assets import preset, open Lab A's profile (computers, software list, assets, maintenance history, weekly schedule), add a software licence expiring in seven days and confirm it appears in the DH licences-expiring widget, file and complete a maintenance task, then generate the inventory report.
COMMIT: feat(resources): asset inventory, maintenance ledger with task-backed maintenance and inventory report

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P24 — Staff profiles, derived views and staff reports
GOAL: (2 days) Complete the staff directory on the foundation `StaffProfile`/`ProfileItem` tables: profile editor with evidence documents, derived views (courses taught, committees, workload, activity), the `staff` import preset, the `staff_activity` projection registration and the seven staff reports plus the single-person profile PDF. P3's people pages are extended, not duplicated.
DEPENDS: ['P23']
DELIVERABLES:
  - No schema change: `StaffProfile`, `ProfileItem`, `Person`, `DepartmentPerson` and `Document`/`DocumentLink` all ship with P3/P6.
  - `src/modules/staff/{service.ts (profile upsert with `rowVersion` optimistic concurrency, ProfileItem CRUD per `ProfileItemKind` with evidence `DocumentLink(linkRole='evidence')`), derived.ts (coursesTaught from the `TeachingAssignment` history, committees from `GroupMembership`, workload from P10's `availability.workload` plus `loadHours`, activity from the `staff_activity` projection), adapters.ts, queries.ts, actions.ts, surface.tsx, reports.ts}`. `adapters.ts` registers projection `staff.staffActivity` (per person per period from task completions, committee memberships, reports submitted, meetings attended and ProfileItems added, declaring the `DomainEvent` names it consumes) plus validate `import.validate.staff` / effect `import.commit.staff` / export `import.template.staff`; `source_binding staff_in_department` is P18's registration, reused.
  - `prisma/seed/features/import_batch.ts` gains the `staff` preset with an explicit `parentSubjectType: 'term'` (the builder's parent for department-wide imports, so `parentSubject.required:true` still holds) - a seed-upgrade draft published under `SEED_AUTO_PUBLISH=1`.
  - Module pages: P3's `src/app/(app)/d/[dept]/(modules)/people/page.tsx` is EXTENDED with rank and specialization filters and P3's `(modules)/people/[personId]/page.tsx` with the tabs overview, items by kind with evidence upload through `/api/uploads`, teaching history, committees, workload and activity; `(modules)/people/me/page.tsx` is the one new route (own profile under `staff.own`).
  - Report registrations `src/modules/staff/reports.ts` on P11's registry, all pdf|xlsx|csv unless noted: `staff_directory`, `qualification_summary`, `rank_specialization_distribution`, `teaching_history`, `committee_assignments`, `workload`, `staff_activity`, `staff_profile` (single person, pdf); templates in `prisma/seed/templates/staff.ts` (the only source of those keys); `prisma/seed/permissions.ts` gains staff.manage, staff.view and staff.own.
  - Dashboard: the `staffActivities` widget from P11's catalogue is enabled in the DH/DPT layouts in `src/platform/dashboard/layouts.ts` now that the projection is registered, and `staff_activity` is added to the projection rebuild list on the existing `/admin/jobs` page.
KEY FILES: src/modules/staff/service.ts; src/modules/staff/derived.ts; src/modules/staff/adapters.ts; src/modules/staff/reports.ts; src/modules/staff/surface.tsx; src/app/(app)/d/[dept]/(modules)/people/page.tsx; src/app/(app)/d/[dept]/(modules)/people/[personId]/page.tsx; src/app/(app)/d/[dept]/(modules)/people/me/page.tsx; prisma/seed/templates/staff.ts; prisma/seed/features/import_batch.ts; src/platform/dashboard/layouts.ts
TESTS:
  - tests/unit/staff/derived.test.ts: workload sums `loadHours` per term, the teaching history is ordered by term, and the rank/specialization distributions bucket correctly.
  - tests/unit/staff/staff-import.test.ts: the staff validator flags an unknown staffId, a duplicate email and an invalid rank.
  - tests/unit/feature/seeds.test.ts (extended): the import_batch seed now carries nine presets and the `staff` preset declares `parentSubjectType: 'term'`, so `PARENT_TYPE` and `PRESET_CONSISTENT` pass.
  - tests/integration/staff/profile.test.ts: an instructor edits own profile items with evidence and cannot edit another's; the DH can; optimistic concurrency rejects a stale `rowVersion`; every report data source respects RLS across the two departments.
  - tests/integration/dashboard/module-projections.test.ts (extended): the incrementally maintained `staff_activity` projection equals a full rebuild after a scripted event sequence and the `staffActivities` widget appears only for DH/DPT.
  - tests/e2e/p24-staff.spec.ts: an instructor adds a publication with an evidence PDF and a certification; the DH opens the directory, filters by rank, runs the staff directory and workload reports and downloads the single-person profile PDF; the DH dashboard shows the staff activities widget.
VERIFY:
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p24-staff.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(staff): staff profiles with evidence documents, derived views, staff activity projection and the staff report set" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Publish the pending system update for the staff import preset, edit your own profile with a publication and an evidence PDF, open another instructor's profile as DH and confirm the workload, committees and teaching history tabs, then generate the teaching history and workload reports and check the staff activities widget on the DH dashboard.
COMMIT: feat(staff): staff profiles with evidence documents, derived views, staff activity projection and the staff report set

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P25 — Load assignment bridge: reviewed preferences, export specs and result import
GOAL: (3 days) Seed the `load_cycle` feature: the closed preference campaign feeds `ReviewedPreference` rows through an event-driven auto action, the department reviews them, P11's versioned admin-editable `ExportFormatSpec` produces the Excel LoadExport against a golden sample, and the `load_result` import preset applies TeachingAssignment rows that drive portfolio provisioning, evaluation subjects and derived instructor grants. P11's export-spec platform file and admin page are extended, never re-delivered.
DEPENDS: ['P24']
DELIVERABLES:
  - Schema delta `prisma/schema/load.prisma`: `LoadCycle { Id, TENANT, termId String @unique + term Term, preferenceCampaignId?, featureRecordId String @unique, importBatchId? }`, `ReviewedPreference` and `LoadExport` exactly as schema §20 (`ExportFormatSpec` itself ships with P11's `reporting.prisma`). Migration `p25_load`: `migrate dev --create-only`, `gen-rls.ts --append` (three TENANT tables), `migrate dev`, `prisma generate`.
  - Seed `prisma/seed/features/load_cycle.ts` per builder seededFeatures: navigation {group:'academic', order:40, icon:'arrow-left-right', visibleRoles:[department_head,deputy_head]}, scope {level:'department', scopeFrom:'parent'}, parentSubject term, record {numberPrefix:'LC', backing:{kind:'module', adapter:'load_cycle.backing'}, field preferenceCampaignId record_picker{featureKey:'campaign', presetKey:'preference'}}, steps preferences_open (wait; `close_preferences` auto event `campaign.closed` matched on the record's preferenceCampaignId -> under_review, effect load.seedReviewedPreferences) -> under_review (surface 'review_grid'; assignee role deputy_head|department_head; export -> exported guard load.allDecided, effect load.exportTabular) -> exported (await -> awaiting_result; re_export -> $self) -> awaiting_result (import_result -> result_imported guard load.resultBatchCommitted, surface 'diff') -> result_imported (apply -> applied effect load.applyTeachingAssignments; reject_result -> awaiting_result requiredComment), terminal applied (success), permission load.manage, listViews by_term, counter load_cycles_under_review. `prisma/seed/features/import_batch.ts` gains the `load_result` preset (parentSubjectType term) as a seed-upgrade draft.
  - `src/modules/load/adapters.ts`: backing `load_cycle.backing`, effect `load.seedReviewedPreferences` (rank matrix answers of the closed preference campaign plus department-added rows), guard `load.allDecided`, effect `load.exportTabular` (active `ExportFormatSpec` + golden validation -> `LoadExport` + Document with the spec version and the sha256 of the reviewed rows), guard `load.resultBatchCommitted`, compute `load.resultDiff`, effect `load.applyTeachingAssignments` (creates `TeachingAssignment(source=load_import, importBatchId)`, ends removed ones, emits `teaching.assigned`/`teaching.ended` so the P3 derive subscribers write INSTRUCTOR@section_offering and the next portfolio provisioning picks up the new instructor), validate `import.validate.load_result` + effect `import.commit.load_result` + export `import.template.load_result`.
  - Export-spec delta only: P11's `src/platform/reporting/export-spec.ts` gains the transforms `upper | date:<fmt> | lookup:<map> | concat` in `applySpec(rows, spec)` and the `previewExport(spec, rows)` helper if they are not already there; P11's `src/app/(admin)/admin/export-specs/page.tsx` (versions, column editor, golden-sample upload, validate, activate) is reused as-is. New here: the seed `prisma/seed/export-specs/load_assigner_v1.ts` added to the ordered list in `prisma/seed.ts`, and `tests/fixtures/build-fixtures.ts` generating the infra-named `golden-load-export.xlsx` plus `load-result.xlsx`.
  - `src/modules/load/{registry.ts, service.ts (createCycle for a term, review actions, export, applyResult), surface.tsx (`surfaces.load_cycle = { stepRenderers: { review_grid: ReviewGrid, diff: DiffReview }, detailExtras: ExportCard }`), queries.ts, actions.ts}`; `next.config.ts` rewrite `/d/:dept/load/:path*` -> `/d/:dept/f/load_cycle/:path*`; `prisma/seed/permissions.ts` gains load.manage; `prisma/seed/templates/load.ts` (load_applied message), the only source of that key.
KEY FILES: prisma/schema/load.prisma; prisma/migrations/<stamp>_p25_load/migration.sql; prisma/seed/features/load_cycle.ts; src/modules/load/adapters.ts; src/modules/load/service.ts; src/modules/load/surface.tsx; src/platform/reporting/export-spec.ts; prisma/seed/export-specs/load_assigner_v1.ts; prisma/seed.ts; prisma/seed/features/import_batch.ts; tests/fixtures/build-fixtures.ts; prisma/seed/templates/load.ts
TESTS:
  - tests/unit/reporting/export-spec.test.ts (extended): each new transform (upper, date:<fmt>, lookup:<map>, concat) maps correctly, required-column errors are reported, `previewExport` returns the header row plus the first rows, and golden-sample validation catches header drift, a wrong sheet name and a reordered column.
  - tests/unit/load/result-validator.test.ts: unknown instructor, unknown course or section for the term, non-numeric hours; the diff computation classifies add/end/unchanged.
  - tests/unit/feature/seeds.test.ts (extended): `load_cycle` validates and compiles; the `close_preferences` auto action compiles to an outbox subscription with handler key `feature.autoOnEvent:load_cycle:preferences_open:close_preferences`; its `record_picker` field passes `BINDING_ARGS` against the published `campaign` feature and `preference` preset; the import_batch seed now carries ten presets, matching the builder's list.
  - tests/integration/load/cycle.test.ts: closing the preference campaign seeds `ReviewedPreference` rows from the ranked answers exactly once (EventHandlerReceipt); export is blocked while a row is undecided; export writes an xlsx Document matching the golden headers plus a `LoadExport` with the spec version and source hash; importing the generated load-result workbook shows the diff; apply creates `TeachingAssignment(source=load_import)`, ends removed ones, produces INSTRUCTOR@section_offering grants and makes the next portfolio provisioning run pick up the new instructor.
  - tests/e2e/p25-load.spec.ts: DH opens the load cycle for the term after the preference campaign closes, accepts two rows, adjusts one rank and adds a course; in /admin/export-specs the spec is validated against the golden sample (drift error, then fixed and activated as v2); DH exports the xlsx, uploads the result fixture, reviews the diff and applies; the affected instructor is notified and their profile teaching history shows the new section.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p25_load
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p25_load/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm -e SEED_AUTO_PUBLISH=1 web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p25-load.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(load): seeded load cycle feature with reviewed preferences, versioned export specs and result import" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Reorder the export spec columns in /admin/export-specs, validate against the golden sample (observe the drift error, then fix and activate v2), export the current load cycle, open the xlsx, re-import a modified result file, review the diff and apply; confirm the instructor's teaching history and the derived instructor grant.
COMMIT: feat(load): seeded load cycle feature with reviewed preferences, versioned export specs and result import

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P26 — Student communication: announcements, conversations and section issues
GOAL: (3 days) Seed the `announcement` feature (audience fan-out with acknowledgement and auto-expiry) and the task-backed `student_issue` feature (section-scoped intake by the representative with limited access), add the program/section/representative browser and the DH-representative conversation threads, and complete the student-representative dashboard layout. Representative assignment and invitation stay P3 code.
DEPENDS: ['P25']
DELIVERABLES:
  - Schema delta `prisma/schema/notification.prisma`: `Announcement { Id, TENANT, authorPersonId FK Person, title, body, templateKey?, audienceSpecJson, ackRequired Boolean @default(false), featureRecordId String @unique, publishedAt? (derived cache), expiresAt?, Ts, @@index([departmentId, publishedAt]) }` per schema §12. Migration `p26_announcement`: `migrate dev --create-only`, `gen-rls.ts --append` (one TENANT table), `migrate dev`, `prisma generate`.
  - Seeds `prisma/seed/features/announcement.ts` and `prisma/seed/features/student_issue.ts` per builder seededFeatures, appended to `prisma/seed/features/index.ts`. announcement: navigation {group:'communication', order:10, icon:'bell', visibleRoles:[department_head,deputy_head,instructor,student_rep]}, scope {level:'department', scopeFrom:'record_field', fieldKey:'audienceSpec'}, record {numberPrefix:'AN', backing:{kind:'module', adapter:'announcement.backing'}, fields title, body, audienceSpec audience_picker, ackRequired, expiresAt, templateKey}, steps draft (slot attachments; assignee creator; publish -> published guard campaign.audienceNonEmpty (the P15 registration, reused), effect announcement.fanout; discard -> discarded) -> published (wait, surface 'ack_status'; `expire` auto field_datetime expiresAt -> expired; withdraw -> withdrawn), terminals expired (success) / withdrawn (cancelled) / discarded (cancelled), counters announcements_unacknowledged. student_issue: navigation {group:'communication', order:20, icon:'message-square-warning', visibleRoles:[department_head,deputy_head,student_rep]}, scope {level:'section', scopeFrom:'parent'}, parentSubject section, record {numberPrefix:'SI', backing:{kind:'task', taskKind:'student_issue', extension:'case'}, fields category, description, affected_students, responsible person_picker, waitingOn, resolution}, steps intake (assignee relationship section_rep; submit -> open effect issue.notifyDh) -> open (assignee role department_head|deputy_head; assign -> assigned kind assign reassignTo record_field responsible) -> assigned (onEnter notify ackRequired; effect case.subscribeIntervalNudge; start -> in_progress) -> in_progress (wait -> waiting requiredFields waitingOn; resolve -> resolved requiredFields resolution) -> waiting (resume) -> resolved (close -> closed; auto close after_days settingKey case.autoCloseDays; reopen -> in_progress), terminals closed (success) / cancelled, counters unresolved_issues, my_section_open_issues.
  - `src/modules/communication/adapters.ts`: backing `announcement.backing`, effect `announcement.fanout` (`Notification.notify` per resolved recipient with dedupeKey `announcement:{id}:{personId}`, email-only delivery for persons without a User, writes `publishedAt`), auto `announcement.autoExpire`, effect `issue.notifyDh`, relationship `issue.repLimitedAccess` (level `limited` for the section's current representatives). The `case.*` adapters (subscribeIntervalNudge, cancelNudge, setResolvedAt, autoClose) are the P9 registrations, reused unchanged, and the `case.openIssues` projection registered in P19 gains the section dimension in `src/modules/cases/adapters.ts`.
  - `src/modules/communication/{registry.ts, hierarchy.ts (program -> section -> representative tree with `StudentSectionMembership` counts, reading P3's `src/platform/people/representatives.ts` for assignment and invite status), conversations.ts (`Thread(kind=conversation)` between DH/DPT and a representative with `sectionId`, open/closed, escalate -> a `student_issue` record), surface.tsx (`surfaces.announcement = { stepRenderers: { ack_status: AckStatus }, createExtras: AudienceComposer }`, `surfaces.student_issue = { detailExtras: SectionContext }`), queries.ts, actions.ts, reports.ts}`.
  - Module pages `src/app/(app)/d/[dept]/(modules)/communication/page.tsx` (hierarchy browser and representative directory with invite status), `(modules)/communication/conversations/[threadId]/page.tsx` and `(modules)/communication/my-section/page.tsx` (the representative's section, submit an issue through the runtime, respond in conversations); `next.config.ts` rewrite `/d/:dept/announcements/:path*` -> `/d/:dept/f/announcement/:path*`.
  - `src/platform/dashboard/layouts.ts`: the student-representative layout is completed (`announcements` unacknowledged first, `openRequests`, `pendingResponses`, `surveys`, `appointments`) and the DH layout gains `announcements_unacknowledged`. Seeds: `prisma/seed/templates/communication.ts` (announcement, representative_invitation, issue_assigned, report.announcement_acknowledgement), the only source of those keys; `prisma/seed/permissions.ts` gains communication.manage, announcement.publish and issue.manage. Report registration `announcement_acknowledgement` (xlsx) on P11's registry.
KEY FILES: prisma/schema/notification.prisma; prisma/migrations/<stamp>_p26_announcement/migration.sql; prisma/seed/features/announcement.ts; prisma/seed/features/student_issue.ts; src/modules/communication/adapters.ts; src/modules/communication/hierarchy.ts; src/modules/communication/conversations.ts; src/modules/communication/surface.tsx; src/modules/cases/adapters.ts; src/app/(app)/d/[dept]/(modules)/communication/page.tsx; src/app/(app)/d/[dept]/(modules)/communication/my-section/page.tsx; src/platform/dashboard/layouts.ts; prisma/seed/templates/communication.ts
TESTS:
  - tests/unit/communication/hierarchy.test.ts: the program/section/representative tree with membership counts and a section without a representative.
  - tests/unit/feature/seeds.test.ts (extended): `announcement` and `student_issue` validate and compile; `student_issue` compiles task-backed (no extra step tasks) and its `scopeFrom:'parent'` and the announcement's `scopeFrom:'record_field'` pass `SCOPE_CONSISTENT`; the announcement `expire` auto action compiles to a `field_datetime` scheduled transition; the seed list now contains exactly the twenty keys of the builder's seededFeatures.
  - tests/integration/communication/announcements.test.ts: a fan-out to a Year 2 audience creates exactly one `Notification` per person with its dedupeKey, email-only deliveries for persons without a User, correct acknowledgement aggregates, and `publishedAt` written only by the effect; auto-expiry fires once; withdraw stops further acknowledgement.
  - tests/integration/communication/issues.test.ts: a representative submits a section issue that becomes a `Task(kind=student_issue)` with the `Case` extension; the DH assigns it and the representative retains level `limited` (read, comment, reopen) but cannot assign; escalation from a conversation thread creates the same record with the thread as origin; the `openIssues` projection counts by section and responsible.
  - tests/integration/people/derived-grants.test.ts (extended): assigning a representative produces STUDENT_REP@section and an invited user with a set-password mail (the P3 path), a second primary representative is refused by the `section_rep_primary` index, and ending the assignment expires the grant.
  - tests/e2e/p26-communication.spec.ts: DH assigns a section representative (mail in Mailpit), the representative sets a password, submits a section issue with an attachment, DH assigns follow-up to DPT and the representative sees the limited view; DH publishes an announcement to Year 3 with acknowledgement required, the representative acknowledges and DH sees the counts; the representative dashboard shows announcements, open requests, pending responses and surveys.
VERIFY:
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p26_announcement
  $ docker compose run --rm web sh -c "npx tsx prisma/scripts/gen-rls.ts --append prisma/migrations/*_p26_announcement/migration.sql"
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p26-communication.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(communication): seeded announcement and student_issue features, representative hierarchy and DH conversations" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Assign a representative and complete the invitation from Mailpit, publish an announcement to Year 3 with acknowledgement required and watch the counts, submit a section issue as the representative and follow it to Resolved as DH, and confirm the representative sees only the limited view and the completed representative dashboard.
COMMIT: feat(communication): seeded announcement and student_issue features, representative hierarchy and DH conversations

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P27 — Search completion, document text extraction and audit coverage
GOAL: (3 days) Close the read side of the suite: index every module subject type into P11's search, extend P6's document text extractor to PDF and DOCX behind a new `document.extract` queue, and mount the merged audit history on every richer module page with filters and export on /admin/audit. No search, reporting or audit platform service is re-implemented.
DEPENDS: ['P26']
DELIVERABLES:
  - Search completion (no new tables; `SearchIndexEntry`, `src/platform/search/*` and the search page ship with P11): every module's `registry.ts` gains its `indexDoc` and ACL-token projection for the subject types added in P12-P26 - committee, committee_report, portfolio, cqi_report, campaign, meeting, appointment, annual_plan, planned_activity, quarterly_report, exam_schedule, exam_session, lab_schedule, lab_session, asset, maintenance_record, load_cycle, announcement, student_issue - beside the `feature_record` and `feature_step_instance` docs P9 registers.
  - Extraction delta: P6's `src/platform/document/extract.ts` (`extractText`, today text/csv) gains the pdf path (`pdfjs-dist`) and the docx path (`mammoth`), both worker-only dependencies installed with `docker compose run --rm web npm install`; it writes `DocumentVersion.extractedText` and re-indexes the linked subjects through P11's `search.reindex`. No new `src/platform/search/extract.ts`.
  - Worker: `src/platform/scheduler/queues.ts` gains the `document.extract` queue (policy `standard`, retryLimit 3) and `apps/worker/src/handlers/document-extract.ts` is delivered, enqueued from P6's upload route inside the same transaction through `enqueue(tx, ...)` with the ledger key `extract:{documentVersionId}`; `enums.prisma` `JobKind` gains `document_extract`, delivered by the prisma-generated migration `p27_job_kind` (no new tables, so no `gen-rls` run).
  - Audit coverage: P4's merged history panel (`HistoryPanel.tsx` over `history(subjectRef)` = `AuditEvent` + `WorkflowTransitionLog`, already rendered as the audit section of the P9 record detail) is mounted on every richer module page delivered in P13-P26 (assessment, consolidated portfolio, campaign results, invigilation and lab boards, resource and asset pages, people profiles, communication pages), and the existing `/admin/audit` page gains actor/subject/date filters and a csv export.
  - Regression: `tests/e2e/p27-search-audit.spec.ts`; the search and projection rebuild buttons on the existing `/admin/jobs` page cover the newly registered subject types (no new admin route).
KEY FILES: src/platform/document/extract.ts; src/platform/scheduler/queues.ts; apps/worker/src/handlers/document-extract.ts; prisma/schema/enums.prisma; prisma/migrations/<stamp>_p27_job_kind/migration.sql; src/app/api/uploads/route.ts; src/app/(admin)/admin/audit/page.tsx; src/modules/committees/registry.ts; src/modules/meetings/registry.ts; src/modules/resources/registry.ts; tests/e2e/p27-search-audit.spec.ts
TESTS:
  - tests/unit/search/extract.test.ts: text extraction from the PDF, DOCX and CSV fixtures, with a corrupt file failing cleanly and leaving the version unindexed.
  - tests/unit/feature/seeds.test.ts (final assertion): every key listed in `prisma/seed/features/index.ts` has a compile snapshot, a simulate happy path and a simulate rejection path - the complete seeded set is committee, committee_report, task, case, portfolio, cqi, campaign, appointment, meeting, annual_plan, planned_activity, quarterly_report, exam_schedule, lab_schedule, load_cycle, import_batch, student_issue, announcement, course_offering and generic_request.
  - tests/integration/search/acl.test.ts (extended): results are filtered by `aclTokens` and post-checked by `can()`; cross-department entries are never returned; text inside an uploaded ToR PDF is searchable after extraction; a rebuild reproduces the index for every registered subject type.
  - tests/integration/audit/panels.test.ts: `history(subjectRef)` merges field changes and transitions in time order for a portfolio and a meeting; `audit_event` and `workflow_transition_log` still reject UPDATE and DELETE as `dept_app` (P4's append-only migration).
  - tests/integration/tenancy/rls-explain.test.ts (new): EXPLAIN for the committee, task, portfolio, campaign and announcement list queries shows index scans on the departmentId-first indexes.
  - tests/worker/document-extract.test.ts: the handler produces text for the PDF and DOCX fixtures exactly once per version under its ledger key and re-indexes the linked subjects.
  - tests/e2e/p27-search-audit.spec.ts: DH searches 'curriculum' and finds the committee, a task, the minutes document and text inside the ToR PDF while an instructor sees only permitted hits; 'Rebuild search index' from /admin/jobs leaves the results unchanged; the audit panel on a portfolio shows before/after values and transitions and /admin/audit filters by actor and date and exports csv.
VERIFY:
  $ docker compose run --rm web npm install
  $ docker compose run --rm web npx prisma migrate dev --create-only --name p27_job_kind
  $ docker compose run --rm web npx prisma migrate dev
  $ docker compose run --rm web npx prisma generate
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test tests/e2e/p27-search-audit.spec.ts
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(search): module search indexing, PDF/DOCX text extraction queue and audit panel coverage" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
MANUAL: Search for a course code and see results grouped by type including a hit inside an uploaded PDF, run the search rebuild from /admin/jobs, then open the audit panel on a portfolio and on a meeting and filter /admin/audit by actor and date before exporting the csv.
COMMIT: feat(search): module search indexing, PDF/DOCX text extraction queue and audit panel coverage

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

######## P28 — Retention, hardening, full regression and v0.2.0
GOAL: (2 days) Finish the retention job and its dry run, apply P11's rate limiting and a nonce CSP to the public routes, add the antivirus seam, the SMS channel stub and channel preferences on P5's settings page, run the whole p12-p28 regression against the production build and tag v0.2.0 inside the container.
DEPENDS: ['P27']
DELIVERABLES:
  - `apps/worker/src/handlers/retention-run.ts` is completed: document retention classes through P6's `document_version_purge()` while skipping locked versions, expired campaign token purge, published `DomainEvent` rows older than 30 days through the `purge_domain_events()` SECURITY DEFINER function, and `ScheduledJob` rows in sent|done|cancelled older than 90 days; its dry-run listing is surfaced on the existing `/admin/jobs` page beside the projection and search rebuild buttons (no new admin route).
  - Hardening: P11's `src/lib/rate-limit.ts` is applied to `/a/[hostSlug]`, `/a/[hostSlug]/[token]` and `/api/auth/*` (429 with Retry-After) beside P8's `/c/[token]` limiter; P11's `next.config.ts` `headers()` gains the nonce CSP (nonce set in `src/proxy.ts`) on top of the HSTS, X-Frame-Options DENY, Referrer-Policy and Permissions-Policy headers it already ships; `src/platform/document/scan.ts` adds a no-op antivirus provider behind an interface, called from P6's upload route.
  - Channels: `src/platform/scheduler/channels/sms.ts` (a `ChannelAdapter` stub that records the delivery as skipped) beside P5's in-app and email channels, and P5's `src/app/(app)/d/[dept]/settings/notifications/page.tsx` is EXTENDED with the `ChannelPreference` editor for the new channel plus digest and quiet hours (no second notifications route). `/admin/roles` gains a read-only 'preview permissions as user' panel.
  - Regression and release: `playwright.config.ts` gains the project `all` (`dependencies: ['setup']`, every p12-p28 spec in order) beside `setup`, `chromium` and `milestone-1`; `tests/e2e/p28-hardening.spec.ts`; `CHANGELOG.md` for v0.2.0; the phase runs the production image build (guard rail 5) and ends with the commit plus the in-container annotated tag `v0.2.0`.
KEY FILES: apps/worker/src/handlers/retention-run.ts; src/app/(admin)/admin/jobs/page.tsx; src/lib/rate-limit.ts; next.config.ts; src/proxy.ts; src/platform/document/scan.ts; src/platform/scheduler/channels/sms.ts; src/app/(app)/d/[dept]/settings/notifications/page.tsx; src/app/(admin)/admin/roles/page.tsx; playwright.config.ts; tests/e2e/p28-hardening.spec.ts; CHANGELOG.md
TESTS:
  - tests/unit/scheduler/channels.test.ts: the sms adapter records a skipped delivery without throwing and channel selection honours category preferences, digest and quiet hours.
  - tests/integration/security/rate-limit.test.ts: the sliding window rolls over and the 31st request to a public route inside the window returns 429 with Retry-After, while an authenticated app route is unaffected.
  - tests/integration/security/headers.test.ts: every response carries the nonce CSP, HSTS, X-Frame-Options DENY, Referrer-Policy and Permissions-Policy, and an inline script without the nonce is rejected by the policy.
  - tests/worker/retention-run.test.ts: retention soft-deletes an expired document, skips a locked minutes version, purges published domain events and old ledger rows, and the dry run reports the same set without deleting anything.
  - tests/e2e/p28-hardening.spec.ts: a burst of 30 requests to the public appointment route returns 429; the channel preference page disables email for one category and the next notification has no email delivery; the retention dry run lists candidates from /admin/jobs; /admin/roles previews an instructor's effective permissions.
  - Full regression: the `all` Playwright project runs every p12-p28 spec against the production build.
VERIFY:
  $ docker compose run --rm web npx prisma db seed
  $ docker compose run --rm web sh -c "npx prisma validate && npm run schema:check && npx next typegen && npx tsc --noEmit && npx eslint . && npm run rls:check"
  $ docker compose --profile test run --rm test npx vitest run --project unit --project components
  $ docker compose --profile test run --rm test npx vitest run --project integration --project worker --coverage
  $ docker compose -f compose.yaml -f compose.e2e.yaml up -d --wait web worker
  $ docker compose -f compose.yaml -f compose.e2e.yaml --profile e2e run --rm e2e npx playwright test --project all
  $ docker compose -f compose.yaml -f compose.e2e.yaml down
  $ docker compose -f compose.prod.yaml build web worker
  $ docker compose run --rm web git add -A
  $ docker compose run --rm web git commit -m "feat(platform): retention job, public-route rate limiting, nonce CSP, upload scanning, sms channel seam and full regression" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
  $ docker compose run --rm web git tag -a v0.2.0 -m "Milestones 2-5: planning, invigilation, labs, resources, staff and load, communication, search, retention and hardening"
MANUAL: Run the retention dry run from /admin/jobs and confirm a locked minutes version is skipped; hammer the public appointment link and see the 429 with Retry-After; disable email for one notification category in /d/cs/settings/notifications and confirm the next notice arrives in-app only; preview an instructor's permissions in /admin/roles; confirm `docker compose run --rm web git tag` lists v0.2.0.
COMMIT: feat(platform): retention job, public-route rate limiting, nonce CSP, upload scanning, sms channel seam and full regression

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
